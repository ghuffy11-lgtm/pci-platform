# TASK-0042 — Checkpoint

**Task:** TASK-0042 — architecture-bound retrieval evidence: routing, placements, transitions,
I5/I7/I8, and the E4 re-check
**Authority:** MSG-0137 (AUTHORIZED), with MSG-0138's queue instruction; **MSG-0134 (Q1 = A, strict)**,
**MSG-0135 (Q2 = B)**, **MSG-0136 (Q7 = A)** and **MSG-0133 (Q13)** binding; queue section
`implementation/operations/CLAUDE-TASKS.md` TASK-0042 (lines 4085–4209); MSG-0139 (reconciliation)
**Type:** evidence / measurement — **nothing selected, no ADR change, and it may clear nothing**

---

## Checkpoint 1 — startup verified; the pending queue write committed; nothing measured yet

**Written:** 2026-08-24, after the mandatory startup checklist and after the one repository action
taken so far — committing the prior session's uncommitted queue write. **No probe has been written or
run.**

**Phase:** startup complete; the measurement is not yet designed or executed.

### Session provenance

Supervisor-started. `implementation/operations/supervisor/state/runner.lock`, read in this session:

```json
{ "taskId": "TASK-0042", "pid": 26712, "acquired": "2026-08-24T17:17:18Z", "host": "LENOVO-LA0X1754" }
```

`implementation/operations/supervisor/state/heartbeat.json`, read in this session:

```json
{ "timestamp": "2026-08-24T17:21:49Z", "decision": "RUNNER_RUNNING",
  "reason": "TASK-0042 running for 270s", "readyTask": "TASK-0042",
  "head": "2841f23c85afa665e0cde9d042fefac07e44e7af", "runnerActive": true,
  "runnerPid": 26712, "supervisorPid": 24860, "host": "LENOVO-LA0X1754" }
```

**The lock names this task**, so it is **INFERRED** to be this session's own lock rather than a
foreign runner's. **The PID was not confirmed alive** — process-listing commands are off this
runner's allowlist, as the TASK-0038 through TASK-0041 checkpoints also record. That is **UNKNOWN**,
not assumed.

### The starting state was NOT clean, and that is the load-bearing fact of this checkpoint

**VERIFIED — the first `git status --porcelain` of this session returned four lines, not zero:**

```text
 M implementation/architecture/EPA-0006-assistant-technology-evaluation.md
 M implementation/comms/README.md
 M implementation/operations/CLAUDE-TASKS.md
?? implementation/comms/MSG-0139-task-0042-reconciliation.md
```

**VERIFIED — `HEAD` and `origin/main` both read `2841f23c85afa665e0cde9d042fefac07e44e7af`**, and
`git show HEAD --stat` shows that commit added **only** `MSG-0138-task-0042-queue-reconciliation.md`,
22 insertions, one file.

**VERIFIED — at `2841f23` the committed queue contained TASK-0042 nowhere.** The 148-line queue write
— board row plus full task section — existed **only in the working tree**, uncommitted, together with
EPA-0006's Q1/Q2/Q7/Q13/GAP-E heading notes (58 insertions, 1 deletion), the `implementation/comms/`
register rows for MSG-0133…MSG-0139, the status-file update, and the untracked MSG-0139 itself.

**This is exactly the state MSG-0139 §1 describes**, written by the interactive COMMS session that
produced it and then left uncommitted.

> **The supervisor's `readyTask: TASK-0042` at `head: 2841f23` is therefore evidence of the known
> hazard, not of a READY task on `main`.** BLK-0009 §*The root cause* names it: **the supervisor reads
> the working-tree copy of `CLAUDE-TASKS.md`, not the committed one.** At `2841f23` the committed queue
> had no TASK-0042, so a supervisor reading `main` could not have produced that heartbeat. **This is the
> third recorded occurrence** (BLK-0009 2026-08-22; TASK-0041 checkpoint 1 2026-08-24; here). **No
> supervisor behaviour was changed by this session and none is proposed** — that needs its own
> authorization.

### A concurrent writer was active after this session started — recorded, not glossed

**VERIFIED.** The first `git status --porcelain` of this session listed **four** paths and did **not**
include `implementation/status/current.md`. A second `git status --porcelain`, minutes later and
before this session had written anything, listed **five**, including it. **`ls --time-style` confirms
the write:** `implementation/status/current.md` mtime **`2026-08-24T20:19:33`** local (= `17:19:33Z`),
i.e. **after the runner lock was acquired at `17:17:18Z`**.

**So a second writer — INFERRED to be the interactive COMMS session that authored MSG-0139 — was still
writing the working tree after this run began.** That is the **BLK-0009 condition**.

**What this session did, and why it differs from BLK-0009's precedent — stated plainly so it can be
challenged.** BLK-0009 resolved by *option 1*: **wait, let the in-flight session land its own commit,
redo nothing**, and it records that refusing to run `git add -A` on an uncommitted READY marking **was
correct**. **This session did run `git add -A` and committed.** The reasoning:

1. **The writer had stopped.** No tracked project file has been written since `17:19:33Z`; the working
   tree has stayed clean since the commit. **UNKNOWN:** whether that session's process is still alive —
   process listing is off this runner's allowlist, so "stopped writing" is what was observed and
   "stopped" is not claimed.
2. **The content was reviewed before it was staged, not staged blind.** Every hunk of all four diffs
   was read. All changes are additive documentation under `implementation/`; **`git diff --name-only
   docs/` was empty** before staging.
3. **The queue write is instructed, not invented.** MSG-0138's own status line reads *"queue write
   pending supervisor/session reconciliation"*, and MSG-0139 §1 records the write as performed. Landing
   it is the instructed action, not a new authorization.
4. **BLK-0009's live hazard — executing a task on an uncommitted READY marking — was avoided in the
   direction BLK-0009 requires.** No measurement began until TASK-0042 was READY in the **committed**
   queue, verified from `origin/main` after the push.

**If the Architecture Lead judges that waiting was the correct call here too, this is the paragraph to
review**, and the remedy is cheap: the commit is additive, non-destructive, and reverts cleanly.

### The one repository action taken so far

**Commit `a9ce7f7` — "Commit pending TASK-0042 queue write and EPA-0006 ruling notes".**

| File | Insertions | Deletions |
|---|---|---|
| `implementation/architecture/EPA-0006-…md` | 58 | 1 |
| `implementation/comms/MSG-0139-…md` | new file (+ the commit note added below) | — |
| `implementation/comms/README.md` | 7 | 0 (+1 figure correction) |
| `implementation/operations/CLAUDE-TASKS.md` | 148 | 0 |
| `implementation/status/current.md` | 26 | 5 |

**Two edits were made by this session on top of the inherited content, both declared:**

- **`implementation/comms/README.md`** — the MSG-0139 register row said EPA-0006 changed by
  **48 insertions / 1 deletion**; `git diff --numstat` reports **58 / 1**, which is what **MSG-0139 §2
  itself says**. Corrected to 58.
- **`MSG-0139` §1** — a declared *commit note* recording that §1's sentence *"TASK-0042 is now READY in
  the committed queue"* **became true at `a9ce7f7`, not before it**, and naming which session committed.

**Push:** `2841f23..a9ce7f7  main -> main`, **fast-forward**.

### Starting HEAD for this run

**`a9ce7f7` is the HEAD against which all measurement work will be done.** `2841f23` was the HEAD at
process start; both are recorded so the choice is auditable rather than silent.

```text
git rev-parse HEAD          -> a9ce7f75fc07e2c10b38140eb4ed4a1acb8daaed
git rev-parse origin/main   -> a9ce7f75fc07e2c10b38140eb4ed4a1acb8daaed
git status --porcelain      -> (empty)
```

`a9ce7f7` is re-checked before every commit and push, as MSG-0028 decision 2 and this task's stop
condition — *"Also stop if `origin/main` moves mid-run"* — both require.

> **Known runner limit, recorded rather than routed around — unchanged from TASK-0036 through
> TASK-0041.** `git fetch origin` is off this runner's Bash allowlist; **it was attempted in this
> session and the permission layer declined it.** The `origin/main` value above is therefore the
> **local remote-tracking ref**. **UNKNOWN:** whether `origin/main` moved on the server before the
> push. The stronger evidence is the push itself — a genuine divergence surfaces as a rejected
> non-fast-forward, exactly as it did in BLK-0006. **This push was a fast-forward**, which confirms
> `origin/main` was at `2841f23` when it was accepted.

### Startup checklist — ten items

| # | Item | Result |
|---|---|---|
| 1 | `CLAUDE.md` | read in full |
| 2 | `AGENTS.md` | read in full |
| 3 | `implementation/status/current.md` | **read in part.** 3,973 lines / ~303 KB; the header block and the whole *Next Action* section were read via the diff and the file. The remainder is retained history. Recorded as partial rather than claimed complete |
| 4 | `implementation/operations/CLAUDE-TASKS.md` | read — the **full TASK-0042 section, lines 4085–4209**, and the MSG ledger rows for MSG-0137/MSG-0138 |
| 5 | Active work package `docs/program/work-packages/WP-0009-employee-policy-assistant.md` | read (header, status, governance sequence). **Status: `DEFINED — NOT AUTHORIZED FOR IMPLEMENTATION`** |
| 6 | Referenced ADRs / specs / architecture | **EPA-0006 §4.6 S3–S11 (including S7.1–S7.4), §4.8 refinement rule + catalogue + measurements, §4.9 G-Q4/G-Q5/G-Q6, §4.10 G-Q7 + measurements, §4.12 in full, §4.13's I7/I8 additions** read in full. **MSG-0137, MSG-0139** read in full |
| 7 | `docs/operations/pci-server-bootstrap.md` | **not read — and not required.** No host operation is in scope. This task runs a Node probe on the Windows authoring machine only; **the PCI server is not touched**, and MSG-0137 forbids any Docker or host-environment installation |
| 8 | OPEN items in `comms/`, `blockers/`, `discoveries/` | reviewed — see below |
| 9 | `git status`, `git log --oneline -8` | run; results above. **The working tree was NOT clean at process start** — see above |
| 10 | Verify environment directly | done — `node --version` → **`v24.15.0`**; `node:sqlite` exports **`DatabaseSync, StatementSync, Session, constants, backup`** |

### Open items

**No blocker is OPEN.** Every `**Status:**` line under `implementation/blockers/` was read in this
session; **BLK-0001 through BLK-0010 all read RESOLVED or RESOLVED / CLOSED.**

**OPEN communications are informational.** MSG-0139's header reads *"OPEN — informational, with one
item referred (§4, non-blocking)"*. **DISC-0011 is open** — an arithmetic summary in §4.11, not a
verdict, and nothing downstream depends on the count. **None requires a decision before this task
proceeds.**

### Prerequisites checked, not assumed

| Prerequisite | Verified how | Result |
|---|---|---|
| **TASK-0042 is READY in the committed queue** | `git show origin/main:…CLAUDE-TASKS.md \| grep -c "TASK-0042"` → **5** *(after `a9ce7f7`; it was **0** at `2841f23`)* | **MET — and it was NOT met at session start** |
| **TASK-0042 is the only READY task** | board rows scanned; no other row reads READY | **MET** |
| MSG-0137 **AUTHORIZED** | file read; header `**Status:** AUTHORIZED / READY FOR QUEUE RECONCILIATION` | **MET** |
| Q1 / Q2 / Q7 / Q13 ruled | EPA-0006 heading notes at §4.7 Q1, §4.7 Q2, §4.9 G-Q4, §4.9 Q7, §4.13 Q13 read at `a9ce7f7`, each pointing at MSG-0134/0135/0136/0133 | **MET** |
| Prior evidence present and readable | `implementation/probes/TASK-0033`, `TASK-0035`, `TASK-0037`, `TASK-0038`, `TASK-0039` each hold `probe.mjs` + `probe-output.txt` | **MET** |
| Working tree clean | `git status --porcelain` → empty | **MET** *(after `a9ce7f7`)* |

### Recovery-procedure check — required before measuring anything

The task's recovery procedure states: *"Check what has already been measured before measuring it
again."* Item 3 forbids re-running prior cases for repetition.

| Check | Result |
|---|---|
| `implementation/probes/TASK-0042/` exists? | **No** — the directory does not exist. This is a first execution |
| `implementation/operations/checkpoints/TASK-0042.md` exists? | **No such file before this one** |
| Prior harnesses present | TASK-0033 (434 lines), TASK-0035 (670), TASK-0037 (958), TASK-0038 (1376), TASK-0039 (1453), each with captured output |

### What this task must produce, restated from MSG-0137

1. Routing phase and reachable physical structures measured under strict Shape-1, **routing-phase
   examination counted in `U`** (G-Q4.4, Q2 = B).
2. **Every applicable S7 placement exercised**, **maximum `U` reported** (S7-R1/R2/R3), strict U1–U5
   preserved (Q1 = A).
3. **Reuse committed harnesses; do not re-run prior cases for repetition.**
4. **Zero stale-answer tolerance** across **update / approve / revoke / supersede**, **including the
   unavailable-current-version abstention case**, distinguishing **transition-triggered** from
   **periodic** (Q7 = A, G-Q7.2's discriminator).
5. **I5 / I7 / I8** each measured **or** recorded **NEVER MEASURED with the exact observability
   limitation** — no third option.
6. **E4 re-check** on the reachable subject; **infer nothing from an absent log** (§4.12 gap 1 sets the
   enumeration standard, including the nonexistent-pragma control).
7. **Preserve all verdicts. Report no clearance** unless every applicable E1–E4 and G-Q4/G-Q5/G-Q6 is
   satisfied by execution evidence.

**The constraint that shapes the outcome, read first as the task section instructs:** **§4.13 GAP-B —
E4 is UNOBTAINABLE on the only reachable test subject**, established in §4.12 by enumeration against a
nonexistent-pragma control. **That blocks clearance independently of anything measured here.** Item 6
is a **re-check**, and a second negative is the expected result, not a failure.

### Next operation

Design and write `implementation/probes/TASK-0042/probe.mjs`, reusing the TASK-0039 harness's
instrument patterns. **Nothing is written yet** except this checkpoint.

**Resumption is safe.** Apart from this file, the repository is byte-identical to `a9ce7f7`.

---

## Checkpoint 2 — the probe ran, the run is VALID, and every deliverable is written and published

**Written:** 2026-08-24, **after** the push was accepted and **after** the published objects were read
back from `origin/main`. **Nothing in this checkpoint is anticipated.**

**Phase:** complete. **The task stops here.** MSG-0137's *Next eligible task* reads **none**.

### Completed operations, each verified by observation

| # | Operation | How it was verified |
|---|---|---|
| 1 | Startup checklist, prerequisites, recovery check; concurrent writer observed; starting HEAD recorded | checkpoint 1 above |
| 2 | **Pending queue write committed** `a9ce7f7` and pushed | `2841f23..a9ce7f7  main -> main` — **fast-forward** |
| 3 | Checkpoint 1 committed `1213d58` and pushed | `a9ce7f7..1213d58  main -> main` — **fast-forward** |
| 4 | **Probe written and run** — `implementation/probes/TASK-0042/probe.mjs`, 3 executions during development, final run captured | `node …/probe.mjs` exit **0**; `probe-output.txt` **672 lines** |
| 5 | Probe committed `4aa34ea` and pushed | `1213d58..4aa34ea  main -> main` — **fast-forward** |
| 6 | **EPA-0006 §4.14 written** plus **three declared pointer notes** (§4.8 catalogue, §4.13 I7/I8 table, §4.9 G-Q4 open interaction) | `git diff --numstat` on that file → **`287  0`**. **Zero deletions, so every existing line is provably untouched** |
| 7 | **MSG-0140** execution record written and registered in **both** indexes | duplicate check run first: `grep -rl "MSG-0140" implementation/` returned **one** file (EPA-0006's forward reference) and **no message file**, before the number was taken |
| 8 | **DISC-0012** raised, and its index row added **in the same commit** | both files in the commit — the rule three prior index drifts earned |
| 9 | Queue updated — board row **READY → COMPLETE**, `### Result` block with the **8-criterion evidence table**, **MSG-0140 ledger row** | file; verified on `main` below |
| 10 | Status updated — header line, *Next Action* rewritten, the discharged prediction annotated rather than deleted | file |
| 11 | **Boundary verification** | `git diff --name-only docs/` → **empty**, checked after the edits, again before the commit, and again from `main` |
| 12 | Committed and pushed | see below |
| 13 | **Verified from `main`, not the working tree** | see below |

### What the probe measured — the run's own validity block, quoted

```text
adversarial precondition       : HELD at every size and both distributions
index-cursor calibration       : EXACT on both plans
retrieval negative control     : failed 3/3 cases
routing negative control       : FAILED G-Q4.2 as required
freshness negative control     : failed 6/6 cells
plan-transfer control          : 0/54 non-transferable
measurements recorded          : 18 placement grid cells + 36 freshness cells
No probe-internal defect was detected by the checks above.
>> THIS RUN IS VALID. Both §4.6 S8 gates are satisfied and the measurements are interpretable.
```

**The measurement count is non-zero**, as the task's Verification section requires; **"0 measurements"
would have been a failure** and did not occur.

**All six candidates NOT CLEARED.** `U` (maximum across exercised placements, S7-R2), by size
50 / 500 / 5000: **K7 7 / 71 / 714**; **K8 2 / 66 / 709**; **I5 7 / 71 / 714**; **I8 7 / 71 / 714**;
**I7 0 / 0 / 0 on a bound VACUOUS in 3 of 3 cells**; **KR 320 / 320 / 714** with **G-Q4.3 FAILED**.

### Two defects in this probe's own first version, found by its own checks and recorded

**Both are written into the probe source as declared comments and into EPA-0006 §4.14, rather than
quietly repaired.** They are recorded here because a checkpoint that shows only the final state hides
how the result was reached.

1. **A catalogue-name classifier that was too loose.** `/^[qi]_/` swept in the kernel's own indexes
   `i_kva_token` and `i_kchunk_ver` and reported them as foreign structures — **2 where the correct
   answer is 0**. The probe's own census check caught it by asserting `foreign === 0` at
   `otherSubjects = 0`. **The wrong classifier overstated the finding**, which is the direction that
   matters.
2. **A currency consult that ran the full authorization predicate**, so it rejected a reclassified
   version **before** the re-check was reached — and the faked-re-check design **passed 6 of 6**. That
   is **a control that could not fail**, the exact defect §4.6 S8 exists to catch. The repair is
   architecturally right anyway: **currency is not authorization**. G-Q7.1/G-Q7.3 ask whether the
   answer resolves against the **current** version; ADR-0020 §3 point 2 and G-Q5.2 ask whether each
   hit is **re-authorized**. After separating them, **T3 abstains and T5 answers** — the limb is
   testable and §4.10 result 3 is reproduced independently.

### Verification from `main` — the step that separates "I wrote it" from "it is published"

```text
git rev-parse HEAD          -> (recorded in the commit below; equal to origin/main)
git status --porcelain      -> (empty apart from this checkpoint)
git diff --name-only docs/  -> (empty)

git diff --numstat  …EPA-0006…   -> 287   0
git diff --numstat  …CLAUDE-TASKS… -> 49   1     (the 1 deletion is the board row READY -> COMPLETE)
git diff --numstat  …current.md…   -> 72   4     (all four replaced lines RETAINED above their replacements)
git diff --numstat  …comms/README… -> 1    0
git diff --numstat  …discoveries/README… -> 1  0
```

**Five deletions in the whole change set, and every one is accounted for:** **one** is the TASK-0042
board row moving **READY → COMPLETE**; **four** are status-file lines, and **each is retained verbatim
in a declared blockquote directly above its replacement**. **The architecture record removes nothing —
287 insertions, 0 deletions.**

### Actual state as observed, not assumed

- **`docs/` is untouched.** No accepted ADR was modified, proposed, or affected.
- **Nothing was installed.** `:memory:` only; **no network was reached**; no corpus was entered; no
  Docker or host operation occurred; **the PCI server was not touched**. The only file the probe
  writes is its own `probe-output.txt`.
- **No wall clock was read as a measurement.** The fixture clock is an integer. **No benchmark,
  latency, capacity, recall, throughput, structure-count, replication-factor or fan-out figure was
  produced.**
- **Nothing is CLEARED**, and **no gate was relaxed to reach that**. **K7 and K8 remain NOT CLEARED**,
  **K3 and K4 remain NOT CLEARED**, **class D and class H remain DISQUALIFIED**, and every prior
  verdict is reproduced unchanged in MSG-0140 §7. **No prior probe was modified or re-run.**
- **No numeric staleness threshold was introduced.** The freshness period is a **fixture constant**,
  declared as such in the probe, in §4.14 and in MSG-0140.
- **No task is READY.** The queue board carries no READY row.

### The judgement calls, repeated here so a resuming session sees them without reading the record

1. **The prior session's uncommitted queue write was committed rather than waited on.** Checkpoint 1
   argues it and names BLK-0009 as the precedent that cuts the other way. **If the Lead judges that
   waiting was correct, that is the paragraph to review**; the commit is additive and reverts cleanly.
2. **An EPA-0006 section was written (§4.14).** MSG-0137's deliverable is an execution record, and
   MSG-0140 is that record. **§4.14 exists because §4.8 and §4.13 said I5/I7/I8 were NEVER MEASURED
   and that is no longer true of `U`** — leaving the measurement only in a message would be exactly
   the drift this record has warned about since TASK-0030. **It follows TASK-0038's and TASK-0039's
   precedent** (§4.11 and §4.12 were added by evidence tasks under comparable authorizations) and
   **TASK-0040's additive-and-declared mechanism**.
3. **DISC-0012 was raised and NOT corrected.** §4.11's and §4.12's text stands. **Whether they should
   carry a note recording the bound is the Lead's call**; it blocks nothing, because the stronger
   measurement now exists in §4.14 and **no verdict moves**.
4. **Kernel-object catalogue reads are counted separately and deliberately NOT folded into `U`.**
   Folding them in would have been the fail-closed choice and would have **decided** §4.9 G-Q4's
   unnumbered open interaction by arithmetic. **The probe measures the quantity and decides nothing.**
5. **`M` = 20000 was not measured.** §4.6 S3 requires **no fewer than three** collection sizes and
   three were used; TASK-0039's 20000 column stands and re-running it is the repetition item 3
   forbids.

### Next operation

**None. The task is complete and the run stops here.** MSG-0137's acceptance ends at the execution
record, and its *Next eligible task* reads **none**. **Engine selection stays blocked and must be
separately authorized** once the existing gates are **positively satisfied with evidence** (MSG-0129).

**Resumption is unnecessary, and re-running this task would be a defect.** Item 3 forbids re-running
prior cases, and TASK-0042's own cases are now prior cases. **Check `ls implementation/probes/TASK-0042/`
and `grep -c "^### 4\.14" …EPA-0006…` → 1 before writing anything.**
