# TASK-0026 — Checkpoint

**Task:** TASK-0026 — A-SURVEY (bounded corpus survey) and A-STACK (stack evaluation)
**Authority:** MSG-0076 (AUTHORIZED) · queue section `CLAUDE-TASKS.md` §TASK-0026 · reconciliation MSG-0077
**Work package:** WP-0009 — Employee Policy Assistant

---

## Checkpoint 1 — startup complete, prerequisites checked

**Written:** 2026-08-22, after the startup checklist and the prerequisite inspection were both
completed and their results observed. Not written in anticipation of anything.

### Session identity

Started by the Execution Supervisor. `runner.lock` read at startup:

```json
{ "taskId": "TASK-0026", "pid": 27312, "acquired": "2026-08-22T06:57:17Z", "host": "LENOVO-LA0X1754" }
```

The lock names **this** session's task and PID. It is not a foreign or stale runner, so no
concurrent-runner stop condition applies. The heartbeat corroborated it
(`RUNNER_RUNNING`, `readyTask: TASK-0026`, `runnerPid: 27312`).

### Starting HEAD — record this; the abort rule depends on it

```text
HEAD        69a4d03d0bec45a98aa80211a609e6ed57e44f25
origin/main 69a4d03d0bec45a98aa80211a609e6ed57e44f25
git status  clean
```

**Local and `origin/main` agreed at session start, and the working tree was clean.** Per CLAUDE.md
*Mid-run repository movement*, this value is the baseline: it is re-checked before any commit or push,
and a move aborts the run at the next safe checkpoint.

> **Known runner limit, carried from the queue section:** `git fetch` is off the runner allowlist, so a
> mid-run move by another actor is detectable only when a push is **rejected**. That is a real limit on
> this evidence, stated rather than papered over. It is not routed around.

### Startup checklist — as executed

| # | Item | Result |
|---|---|---|
| 1 | `CLAUDE.md` | READ in full |
| 2 | `AGENTS.md` | READ in full |
| 3 | `implementation/status/current.md` | READ (paginated; header, queue, and Open Communications sections) |
| 4 | `implementation/operations/CLAUDE-TASKS.md` | READ — board + the full TASK-0026 section (lines 1483–1630) |
| 5 | Active work package — `WP-0009` | READ in full |
| 6 | Referenced ADRs | **ADR-0017 … ADR-0022 all READ in full**, from `docs/decisions/`; plus ADR-0015 (to establish what is *not* inherited) and `technology-selection-principles.md` |
| 7 | `docs/operations/pci-server-bootstrap.md` | **Not read — and not required.** The checklist conditions it on a host operation. **TASK-0026 performs none**: it is documentary work on the Windows workstation, touches no PCI server, and the `/data` boundary is not engaged. Recorded explicitly rather than silently skipped |
| 8 | OPEN comms / blockers / discoveries | **MSG-0060** and **MSG-0077** OPEN, both READ. **No blocker is OPEN** — `implementation/blockers/README.md` states "**No blocker is open.**" No open discovery obstructs this task |
| 9 | `git status`, `git log` | Clean; HEAD = `origin/main` = `69a4d03` (above) |
| 10 | Verify current state directly | Done — see the prerequisite inspection below |

### Task eligibility — checked, not assumed

- **TASK-0026 is READY** and is the **single** READY task on the board (row for TASK-0026;
  every other row reads COMPLETE or ABORTED).
- **MSG-0076 is AUTHORIZED** and carries the scope, constraints and six acceptance criteria.
- **MSG-0076 §closing requires COMMS to reconcile it as the single READY task before execution.**
  That reconciliation is MSG-0077, committed in `69a4d03`. **Acceptance criterion 5 was therefore met
  before this session started**, by a prior session, and this session inherits rather than repeats it.
- Prerequisite chain verified: TASK-0025 COMPLETE; MSG-0071 DECIDED; **ADR-0017 … ADR-0022 all present
  in `docs/decisions/`** — confirmed by directory listing, all six files exist.

---

## The A-SURVEY prerequisite — re-checked by inspection, not inherited

The queue section requires this be established **by looking**, because the operator could have supplied
material after MSG-0077 was written, and a runner that trusted that text would then wrongly refuse to
survey a corpus that exists.

**It was re-checked in this session, on 2026-08-22, and the answer is unchanged: no approved policy
corpus is reachable.**

Evidence — a search of the whole working tree for document-like files of any policy-bearing format:

```text
glob **/*.{pdf,docx,doc,pptx,xlsx,rtf,odt,txt,html,htm,csv}
  services/kernel/node_modules/typescript/LICENSE.txt
  services/kernel/node_modules/typescript/ThirdPartyNoticeText.txt
```

**Two hits, both TypeScript dependency licence texts.** No PDF, no DOCX, no policy document of any
format exists anywhere in the tree.

And a search for anything named for policy, corpus, handbook or procedure:

```text
glob **/*{policy,corpus,handbook,procedure}*
  services/kernel/src/adapters/policy/static-policy-engine.ts   <- kernel source
  services/kernel/src/ports/policy.ts                           <- kernel source
  services/kernel/test/unit/policy.test.ts                      <- kernel source
  docs/product/licensing-and-open-source-policy.md              <- governance markdown
  docs/specifications/SPEC-0011-policy-and-authorization.md     <- governance markdown
  docs/decisions/ADR-0019-bilingual-policy-semantics.md         <- governance markdown
  … EPA / WP-0009 / MSG governance records
```

**Kernel source files named `policy`, and the project's own governance documents. No organizational
policy content of any kind.** The repository root additionally holds no `data/`, `corpus/`, `content/`
or `ingest/` directory — the listing is `.claude`, `deploy`, `docs`, `implementation`, `knowledge`,
`services`.

**Conclusion: PR5 is UNMET.** This matches — and does not merely repeat — WP-0009 §6.1, EPA-0004 §11.5
and MSG-0061 §7.5.

**Consequence for this run, per the queue section:** A-SURVEY **stops at this prerequisite**. It
produces no formats, no language mix, no scanned-document prevalence, no classification patterns, and
no version characteristics — not as estimates, not as illustrations, not as expected values. No survey
method or plan is substituted for the authorized output. **A-STACK proceeds**, and the task will be
reported **PARTIAL**.

### Last verified operation

The prerequisite inspection above. Nothing has been written to the repository at this checkpoint
beyond this file.

### Next operation

Execute **A-STACK** — evaluate candidate service-stack approaches against the accepted contracts and
the EPA ADR set, evaluating and **not selecting**.

### Resumption safety

**Safe.** No repository content outside this checkpoint has been modified; no host, service, or
external state has been touched; the operation performed so far is read-only inspection, which is
idempotent and may be repeated without risk.

---

## Checkpoint 2 — both halves resolved, records written, verification run

**Written after** the A-STACK deliverable was produced, all documentation updated, and the two
verification checks the queue section names were **executed and their output observed**. Not written
in anticipation.

### Phase

Documentation complete; **pre-commit**. The commit and push are the only operations remaining.

### Completed operations

| # | Operation | Verified how |
|---|---|---|
| 1 | Startup checklist + prerequisite inspection | Checkpoint 1 |
| 2 | **A-STACK executed** → `implementation/architecture/EPA-0005-assistant-stack-evaluation.md` created, **PROPOSED** | File created; content maps candidate approaches to the accepted EPA ADR set (§3, §6) and preserves every open selection (§9.2) |
| 3 | **A-SURVEY stopped at PR5** — no record created, no figure produced | Deliberate non-action. Confirmed by the absence of any survey artifact in `git status --porcelain` |
| 4 | Identifier collision checks before allocation | `EPA-0005` and `MSG-0078` each returned **no matches** repo-wide; COMMS register's highest row was MSG-0077; directory listing agreed. All three checks of the MSG-0035 convention |
| 5 | `implementation/architecture/README.md` index row + A-SURVEY note | Edited |
| 6 | `MSG-0078` execution record created | Six criteria mapped to evidence in §2 |
| 7 | `implementation/comms/README.md` register row | Edited |
| 8 | `CLAUDE-TASKS.md` — board row, task-section outcome, ledger row, A-SURVEY outcome note | Edited; superseded text retained as issued |
| 9 | `implementation/status/current.md` — header, task table, current position, OPEN messages, Next Action | Edited; superseded lines retained |
| 10 | `WP-0009` §6.2 A-STACK/A-SURVEY outcome rows and §8 prerequisite update | Edited — **PR5 UNKNOWN → VERIFIED UNMET** |

### Last verified operation

The two verification checks required by the queue section, run and quoted:

```text
$ git diff --name-only docs/decisions/
(no output)                       <- criterion 3: no accepted ADR modified

$ git status --porcelain
 M implementation/architecture/README.md
?? implementation/architecture/EPA-0005-assistant-stack-evaluation.md
?? implementation/operations/checkpoints/TASK-0026.md
                                  <- no path under docs/decisions/
```

Board check: **no row gained `READY`.** TASK-0026 moved READY → COMPLETE (PARTIAL); no other row
changed status.

> The porcelain above was captured **before** the remaining documentation edits. The pre-commit
> verification is re-run in full immediately before the commit, and the commit-time result — not this
> one — is the evidence for criterion 3.

### Acceptance criteria at this checkpoint

**5 MET, 1 UNMET.** Criterion 1 (corpus survey) UNMET on **PR5**; criteria 2, 3, 4, 5, 6 MET. Evidence
per criterion: MSG-0078 §2. **Task result: PARTIAL.**

### Actual external state observed

**None was touched.** No host operation, no service, no container, no network egress. This was
documentary work on the Windows workstation; the PCI server was not contacted and the `/data` boundary
was not engaged.

### Git commit / HEAD

Starting HEAD `69a4d03d0bec45a98aa80211a609e6ed57e44f25`, re-checked immediately before the commit.
**No mid-run movement was observed** — with the standing caveat from checkpoint 1 that `git fetch` is
off the runner allowlist, so a remote move surfaces only as a **rejected push**. If the push is
rejected, the run stops and records rather than forcing.

### Next operation

Commit the documentation set and push to `origin/main`; then report **PARTIAL**.

### Resumption safety

**Safe, with one instruction.** If this task is resumed after an interruption at this point:
**re-check the corpus question by inspection first** (the operator may have supplied material since),
and **check which records already exist rather than rewriting them** — `EPA-0005`, `MSG-0078` and the
index/queue/status edits may already be present. Per CLAUDE.md *Checkpointing and Recovery* rule (f),
do **not** repeat an operation merely because a checkpoint says it was incomplete.

---

## Checkpoint 3 — committed, pushed, reconciled; task terminal

**Written after** the push was accepted and the resulting state observed.

### Phase

**TERMINAL.** TASK-0026 is **COMPLETE (PARTIAL)**. Nothing remains to execute under MSG-0076.

### Commit and push — observed, not assumed

```text
commit  00a1765dece0e5d99cf1fad8274d9916d0e7b0c7
        "ops: execute TASK-0026 - A-STACK delivered, A-SURVEY stopped at PR5"
        8 files changed, 1109 insertions(+), 18 deletions(-)

push    69a4d03..00a1765  main -> main        (accepted)

$ git rev-parse HEAD origin/main
00a1765dece0e5d99cf1fad8274d9916d0e7b0c7
00a1765dece0e5d99cf1fad8274d9916d0e7b0c7

$ git status --porcelain
(clean)
```

**HEAD and `origin/main` agree, and the tree is clean.**

**The accepted push is itself the mid-run-movement evidence.** Checkpoint 1 recorded the starting HEAD
as `69a4d03` and noted that `git fetch` is off the runner allowlist, so a remote move would surface
**only** as a rejected push. The push was **accepted** and fast-forwarded from exactly `69a4d03` —
which is positive evidence that `origin/main` did not move under this session. That is a stronger
result than "no move was detected", and it is the strongest available under the allowlist limit.

### Pre-commit verification — the criterion-3 and criterion-4 evidence

Run at commit time; this result, not checkpoint 2's earlier snapshot, is the evidence:

```text
$ git diff --name-only docs/decisions/
(no output)                        <- no accepted ADR modified

$ git status --porcelain           (staged, pre-commit)
 M docs/program/work-packages/WP-0009-employee-policy-assistant.md
 M implementation/architecture/README.md
 M implementation/comms/README.md
 M implementation/operations/CLAUDE-TASKS.md
 M implementation/status/current.md
?? implementation/architecture/EPA-0005-assistant-stack-evaluation.md
?? implementation/comms/MSG-0078-task-0026-execution-record.md
?? implementation/operations/checkpoints/TASK-0026.md
                                   <- no path under docs/decisions/
```

**Criterion 4 was checked by search rather than by assertion.** The literal string `READY` occurs in
the queue at only one place outside the historical/retained text — the **status-value legend** — and in
EPA-0005 exactly once, in a **negation**: *"marks no task READY — T-A, T-B, T-D, T-E and T-0 remain
unauthorized."* **No board row is READY.**

### Final state

| Item | State |
|---|---|
| TASK-0026 | **COMPLETE (PARTIAL)** — 5/6 criteria MET; criterion 1 UNMET on PR5 |
| A-STACK | **COMPLETE** — `EPA-0005`, PROPOSED, selecting nothing |
| A-SURVEY | **NOT PERFORMED** — PR5 verified unmet; **no figures produced** |
| Accepted ADRs | **Untouched** — ADR-0017 … ADR-0022 unchanged |
| READY tasks | **None** |
| OPEN messages | **MSG-0060, MSG-0077, MSG-0078** |
| Blockers | **None OPEN** |
| Records reconciled | Queue, status, COMMS register, architecture index, WP-0009 §6.2/§8 — all describe the same state |

### Actual external state

**Untouched.** No host, service, container, or network operation occurred at any point in this task.

### Resumption safety

**Nothing to resume.** The task is terminal and its records are pushed. A new session can reconstruct
the entire outcome from `MSG-0078`, `EPA-0005`, and this checkpoint without the conversation that
produced them.

**The next action is not Claude's.** It is the organization's corpus decision and the Architecture
Lead's ruling on EPA-0005 — see MSG-0078 §8 and `implementation/status/current.md` *Next Action*. **No
task may be marked READY without the Architecture Lead's authorization.**
