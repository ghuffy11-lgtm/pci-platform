# TASK-0031 — checkpoint record

**Task:** apply ADR-0020 AMD-01 in place
**Authorization:** MSG-0095 (Architecture Lead ruling) — accept AMD-01 as drafted **with** the
optional traceability row, apply **in place**, add a concise header amendment note, do **not** create
a superseding ADR.
**Queue section:** `implementation/operations/CLAUDE-TASKS.md` → *TASK-0031 — apply ADR-0020 AMD-01
in place*.
**Type:** governance application, editing an **accepted, promoted** ADR — authorized here and only
here.

---

## Checkpoint 1 — pre-application state verified

**Phase:** startup and pre-state verification complete; no file modified yet.
**Written:** 2026-08-23, after the verification below was observed in this session.

### Starting HEAD — record it, and re-check before every push

```text
$ git rev-parse HEAD origin/main
dfb719df130f6118b525c4ee6e0f4cba666315a4     <- HEAD
dfb719df130f6118b525c4ee6e0f4cba666315a4     <- origin/main (local ref)

$ git status --short
(no output)                                   <- clean tree
```

**One caveat, stated rather than glossed.** `git fetch` is off this runner's Bash allowlist and was
**refused** in this session, exactly as the queue section's *Known runner limit* note predicts. So
`origin/main` above is the **local remote-tracking ref**, not a freshly fetched one. A mid-run move
of the true remote is therefore detectable only when a push is rejected. **No substitute was used**
and the denial was not routed around.

### Recovery rule (f) applied before acting — is the amendment already applied?

`CLAUDE.md` recovery rule (f) and this task's own recovery procedure both say the same thing, and it
is the specific failure this task could cause: re-running it against an already-amended ADR would
insert hunk 1 **twice**, and a duplicated clause in an accepted ADR is far worse than a missing one.

**Checked by reading `docs/decisions/ADR-0020-retrieval-projection-and-index-boundary.md` in full,
not by trusting any record. VERIFIED NOT APPLIED**, three ways:

| Check | Observed |
|---|---|
| §4 body | Ends at the sentence *"An exclusion cannot fail open; a filter can."* — **nothing follows it** before the `### 5.` heading |
| Traceability table | **11 rows**, none mentioning engine selection, G3 evidence, MSG-0092 or EPA-0005 |
| Header block | **No `Amended:` line**; no reference to AMD-01 or MSG-0095 anywhere in the file |
| Checkpoint file | No `TASK-0031.md` existed before this one — this is the **first** execution |

### Insertion point located exactly — the first stop condition, tested and cleared

The task stops if the insertion point cannot be located exactly, **or if §4's closing sentence
differs from what AMD-01 quoted**. It does not differ. AMD-01 §2 quotes §4 in full, and the accepted
copy matches it, including the MSG-0062 §7.6 block quote that precedes the closing sentence. The
insertion goes after that sentence and before the `### 5.` heading.

### Startup checklist

Items 1–6 and 8–10 completed in this session by reading the files directly. **Item 7
(`docs/operations/pci-server-bootstrap.md`) is not applicable and was not read**: it is required
"before any host operation", and this task performs none — it edits Markdown in the workstation
checkout. Stated rather than silently skipped.

**Resumption safe:** yes. Nothing has been modified.
**Next operation:** apply the three edits to ADR-0020.

---

## Checkpoint 2 — amendment applied, verified, committed and pushed

**Phase:** application complete and pushed; documentation reconciliation in the same run.
**Written:** 2026-08-23, after the push below was observed to succeed.

### Last verified operation

**The amendment is applied and pushed.**

```text
$ git commit  -> a1be892178dea11d62dee6693c7c8d7d80798e43
   2 files changed, 82 insertions(+)

$ git push origin main
   dfb719d..a1be892  main -> main
```

**The push succeeding is the mid-run-movement check**, not a formality. `git fetch` is off this
runner's allowlist, so a move of the true `origin/main` is detectable **only** as a rejected
non-fast-forward push. It was accepted, so `origin/main` was still at `dfb719d` — the HEAD recorded in
checkpoint 1 — at the moment of the push. **The repository did not move under this session.**

### Verification observed before the commit

```text
$ git diff --name-only docs/decisions/
docs/decisions/ADR-0020-retrieval-projection-and-index-boundary.md    <- and nothing else

$ git diff -- docs/decisions/ | grep '^-'
--- a/docs/decisions/ADR-0020-retrieval-projection-and-index-boundary.md   <- diff header only

$ git diff --stat -- docs/decisions/
 1 file changed, 15 insertions(+)                                     <- ZERO deletions
```

**Zero deletions exceeded what the criterion required**, and deliberately. The task's verification
anticipated a rewritten header line ("header line change aside"); the note was **added** as a new line
instead, so no existing line in the file changed at all. Every accepted semantic in ADR-0020 is
byte-identical to the promoted copy.

**Double-insertion guard, checked after applying:** the four new markers — hunk 1's first line, hunk
1's disclaiming sentence, hunk 2's traceability row, and the `**Amended:**` header line — occur
**exactly once each** (4 matches across 1 file).

**No technology leaked in:** a case-insensitive search of the whole amended file for twenty product
names (search engines, vector stores, model runtimes, frameworks, datastores) returns **no matches**.

### Supervisor state — checked directly, and it agrees

```text
runner.lock    : taskId TASK-0031, pid 27032, acquired 2026-08-23T04:57:18Z
heartbeat.json : RUNNER_RUNNING, readyTask TASK-0031, head dfb719d…, runnerPid 27032
```

The lock names **this** task and the heartbeat's observed `head` is **the same starting HEAD** this
checkpoint recorded. **UNKNOWN by direct check: whether the Windows scheduled task is Enabled or
Disabled** — `schtasks` is off the Bash allowlist and was refused. The status file records it as
`Disabled` with the supervisor path available by manual trigger, and the lock/heartbeat pair is
consistent with that account; it is **not independent confirmation of it**, and is not reported as
such.

### Runner limits hit in this run — recorded, not routed around

| Refused | Consequence | What was done instead |
|---|---|---|
| `git fetch` | `origin/main` in checkpoint 1 is the local tracking ref, not a live one | Reported as such; the accepted push is the real evidence |
| `python` | The intended file-level `diff` of both hunks against AMD-01 could not run | Compared with the permitted file/search tools; the applying commit's diff left as re-readable evidence |
| compound shell with redirection / process substitution | Same as above | Same |
| `schtasks` | Scheduler enabled/disabled state unverified | Reported **UNKNOWN** rather than inferred |

**No substitute path was used for any of them.**

**Resumption safe:** yes — **but do not re-apply.** The amendment is present exactly once. A session
resuming here should verify the four markers still occur once each and then continue with
documentation only.
**Next operation:** commit and push the documentation reconciliation (MSG-0097, queue, status,
comms register, WP-0009, this checkpoint).
