# TASK-0031 — checkpoint record

**Task:** apply ADR-0020 AMD-01 in place
**Authorization:** MSG-0095 (Architecture Lead ruling) — accept AMD-01 as drafted **with** the
optional traceability row, apply **in place**, add a concise header amendment note, do **not** create
a superseding ADR.
**Queue section:** `implementation/operations/CLAUDE-TASKS.md` → *TASK-0031 — apply ADR-0020 AMD-01
in place*.
**Type:** governance application, editing an **accepted, promoted** ADR — authorized here and only
here.

---

## Checkpoint 1 — pre-application state verified

**Phase:** startup and pre-state verification complete; no file modified yet.
**Written:** 2026-08-23, after the verification below was observed in this session.

### Starting HEAD — record it, and re-check before every push

```text
$ git rev-parse HEAD origin/main
dfb719df130f6118b525c4ee6e0f4cba666315a4     <- HEAD
dfb719df130f6118b525c4ee6e0f4cba666315a4     <- origin/main (local ref)

$ git status --short
(no output)                                   <- clean tree
```

**One caveat, stated rather than glossed.** `git fetch` is off this runner's Bash allowlist and was
**refused** in this session, exactly as the queue section's *Known runner limit* note predicts. So
`origin/main` above is the **local remote-tracking ref**, not a freshly fetched one. A mid-run move
of the true remote is therefore detectable only when a push is rejected. **No substitute was used**
and the denial was not routed around.

### Recovery rule (f) applied before acting — is the amendment already applied?

`CLAUDE.md` recovery rule (f) and this task's own recovery procedure both say the same thing, and it
is the specific failure this task could cause: re-running it against an already-amended ADR would
insert hunk 1 **twice**, and a duplicated clause in an accepted ADR is far worse than a missing one.

**Checked by reading `docs/decisions/ADR-0020-retrieval-projection-and-index-boundary.md` in full,
not by trusting any record. VERIFIED NOT APPLIED**, three ways:

| Check | Observed |
|---|---|
| §4 body | Ends at the sentence *"An exclusion cannot fail open; a filter can."* — **nothing follows it** before the `### 5.` heading |
| Traceability table | **11 rows**, none mentioning engine selection, G3 evidence, MSG-0092 or EPA-0005 |
| Header block | **No `Amended:` line**; no reference to AMD-01 or MSG-0095 anywhere in the file |
| Checkpoint file | No `TASK-0031.md` existed before this one — this is the **first** execution |

### Insertion point located exactly — the first stop condition, tested and cleared

The task stops if the insertion point cannot be located exactly, **or if §4's closing sentence
differs from what AMD-01 quoted**. It does not differ. AMD-01 §2 quotes §4 in full, and the accepted
copy matches it, including the MSG-0062 §7.6 block quote that precedes the closing sentence. The
insertion goes after that sentence and before the `### 5.` heading.

### Startup checklist

Items 1–6 and 8–10 completed in this session by reading the files directly. **Item 7
(`docs/operations/pci-server-bootstrap.md`) is not applicable and was not read**: it is required
"before any host operation", and this task performs none — it edits Markdown in the workstation
checkout. Stated rather than silently skipped.

**Resumption safe:** yes. Nothing has been modified.
**Next operation:** apply the three edits to ADR-0020.
