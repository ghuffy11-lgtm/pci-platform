# TASK-0035 — checkpoint

**Task:** TASK-0035 — physical projection isolation evaluation against strict Shape-1
**Authority:** MSG-0107b (AUTHORIZED); queue section `CLAUDE-TASKS.md` §TASK-0035
**Type:** architecture / evidence evaluation — selects nothing, deploys nothing

---

## Checkpoint 1 — startup verified, execution not yet begun

**Written:** 2026-08-23, after the mandatory startup checklist completed and the environment was
re-checked directly. **Nothing has been changed in the repository at this point except this file.**

### Phase

Startup and state verification — COMPLETE. Probe construction — NOT STARTED.

### Session identity

Supervisor-started runner. `implementation/operations/supervisor/state/runner.lock` read in this
session:

```json
{ "taskId": "TASK-0035", "pid": 26532, "acquired": "2026-08-23T09:57:18Z", "host": "LENOVO-LA0X1754" }
```

The heartbeat file read in the same session reports `RUNNER_RUNNING`, `readyTask: TASK-0035`,
`head: f24b21e4c8ad537e9f2589f0b6b8e5adf1b42a1c`.

### Git state at start — record this; the abort rule depends on it

```text
HEAD        = f24b21e4c8ad537e9f2589f0b6b8e5adf1b42a1c
origin/main = f24b21e4c8ad537e9f2589f0b6b8e5adf1b42a1c   (local remote-tracking ref)
git status  -> "On branch main / Your branch is up to date with 'origin/main' / nothing to commit, working tree clean"
```

**Known runner limit, re-confirmed in this session rather than recalled:** `git fetch origin` was
**refused** by the permission layer ("This command requires approval"), as MSG-0097 recorded. The
`origin/main` value above is therefore the **local remote-tracking ref**, not a freshly fetched one.
A rejected non-fast-forward on push would be the stronger signal that the remote moved; that check
happens at push time. `git status --porcelain=v1 -b` was also refused; plain `git status` was used.

### Completed operations

1. Read `CLAUDE.md` and `AGENTS.md` in full.
2. Read `implementation/status/current.md` (first page; the file is 3318 lines — the current-position
   sections were read).
3. Read `implementation/operations/CLAUDE-TASKS.md` status board and the full TASK-0035 section.
4. Read the task's authority, `MSG-0107-physical-projection-isolation-evaluation-authorization.md`
   (**MSG-0107b**), in full.
5. Read `EPA-0006` §3, §4.1–§4.7 in full — the derived predicate, the three shapes, the class table,
   the three evidence tiers, **§4.6 the strict Shape-1 criterion (S1–S11)**, and **§4.7 Q1–Q3**.
6. Read `MSG-0104` §4–§7 in full, including the **nine-row verdict table** that must be reproduced
   unchanged.
7. Read the TASK-0033 harness `implementation/probes/TASK-0033/probe.mjs` in full.
8. Listed OPEN items in `implementation/comms/`, `implementation/blockers/`,
   `implementation/discoveries/`.

### Last verified operation

**Environment re-checked directly in this session, as the queue section requires ("check, do not
assume"):**

```text
node --version                      -> v24.15.0
node:sqlite sqlite_version()        -> 3.51.3          (embedded; no install performed)
docker info                         -> "bash: line 1: docker: command not found"
```

**So class R has a reachable test subject and classes S and V do not** — unchanged from MSG-0104.
**Nothing was installed and Docker Desktop was not started** (operator action, explicitly forbidden
by the queue section).

### Prerequisites

| Prerequisite | State | How verified in this session |
|---|---|---|
| MSG-0107b AUTHORIZED | **MET** | File read; header `**Status:** AUTHORIZED` |
| MSG-0105 strict Shape-1 ruling | **MET** | Quoted in EPA-0006 §4.6 S1, read here |
| MSG-0104 probe evidence | **MET** | §5 and §7 read; verdict table captured verbatim |
| TASK-0034 criterion | **MET** | EPA-0006 §4.6 S1–S11 read |
| EPA-0006 classes | **MET** | §4.3 table read, including the class-K withdrawal note |
| TASK-0035 status READY | **MET** | Queue status board row 47 reads **READY**, priority 1, sole READY task |

### Next operation

Build a bounded physical-isolation probe under `implementation/probes/TASK-0035/`, extending — not
re-running — the TASK-0033 harness, and measure `U` per EPA-0006 §4.6 S3 for a series of
physical-isolation designs against the same synthetic adversarial fixture.

### Resumption safety

**Safe.** No engine, no host, no external state has been touched. Re-running from this point repeats
nothing destructive: the probe is `:memory:` only and writes no file outside the repository.

---

## Checkpoint 2 — the isolation probe is built and has executed

**Written after the probe ran to completion and its output was captured**, not in anticipation of it.

### Phase

Execution — COMPLETE. Records (COMMS message, EPA-0006 §4.8, status, queue) — NOT YET WRITTEN.

### Completed operations since checkpoint 1

1. Wrote `implementation/probes/TASK-0035/probe.mjs` — a **new** harness. **TASK-0033's harness was
   not modified and its probe was not re-run**; the recovery procedure forbids it and its evidence
   stands untouched.
2. Executed it three times during construction; the **third run is the recorded one**, captured to
   `implementation/probes/TASK-0035/probe-output.txt` (354 lines). Exit code 0, and the run is
   **non-empty**: 8 designs × 3 collection sizes, plus the staleness measurement.
3. **Two instrument defects were found and corrected during construction, and both are recorded in
   the harness comments rather than quietly fixed** — they are the S5 hazard occurring in practice:
   - a `probe_seen` placed in a subquery's **select list** was **elided by SQLite** because the outer
     query never read the column, so it reported `0` rows seen while the scan surfaced 22. Moved
     into the branch's `WHERE`, where the engine must call it per row scanned.
   - the negative control's `U` was initially read from `probe_seen`, **which is not active in that
     query at all** — the same "zero from an absent instrument" error MSG-0104 §5.3 flagged in its
     own record. It now reports `probe_rank`, which *is* the control's ranking function.

### Last verified operation — the recorded probe run

```text
node implementation/probes/TASK-0035/probe.mjs  -> exit 0
engine under test : SQLite 3.51.3 (embedded, node:sqlite)   node: v24.15.0
adversarial precondition: "ADVERSARIAL, as required" on all 3 fixtures
negative control: k-complete at M=50, returned 0/5 at M=500 and M=5000  -> the control FAILS,
                  so the run is valid under EPA-0006 §4.6 S8
```

`U` = unauthorized units examined, maximum across instrument placements (S7):

```text
design  M=50   M=500  M=5000   structure invariant (E1)
P0        20     200    2000   VIOLATED
P1        40     400    4000   VIOLATED
P2        20     200    2000   VIOLATED
P3        10     100    1000   VIOLATED
P4         0       0       0   holds
P5         0       0       0   holds
P4S        5      50     500   VIOLATED   (and it RETURNED 5/5 unauthorized rows)
NC        50     500    5000   VIOLATED   (control)
```

### Actual external/system state

Unchanged. Every database was `:memory:`; nothing was installed; no network was reached; no corpus
was read; Docker was not started and remains unreachable (`docker: command not found`).

### Git state

`HEAD` at checkpoint 1's commit `8611c5f`, pushed and accepted — which is itself evidence that
`origin/main` had not moved past `f24b21e` at that moment. Re-checked before each subsequent push.

### Next operation

Write the numbered COMMS execution record (MSG-0109) with per-candidate verdicts, add the additive
EPA-0006 §4.8, reconcile the status file and the queue, then commit and push.

### Resumption safety

**Safe.** The probe is deterministic and leaves no state. Re-running it is harmless; it is also
unnecessary, because its output is committed.

---

## Checkpoint 3 — TERMINAL. TASK-0035 is COMPLETE

**Written after every record was in place and the boundary checks were re-run**, not in anticipation.

### Phase

**Terminal.** No further operation is authorized under MSG-0107b: §5 requires stopping at evidence,
and selection remains a later Architecture Lead decision.

### Completed operations

1. Checkpoints 1 and 2, committed and pushed (`8611c5f`, `d707c51`).
2. `implementation/probes/TASK-0035/probe.mjs` written and executed; output captured.
3. `implementation/comms/MSG-0109-task-0035-physical-projection-isolation-execution-record.md`
   written — per-candidate verdicts, the MSG-0104 table reproduced verbatim, three questions referred.
4. `implementation/architecture/EPA-0006-…md` **§4.8 added — additive and declared.** No existing
   sentence deleted or reworded; the file lives under `implementation/`, so `docs/` is untouched.
5. `implementation/operations/CLAUDE-TASKS.md` — status-board row set to **COMPLETE**, task section
   headed with a closure note, **MSG-0109 added to the ledger**.
6. `implementation/comms/README.md` — **MSG-0109 registered.**
7. `implementation/status/current.md` — header line replaced with the prior line retained, task table
   row added, and a full post-TASK-0035 position section written with the TASK-0034 one retained.

### Last verified operation — boundary checks, re-run at the end

```text
git diff --name-only docs/decisions/                      -> empty
git diff --name-only docs/                                -> empty
git diff --name-only f24b21e HEAD -- docs/                -> empty
git diff --name-only f24b21e HEAD -- implementation/probes/TASK-0033/  -> empty
git status --porcelain                                    -> empty after commit
```

### Actual external/system state

**Unchanged by this task.** No host was modified, nothing was installed, no service was started, no
network was reached, no corpus was read, and every database used was `:memory:`. Docker remains
unreachable from this shell and **was not started** — that is an operator action.

### Verdict summary — nothing was cleared

`U` = unauthorized units examined, maximum across instrument placements:

| Design | M=50 | M=500 | M=5000 | Verdict |
|---|---|---|---|---|
| P0 no isolation | 20 | 200 | 2000 | NOT CLEARED |
| P1 scope only | 40 | 400 | 4000 | NOT CLEARED |
| P2 + classification, state | 20 | 200 | 2000 | NOT CLEARED |
| P3 + audience | 10 | 100 | 1000 | NOT CLEARED |
| P4 + effectivity materialised | 0 | 0 | 0 | **NOT CLEARED** — E4 not obtained; zero holds at the materialisation instant only |
| P5 P4 + per-partition lexical index | 0 | 0 | 0 | **NOT CLEARED** — E3 argued from construction, not instrumented |
| P4S P4 after the clock moved | 5 | 50 | 500 | NOT CLEARED — **returned 5 of 5 unauthorized rows** |
| NC negative control | 50 | 500 | 5000 | **DISQUALIFIED** — the control fails, so the run is valid |

**Classes S, V, K, L keep their MSG-0104 verdicts of NOT CLEARED, unchanged and with no new
evidence.** Classes D and H remain DISQUALIFIED.

### Next operation

**None.** Stop and report. **No task is READY**, no implementation task was marked READY, and no next
step was self-authorized. The three questions in MSG-0109 §9 are referred and **none blocks anything**.

### Resumption safety

**Safe, and resumption is unnecessary.** This checkpoint is terminal. A future session should read
MSG-0109 first, then EPA-0006 §4.8, and should **not** re-run either probe: TASK-0033's and
TASK-0035's outputs are both committed, and re-running proves nothing that the committed output does
not already show.

---
