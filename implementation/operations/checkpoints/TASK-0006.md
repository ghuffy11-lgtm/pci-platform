# CHECKPOINT — TASK-0006

**Checkpoint:** 2 — **written AFTER the destructive operation and the verified rebuild**
**Written:** 2026-08-19
**Task status:** COMPLETE
**Resumption safe:** YES — nothing in flight. **Do NOT re-run `down -v`**: it would destroy the
clean-room evidence this task produced.

## Current phase

Complete. Volume destroyed, stack rebuilt from repository configuration only, gate G3 verified.

## Completed operations

1. Pre-destruction inspection and checkpoint 1 — committed and pushed **before** acting (`9c4137e`).
2. `docker compose down -v` — verified: `pci-kernel_postgres-data` removed, network removed.
3. `docker compose up -d` from repository configuration — verified: postgres healthy, migrate
   exited 0, kernel started.
4. Gate G3 assertions — verified directly against the clean database (below).

**No manual SQL was run at any point.**

## Last verified operation

Gate **G3**, observed on the freshly initialised stack:

```text
containers   pci-kernel-postgres-1  Up (healthy)
             pci-kernel-migrate-1   Exited (0)
             pci-kernel-kernel-1    Up (healthy)

migrations   applying 0001_kernel_foundation ... applied
             migrations complete: 1 applied, 0 already present

role         password_set=true  super=false  bypassrls=false  login=true
privileges   CREATE on public: true | USAGE: true
pci_test     exists=true  owner=pci_app
schema       8 tables | 6 with FORCE RLS | 6 policies

kernel       /health/ready HTTP 200
             checks: store ok (1ms), identity ok (static), policy ok (kernel-static/1.0.0)

boundary     /data/docker/volumes/pci-kernel_postgres-data/_data
```

The single `FATAL: the database system is shutting down` in the postgres log occurs between
`LOG: shutting down` and `LOG: database system is shut down` — a connection arriving during the
entrypoint's normal init-server shutdown. It is not an initialisation failure.

## Next operation

None in this task. **TASK-0007 is NOT authorized** — MSG-0016 limits its authorization to TASK-0006
alone. Execution stops here.

## Actual external / system state

- `pci-kernel_postgres-data`: **new volume**, created by this rebuild. The prior volume and the
  2026-08-19 manual workaround are gone.
- All three containers running from repository configuration and the host `.env`.
- `.env` untouched; mode 0600; gitignored; three keys.
- No PCI artifact outside `/data`.

## Git

HEAD: 9c4137e   origin/main: 9c4137e   working tree: clean at time of writing

## Notes for a resuming session

The database you are looking at is now genuinely clean-room provisioned. That is a **change of
meaning**, not just of contents: before this task, the live database's state proved nothing because
it had been repaired by hand. It now demonstrates the DISC-0007 and DISC-0008 fixes.

If `pci-kernel_postgres-data` is missing when you resume, the stack was torn down after this
checkpoint by something outside the record — stop and document rather than rebuilding, because a
rebuild would look identical to this one and would silently replace the evidence.
