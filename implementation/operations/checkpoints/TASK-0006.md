# CHECKPOINT — TASK-0006

**Checkpoint:** 1 — **written BEFORE the destructive operation**
**Written:** 2026-08-19
**Task status:** IN_PROGRESS
**Resumption safe:** YES — nothing destroyed yet at the time of writing

## Current phase

About to perform the destructive volume re-initialisation authorized by MSG-0016.

## Completed operations

1. MSG-0016 read; authorization confirmed as limited to TASK-0006 — verified by reading the message.
2. Queue reconciled by the architecture lead in `2d348c1`; TASK-0006 READY — verified by reading the queue.
3. Host workspace pulled to 2d348c1 — verified by `git rev-parse`.
4. Pre-destruction inspection completed — see below.

## Last verified operation

Pre-destruction inspection of what will be destroyed (observed, not assumed):

```text
containers : pci-kernel-postgres-1 Up 2 hours (healthy), pci-kernel-migrate-1 Exited (0)
volume     : pci-kernel_postgres-data -> /data/docker/volumes/pci-kernel_postgres-data/_data
databases  : postgres, pci, pci_test
tables     : 8 in public
rows       : knowledge_objects 0 | audit_records 0 | tenants 1
```

**Nothing of value is in this volume.** It contains only TASK-0001 verification artifacts and the
2026-08-19 manual workaround. No customer data, no irreplaceable state.

## Next operation

`docker compose down -v` — removes the `pci-kernel_postgres-data` volume. **This is
irreversible.** Authorized explicitly by MSG-0016 solely for clean-room reproducibility
verification.

Then: bring the stack up from repository configuration only, with **no manual SQL**, and verify
role provisioning, database availability, privileges, RLS posture, kernel startup, and health
endpoints against the clean initialisation.

## Actual external / system state

As inspected above. `.env` on the host holds three keys (POSTGRES_SUPERUSER_PASSWORD,
PCI_APP_PASSWORD, PCI_STATIC_PRINCIPALS), mode 0600, gitignored. It is **not** destroyed by this
operation and is required for the rebuild.

## Git

HEAD: 2d348c1   origin/main: 2d348c1   working tree: clean at time of writing

## Notes for a resuming session

**If you are resuming and this is the newest checkpoint, the destructive step may or may not have
run.** Do not assume either way, and do not re-run it on the strength of this file.

Determine the truth by observation:

```bash
docker volume ls | grep pci-kernel_postgres-data     # gone => down -v ran
docker ps -a --format '{{.Names}} {{.Status}}'       # what is up
docker exec -i pci-kernel-postgres-1 psql -U pci_admin -d pci -tAc   "select rolpassword is not null from pg_authid where rolname='pci_app'"
```

- Volume **absent**: the destruction happened; resume at the rebuild.
- Volume **present and `pci_app` has a password**: either the rebuild already succeeded, or the
  destruction never ran. Check whether `pci_test` exists **and** whether the init log shows the
  new guard messages — the pre-existing state was manually repaired and looks similar.
- Anything else: **stop and document**, per the recovery procedure.

`docker compose down -v` is NOT idempotent in effect: running it after a successful rebuild
destroys the clean-room evidence this task exists to produce.
