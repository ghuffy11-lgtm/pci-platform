# TASK-0049 — Checkpoint

**Task:** TASK-0049 — promote TASK-0046's topology/durability evidence into EPA-0006 §4.19
**Checkpoint:** 1 (and final)
**Written:** 2026-08-26, **after** the section was written, pushed and read back — not in anticipation
**Session:** interactive COMMS session; **no `runner.lock` held or observed**

---

## Phase

**COMPLETE.** Documentation / evidence promotion. **Nothing was executed and no test count is claimed.**

## Repository state

| | |
|---|---|
| **Starting HEAD** | **`20edd9d`** — fast-forwarded from `ce31383` and verified equal to `origin/main` before editing |
| `git diff --name-only docs/` | **empty** |
| **Deletions in EPA-0006** | **0** — the mechanical check that no gate, table or verdict was edited |
| Mid-run movement | **none observed**; `origin/main` re-checked before the push |

## Completed operations, each verified

1. **Read the Lead's task file as the authority**, not the board row — the row says in terms that
   **where they differ the file wins**. Then **MSG-0161b** (Q18), **MSG-0158** (the evidence), **§4.17**,
   **§4.18**, **§4.16 DA-4/DA-5**.
2. **Wrote §4.19** with the full 16-configuration table, both parts of the result, the mechanism as
   exhibited, the four required relationships, run validity with the controls **kept structurally
   separate**, the two apparatus defects, and the boundary stated as limits.
3. **Added a declared pointer note in §4.17** recording that §4.19 **inverts** its W-B result, **without
   withdrawing anything §4.17 states** — §4.12's Q12 precedent.
4. **Recorded MSG-0165**, mapping all seven required outcomes to evidence.
5. **Reconciled the queue row, the ledger, the register and the status file.**

## A slip in this session, recorded rather than tidied away

**I inserted a duplicate `MSG-0164` ledger row** — the Lead's reconciliation commit had already added
one — and **removed it in the same pass**, keeping the original. **Third occurrence** (MSG-0115,
MSG-0123 were the others).

**The cause is mechanical and mine: I checked the register for MSG-0164/0165 before inserting and did
not check the ledger.** **The check is one `grep` and belongs before every insert, in both indexes.**
It was applied to the register in the same pass, where it found both rows absent and inserted them once.

## Last verified operation

**§4.19 read back from `origin/main` after pushing**, present between §4.18 and section 5, with
**zero deletions** across the file.

## Actual external state as observed

- **Nothing installed, executed, deployed or measured.** No probe, fixture or harness written or run.
- **No engine selected, adopted, deployed, implemented or cleared. Promotion clears nothing.**
- **No gate changed; no candidate verdict moved; ten probes have cleared nothing.**

## Next operation

**None. The run stops here.** **No task is READY and none is authorized.** **Q21 is referred** — whether
an N6 violation joins §4.13's EV-list, and at what strength.

## Resumption safety

**Safe, and there is nothing to resume. Do not re-run this task**: re-running it would insert a second
§4.19 and a second pointer note in §4.17 — **two statements of one record**, which is the drift §4.12's
Q12 note exists to prevent. **Check whether §4.19 exists before writing anything** — it does.
