# CHECKPOINT — TASK-0007

**Checkpoint:** 1 (final)
**Written:** 2026-08-19
**Task status:** COMPLETE
**Resumption safe:** YES — non-destructive throughout; nothing in flight

## Current phase

Complete. All three tiers re-run against the clean-room stack; ADR-0016 obligations re-proven.

## Completed operations

1. Queue reconciled to IN_PROGRESS from MSG-0018 — committed `c106aff`.
2. Pre-check: stack still the clean-room one — volume created 2026-08-19T18:25:02Z, containers healthy.
3. Unit tier — 102 pass / 0 fail.
4. Contract tier — 101 pass / 0 fail.
5. Integration tier against the clean-room database — 26 pass / 0 fail / 0 skipped / 0 cancelled.
6. ADR-0016 obligations re-proven live.
7. AC-01 images, AC-08 health endpoints, and the `/data` boundary re-checked.

## Last verified operation

```text
unit         102 pass / 0 fail
contract     101 pass / 0 fail
integration   26 pass / 0 fail / 0 skipped / 0 cancelled
TOTAL        229 pass / 0 fail        every tier non-zero

runtime role      super=false  bypassrls=false
FORCE RLS         6 of 6 RLS-enabled tables
policies          6

named ADR-0016 evidence (all passing):
  ADR-0016 layer 3 - row-level security blocks a cross-tenant read
  an unset tenant GUC returns no rows rather than all rows
  a cross-tenant relationship is rejected by the composite foreign key
  audit records cannot be updated or deleted by the runtime role
  audit records are not visible across tenants
  an object from another tenant is invisible
  updating an object in another tenant returns null
  listing never returns another tenant's objects

images       pci-kernel-kernel:latest, pci-kernel-migrate:latest
health       /health/ready 200, /health/live 200
boundary     /data/docker/volumes/pci-kernel_postgres-data/_data
```

No volume was re-initialised and no manual SQL was run.

## Next operation

TASK-0008 (record reconciliation) — permitted by MSG-0018's continuation clause. **TASK-0009 is not
authorized** and execution stops before it.

## Actual external / system state

- Same clean-room stack as TASK-0006 checkpoint 2; volume creation timestamp unchanged, confirming
  it was not re-initialised by this task.
- Containers healthy; `.env` untouched.
- One boundary finding: `/home/claude/.docker/buildx/*`, Docker CLI state created by the earlier
  image builds. Recorded as **DISC-0009**; not deleted, because tidying the operator's account to
  make a check pass would be the wrong instinct.

## Git

HEAD: c106aff   origin/main: c106aff   working tree: clean at time of writing

## Notes for a resuming session

The tiers are idempotent and safe to re-run. Before trusting any result, confirm the volume
creation timestamp still matches TASK-0006 checkpoint 2 — if it differs, the stack was
re-initialised and the evidence describes a different system.
