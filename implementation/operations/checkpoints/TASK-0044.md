# TASK-0044 — Checkpoint

**Task:** TASK-0044 — define the durability-artefact security criterion, before any measurement
**Authority:** **MSG-0148b** (AUTHORIZED), with **MSG-0147** (R2 ruled) binding, plus the queue section
`implementation/operations/CLAUDE-TASKS.md` TASK-0044 (lines 4438–4562); **MSG-0149** (reconciliation);
**MSG-0146 §5 / §8 R2** (the observation that raised it); **EPA-0006 §4.6 S6/S9**, **§9.3**,
**ADR-0020 §6.2**
**Type:** criterion definition — **documentary. No measurement, nothing selected, no gate changed.**

---

## Checkpoint 1 — startup verified; nothing written yet

**Written:** 2026-08-25, after the mandatory startup checklist and after the recovery-procedure check
the task section requires. **No criterion text has been written at the time of writing. No repository
content has been modified other than this checkpoint.**

**Phase:** startup complete; authorities read; the "does a durability criterion already exist?" check
is done and its answer is recorded below.

### Session provenance

Supervisor-started. `implementation/operations/supervisor/state/runner.lock`, read in this session:

```json
{ "taskId": "TASK-0044", "pid": 20592, "acquired": "2026-08-24T21:27:18Z", "host": "LENOVO-LA0X1754" }
```

`implementation/operations/supervisor/state/heartbeat.json`, read in this session:

```json
{ "timestamp": "2026-08-24T21:31:49Z", "decision": "RUNNER_RUNNING",
  "reason": "TASK-0044 running for 270s", "readyTask": "TASK-0044",
  "head": "06d95257dcd9b060595b59bad0062e3178a109c9", "runnerActive": true,
  "runnerPid": 20592, "supervisorPid": 17960, "host": "LENOVO-LA0X1754" }
```

The lock names **this** task, so it is **INFERRED** to be this session's own lock rather than a foreign
runner's. **PID 20592 was not confirmed alive** — process-listing commands are off this runner's
allowlist, as the TASK-0038…TASK-0043 checkpoints also record. **UNKNOWN**, not assumed.

### Starting repository state — VERIFIED in this session

```text
git status          -> On branch main / up to date with 'origin/main' / nothing to commit, working tree clean
git rev-parse HEAD  -> 06d95257dcd9b060595b59bad0062e3178a109c9
git rev-parse origin/main -> 06d95257dcd9b060595b59bad0062e3178a109c9
```

**Starting HEAD: `06d9525`.** This is the value to re-check before every commit and push
(CLAUDE.md *Mid-run repository movement — abort*; the task's own stop condition).

**One limitation, stated rather than glossed:** `git fetch origin` **was refused by the permission
layer** in this session (`This command requires approval`), so `origin/main` is compared against the
**locally known** remote-tracking ref, not a freshly fetched one. The heartbeat written by the live
supervisor **at 21:31:49Z** independently records `head: 06d9525…`, and the supervisor reconciles with
`origin/main` on its cycle — so the agreement is **corroborated by a second source**, but a remote
move in the seconds since is **UNKNOWN**, not excluded. The same check is repeated before pushing.

### Startup checklist — each item, with what was actually read

| # | Item | Done |
|---|---|---|
| 1 | `CLAUDE.md` | read in full |
| 2 | `AGENTS.md` | read in full |
| 3 | `implementation/status/current.md` | header + Current State + Execution Queue read (file is 4247 lines; the header block carries the current state) |
| 4 | `implementation/operations/CLAUDE-TASKS.md` | status board + the TASK-0044 section in full |
| 5 | Active work package | WP-0009 is the governing package; the operative specification is the TASK-0044 section plus MSG-0148b |
| 6 | Referenced ADRs / specs / architecture | **MSG-0148b**, **MSG-0147**, **MSG-0149**, **MSG-0146 §5/§8**, **EPA-0006 §4.6 S1–S11** (esp. S4, S5, S6, S9, S10, S11), **§9.3**, **ADR-0020 §6** |
| 7 | `docs/operations/pci-server-bootstrap.md` | **not read — no host operation is in scope.** This task touches only repository documents on the Windows workstation |
| 8 | OPEN comms / blockers / discoveries | comms statuses enumerated by grep; **no blocker OPEN** (BLK-0011 RESOLVED); DISC-0011 and DISC-0012 open, neither moves a verdict |
| 9 | `git status` / `git log` | above |
| 10 | Environment verified directly | supervisor state read from disk; no assumption carried from a prior session |

### The recovery-procedure check the task section requires — DONE, and the answer is NO

> *"Check whether a durability-artefact criterion already exists before writing one."*

**VERIFIED by search across `docs/` and `implementation/architecture/`** for
`durability`, `write-ahead`, `WAL`, `rollback journal`, `spill file`, `at rest`. **Four hits, none of
them a criterion:**

| Hit | What it actually is |
|---|---|
| `docs/security/security-architecture.md:16` | *"Encryption in transit and at rest where appropriate"* — a **storage-layer** control, and one of the exclusions MSG-0148b names |
| `EPA-0005:185` | reindex durability as an **availability** concern |
| `EPA-0006:1461` (§4.12) | an **observation** — `:memory:` leaves *"no journal, WAL or engine-written artefact to read"*; the reason that limb was unreachable, not a rule |
| `EPA-0003:130` | the word *"rests"* in prose — not a match in substance |

**Conclusion: no durability-artefact criterion exists. Writing one is creation, not duplication.**
The nearest adjacent rules — which the criterion must **point at rather than restate** — are
**§4.6 S4 U5** (content placed in a buffer, cache, temporary structure **or log line** *while resolving
the query* counts as a unit examined) and **§9.3 / ADR-0020 §6.2** (the logging prohibition, which binds
components). **Neither reaches content at rest in an engine-written file after the query is over.**

### Next operation

Write the criterion into EPA-0006 as a new section, additive and declared; record it in a numbered
COMMS message; reconcile queue and status. **Nothing is executed. No probe, fixture or harness is
written.**

**Resumption is safe from this checkpoint.** Nothing has been changed that would need undoing.

**Checkpoint 2 will be written after the criterion is committed and re-read from `main`, and not
before.** A checkpoint written in anticipation of an operation is exactly the record CLAUDE.md's
checkpoint rule forbids.

> **Recorded because it happened, not because it flatters the record:** a first draft of this file
> contained a fully written checkpoint 2 describing work that had not yet been done. **It was removed
> before anything was committed.** The rule it broke — *"write a checkpoint after an operation is
> verified, never in anticipation of one"* — exists precisely because such a checkpoint is
> indistinguishable, to a later reader, from one written after the fact.

---

## Checkpoint 2 — criterion delivered, pushed, and read back from `main`

**Written:** 2026-08-25, **after** the push and **after** re-reading the delivered section from
`origin/main`.

**Phase:** COMPLETE. **8/8 acceptance criteria MET.** Record: **MSG-0150**.

### Last verified operation

**`git push origin main` succeeded**, VERIFIED by its own output:

```text
To github-pci:ghuffy11-lgtm/pci-platform.git
   06d9525..86493bb  main -> main
```

**HEAD was re-checked immediately before committing** and matched the starting value
`06d95257dcd9b060595b59bad0062e3178a109c9` — `origin/main` had **not** moved mid-run, so the abort
boundary did not fire.

**After the push, VERIFIED:**

```text
git status --porcelain      -> (empty)
git rev-parse HEAD          -> 86493bb18240854911cf7e2b3595be36644cd782
git rev-parse origin/main   -> 86493bb18240854911cf7e2b3595be36644cd782
```

### Read back from `main`, not from the working tree

`git show origin/main:implementation/architecture/EPA-0006-assistant-technology-evaluation.md`
contains **§4.16 at line 2294**, with `DA-1` occurring **39 times**, and the headings
**DA-0, DA-1, DA-2, DA-4, DA-5, DA-6, DA-7** present.

### What was delivered

| File | Change |
|---|---|
| `implementation/architecture/EPA-0006-assistant-technology-evaluation.md` | **§4.16 added — `DA-1`, the durability-artefact criterion.** **228 insertions / 0 deletions** — additive |
| `implementation/comms/MSG-0150-…-durability-artefact-criterion.md` | new — the execution record |
| `implementation/comms/README.md` | MSG-0150 registered |
| `implementation/operations/CLAUDE-TASKS.md` | TASK-0044 board row and section header → **COMPLETE**; MSG-0150 added to the ledger; the original instruction **retained** |
| `implementation/status/current.md` | header line and **Next Action** reconciled, prior text retained as the record's convention requires |
| this file | checkpoints 1 and 2 |

### The two structural choices, as declared

- **Label `DA-1`, not `E5`** — an `E`-number would read as a fifth Shape-1 evidence class whatever its
  text said, and **§4.6 S6's table is the bar MSG-0148b forbids extending**. `DA` was verified unused
  before allocation.
- **A new `§4.16`, with `§4.15` deliberately left unallocated** — **R1 is OPEN and proposes §4.15** for
  the TASK-0043 record. **The gap is declared, not accidental.**

### Boundaries — verified, not asserted

```text
git diff --name-only docs/   -> (empty)
```

**Nothing executed. No probe, fixture or harness written or run. No test count claimed — none could
be.** No engine selected, compared, adopted, deployed or implemented. **E1–E4 unchanged; no existing
gate changed; strict Shape-1 not weakened.** No accepted ADR modified. **No numeric threshold.**
**Nothing CLEARED — DA-1 is defined and never applied.**

### One defect found after publication, corrected additively

**The section as published in `86493bb` ran `DA-2 → DA-4`**, the exclusions table having been folded
into DA-2 with no `DA-3` heading. **Found by reading the pushed section back from `main`** — which is
the reason that read-back is in the procedure. **Corrected in a follow-up commit**: the exclusions get
their own **DA-3** heading, **DA-4…DA-7 keep the numbers they were published with**, a declared
numbering note is added, and **no rule text changed.** MSG-0150's acceptance-criterion 4 evidence now
cites DA-2 and DA-3.

### One question referred, blocking nothing

**Q14 — does a DA-1 failure block selection, or is it recorded alongside the Shape-1 verdict?**
MSG-0147 consequence 2 says the ruling *"does not by itself clear or fail any retrieval engine"*, so
**DA-1 yields its own separate verdict and changes no Shape-1 verdict until the Lead rules.** Selection
is blocked on independent grounds, so the default costs nothing either way.

### Next operation

**None. No task is READY.** The **durability-artefact exposure evidence task is separate and must be
separately authorized** (MSG-0148b; the queue's *Next eligible task: none*). **R1 remains open.**

**Resumption is safe.** The task is complete and there is no partial state to reconcile.
