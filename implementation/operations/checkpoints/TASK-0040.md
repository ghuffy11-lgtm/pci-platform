# TASK-0040 — Checkpoint

**Task:** TASK-0040 — encode Q12 in EPA-0006 §4.6 S7: reachable index-cursor placements must be exercised
**Authority:** MSG-0125 (AUTHORIZED), with MSG-0124 (Q12, DECIDED) binding; queue section
`implementation/operations/CLAUDE-TASKS.md` TASK-0040; MSG-0126 (reconciliation)
**Type:** criterion / evidence-instrument update — selects nothing, deploys nothing, amends no ADR,
executes no probe

---

## Checkpoint 1 — startup verified; starting HEAD recorded; nothing else written

**Written:** 2026-08-24, after the mandatory startup checklist and before any file other than this
checkpoint was created or modified.

**Phase:** startup complete; the S7 update is not yet written.

### Starting HEAD

```text
git status --porcelain      -> (empty)
git rev-parse HEAD          -> ef8561ed8e315c78b46ff9ac55d03638937f1529
git rev-parse origin/main   -> ef8561ed8e315c78b46ff9ac55d03638937f1529
git status -sb              -> ## main...origin/main        (no ahead/behind marker)
git log --oneline -1        -> ef8561e ops: reconcile TASK-0040 - encode Q12 in EPA-0006 S7
```

**Starting HEAD for this run is `ef8561e`.** It is re-checked before every commit and push, as the
mid-run movement rule (MSG-0028 decision 2) and the TASK-0040 stop conditions both require.

**No repository movement was observed during startup.** Both reads in this session returned
`ef8561e`.

> **Known runner limit, recorded rather than routed around — unchanged from TASK-0036, TASK-0037,
> TASK-0038 and TASK-0039.** `git fetch origin` and `git ls-remote origin` are off this runner's Bash
> allowlist; both were attempted in this session and the permission layer declined them. The
> `origin/main` value above is therefore the **local remote-tracking ref**, not a freshly fetched one,
> and is reported as such. **UNKNOWN:** whether `origin/main` has moved since the last successful
> fetch. The stronger evidence remains the push — a genuine divergence surfaces as a rejected
> non-fast-forward, which is exactly how BLK-0006 was caught.

### Session provenance

Supervisor-started. `implementation/operations/supervisor/state/runner.lock`, read in this session:

```json
{ "taskId": "TASK-0040", "pid": 20752, "acquired": "2026-08-24T10:27:18Z", "host": "LENOVO-LA0X1754" }
```

**The lock names this task**, so it is **INFERRED** to be this session's own lock rather than a
foreign runner's. **The PID was not confirmed alive** — process-listing commands are off this
runner's allowlist, as the TASK-0038 and TASK-0039 checkpoints also record. That is **UNKNOWN**, not
assumed.

### Startup checklist — ten items

| # | Item | Result |
|---|---|---|
| 1 | `CLAUDE.md` | read in full |
| 2 | `AGENTS.md` | read in full |
| 3 | `implementation/status/current.md` | **read in part.** The file is 271.8 KB and exceeds the reader's 256 KB single-call limit; the header and current-position section were read, the remainder is retained history. Recorded as a partial completion of this item rather than claimed complete |
| 4 | `implementation/operations/CLAUDE-TASKS.md` | read — board row (line 52) and the full TASK-0040 section (lines 3704–3812) |
| 5 | Active work package `docs/program/work-packages/WP-0009-employee-policy-assistant.md` | read (header, allocation and governance sequence) |
| 6 | Referenced ADRs / specs / architecture | **EPA-0006 §4.6 S1–S11 read in full**, plus §4.7 preamble, §4.8 measurement table, §4.9 G-Q4, §4.12 gaps 1–2 and the Q12 referral at line 1433. **MSG-0123 verdicts, MSG-0124, MSG-0125, MSG-0126** read in full |
| 7 | `docs/operations/pci-server-bootstrap.md` | **not read — and not required.** No host operation is in scope. This task edits repository documents on the Windows authoring machine only; the PCI server is not touched |
| 8 | OPEN items in `comms/`, `blockers/`, `discoveries/` | reviewed — see below |
| 9 | `git status`, `git log --oneline -8` | run; results above |
| 10 | Verify environment directly | done — no execution environment is required by this task; see below |

### Open items

**No blocker is OPEN.** Every `**Status:**` line under `implementation/blockers/` reads RESOLVED or
RESOLVED / CLOSED — BLK-0001 through BLK-0010, checked by reading the status lines in this session.

**OPEN communications are informational.** MSG-0126's own header says so: *"OPEN — informational; no
decision blocks TASK-0040."* **None requires a decision before this task proceeds.**

### Prerequisites checked, not assumed

| Prerequisite | Verified how | Result |
|---|---|---|
| **TASK-0040 status is READY in the committed queue** | board row line 52 and section line 3706 of `CLAUDE-TASKS.md` at `ef8561e`, tree clean | **MET** |
| MSG-0125 **AUTHORIZED** | file read; header `**Status:** AUTHORIZED / READY` | **MET** |
| MSG-0124 **DECIDED** (Q12) | file read; header `**Status:** DECIDED` | **MET** |
| MSG-0123 evidence present | referenced verdicts read in EPA-0006 §4.12 verdict table (K7/K8 **NOT CLEARED**) | **MET** |
| EPA-0006 §4.6 S7 exists exactly once | `grep -c "^#### S7"` → **`1`** (line 486) | **MET** |
| **TASK-0040 is the only READY task** | board scanned; no other row reads READY | **MET** |

### Double-application check — required by the recovery procedure

The TASK-0040 recovery procedure states: *"Check whether S7 already carries the Q12 requirements
before editing. Encoding it twice would leave two statements of one rule."*

| Check | Command | Result |
|---|---|---|
| S7 heading count | `grep -c "^#### S7" …EPA-0006…` | **`1`** |
| Q12 occurrences in EPA-0006 | `grep -n "Q12" …EPA-0006…` | **three hits, all at lines 1433/1437/1455 — the §4.12 referral only.** **No Q12 text exists inside §4.6 S7** (lines 486–500) |
| Checkpoint | `ls implementation/operations/checkpoints/TASK-0040.md` | **No such file** before this one |

**Conclusion: the Q12 requirements are NOT yet encoded in S7.** The edit is a first application, not
a repeat.

### Environment

**No execution environment is required and none is used.** TASK-0040 is documentary: it edits
repository markdown. **No probe is written or run, no prior probe is re-run, no engine is installed
or started, no network is reached, no corpus is entered, and no benchmark or timing figure is
produced.** This is why item 10 above records "no execution environment required" rather than a
capability table.

### What this task must produce, restated from MSG-0125 with MSG-0124 binding

EPA-0006 §4.6 S7 must carry, explicitly and testably:

1. **every reachable index-cursor placement exposed by the test subject must be exercised**, in
   addition to other applicable placements;
2. **the maximum observed result across the exercised applicable placements is what is reported**;
3. **row-access-only `U = 0` is insufficient for E2** when a reachable index-cursor placement exists
   but has not been exercised — and the omission is **disqualifying**, not merely noted.

**Method:** additive and declared on TASK-0034's precedent; **no accepted ADR modified**; **no gate
weakened**; **no numeric tolerance or threshold**; **all existing verdicts preserved and no prior
probe re-run**; post-change EPA-0006 verified **from `main`** with exact change statistics recorded.

### Next operation

Write the additive Q12 encoding into EPA-0006 §4.6 S7. **Nothing is written yet** except this
checkpoint.

**Resumption is safe.** Apart from this file, the repository is byte-identical to `ef8561e`.

---

## Checkpoint 2 — S7 update written, committed, pushed, and verified from `main`

**Written:** 2026-08-24, **after** the push was accepted and **after** the published object was read
back. Nothing in this checkpoint is anticipated.

**Phase:** complete. The task stops here, as MSG-0125 requires.

### Completed operations, each verified by observation

| # | Operation | How it was verified |
|---|---|---|
| 1 | Startup checklist, prerequisites, double-application check; starting `HEAD` recorded | checkpoint 1 above |
| 2 | Checkpoint 1 committed `ef3cd84` and pushed | `ef8561e..ef3cd84  main -> main` — fast-forward |
| 3 | **EPA-0006 §4.6 S7.1–S7.4 written** — the Q12 encoding | `git diff --numstat` → **`98  0`** on one file |
| 4 | **Declared pointer note added under §4.12's Q12 heading** — the one judgement call, declared in MSG-0127 §7 | included in the same 98 insertions; **0 deletions**, so the heading and every existing line are provably untouched |
| 5 | **MSG-0127** execution record written | file present and registered in **both** indexes |
| 6 | Queue updated — board row and section → **COMPLETE**, `### Result` block added; **MSG-0127 ledger row added** | file. **The duplicate-row check MSG-0126 §5 asks for was run first**: `grep -c "MSG-0127"` on the queue and the register **before** inserting — the register returned `0`, and the queue's hits were confirmed to be this run's own section text, not a ledger row |
| 7 | COMMS register updated — MSG-0127 row, newest-first | file |
| 8 | Status updated — new current position, new header line, and a TASK-0040 table row; **prior line and prior paragraph retained verbatim, both declared** | file |
| 9 | **Boundary verification** | `git diff --name-only docs/` → **empty**, re-checked after the edits and again before the commit |
| 10 | **Nothing executed** | no file was created under `implementation/probes/`; `git status --porcelain implementation/probes/` → **empty**. No probe written, run or re-run |
| 11 | **Committed `3a19dfb` and pushed** | `ef3cd84..3a19dfb  main -> main` — fast-forward; `git status --porcelain` → **empty** afterwards apart from this checkpoint |
| 12 | **Verified from `main`, not the working tree** | see below |

### Verification from `main` — the step MSG-0125 asks for specifically

```text
git show origin/main:implementation/architecture/EPA-0006-…md | grep -n "^##### S7\."
  511: ##### S7.1 — The ruling, quoted rather than paraphrased
  534: ##### S7.2 — The three requirements, stated so a probe can be failed against them
  546: ##### S7.3 — "Reachable", and the one way a probe may report that no such placement exists
  574: ##### S7.4 — What this does NOT change, stated item by item

git diff --numstat ef8561e origin/main -- …EPA-0006…      -> 98      0
git show origin/main:…EPA-0006… | grep -c "Q12 has since been RULED"  -> 1
git diff --name-only ef8561e origin/main                  -> six files, none under docs/
git status --porcelain                                    -> (empty)
```

**The S7-R1/R2/R3 table was read back from the committed object** and carries the three requirements
as published, including R3's *"E2 is NOT satisfied, and by S6 the candidate is NOT CLEARED"*.

**Exact change statistics, as required:** **98 insertions, 0 deletions, one file** —
`implementation/architecture/EPA-0006-assistant-technology-evaluation.md`.

**Six files changed by this run in total**, none under `docs/`: EPA-0006, MSG-0127, the COMMS
register, the queue, this checkpoint, and the status file.

### Last verified operation

**Operation 12 — the read-back from `origin/main` succeeded.** Both pushes in this session were
**fast-forward**, which is also this session's mid-run movement check: a move of `origin/main` under
the session would have surfaced as a rejected non-fast-forward, as it did in BLK-0006. `HEAD` was
re-read immediately before each commit and read the value this session itself had written.

### Actual state as observed, not assumed

- **`docs/` is untouched.** No accepted ADR was modified, proposed, or affected.
- **Nothing was executed.** No probe was written, run or re-run; no engine installed or started; **no
  network reached**; no corpus entered; no wall clock read. **There is no test count and none is
  claimed** — this task is documentary.
- **Nothing is CLEARED.** **K7 and K8 remain NOT CLEARED**, **K3 and K4 remain NOT CLEARED**, and
  every prior verdict is reproduced unchanged. **TASK-0038's `U = 0` for K8 remains correct as a
  row-access count** and was not relabelled.
- **No gate was weakened** — the diff removes no line — **no numeric tolerance or threshold was
  introduced**, and **nothing was selected, adopted, recommended, installed or deployed.**
- **No implementation task is READY.** TASK-0040's *Next eligible task* reads **none**.
- Repository `HEAD` is **`3a19dfb`** at the time of the deliverable push, matching `origin/main`.

### The one judgement call, repeated here so a resuming session sees it

Alongside the authorized S7 update, **a six-line declared pointer note was added under §4.12's Q12
heading**, which reads *"Surfaced, NOT decided"*. Without it the record would state Q12 as ruled in
one section and open in another. **The heading and every existing line are untouched** (0 deletions),
the note is dated and attributed, and it **points at S7 rather than restating the rule** — TASK-0030's
*"two statements invite drift"*. Full reasoning: **MSG-0127 §7**. If judged out of scope it is one
blockquote and reverts cleanly.

### Next operation

**None. The task is complete and the run stops here.** MSG-0125 requires stopping after the update and
its verification; both are done, pushed, and verified from `main`.

**The next evidence action must be separately authorized.** The standing next step is unchanged and is
the Architecture Lead's: MSG-0119 returns the question to **EPA-0006 §4.7 Q3**, and failure does not
authorize weakening the gates.

**Resumption is unnecessary, and re-running this task would be a defect.** §4.6 S7.1–S7.4 exists and
the §4.12 pointer note exists. **Re-running would state one rule twice** — the drift the task's own
recovery procedure warns about. **Check `grep -c "^##### S7\."` → 4 and
`grep -c "Q12 has since been RULED"` → 1 before writing anything.**
