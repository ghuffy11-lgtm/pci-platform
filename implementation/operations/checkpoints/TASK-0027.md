# TASK-0027 — Checkpoint 1

**Task:** TASK-0027 — A-SURVEY (n=1): inspect the approved/synthetic corpus
**Checkpoint:** 1 (and the only one — the task stopped at its first action)
**Written:** 2026-08-22, ~09:53Z, **after** every operation it reports was verified
**Session:** supervisor-started runner, `runner.lock` pid **24140**, acquired **2026-08-22T09:47:18Z**, host `LENOVO-LA0X1754`
**Authority:** MSG-0080 (AUTHORIZED) + the TASK-0027 section of `CLAUDE-TASKS.md`

## Current phase

**Stopped at the task's first action — the corpus read — on a permission boundary.**
See [`BLK-0010`](../../blockers/BLK-0010-corpus-read-denied-to-unattended-runner.md).

**Prerequisites were unmet at session start and became met mid-session.** The run got past the point
where the previous cycle (BLK-0009) stopped, and then stopped one step later for a different reason.

## Git state — including a mid-run movement

```text
session start, 09:47Z    HEAD = origin/main = f67bc7c   (the commit that created BLK-0009)
re-checked   09:48:15Z   HEAD = origin/main = f67bc7c   unchanged
re-checked   09:50:10Z   HEAD = origin/main = f67bc7c   unchanged
re-checked   09:52:16Z   HEAD = origin/main = 66314e1   MOVED
                         66314e1 "ops: reconcile TASK-0027; resolve BLK-0008/BLK-0009; raise MSG-0082"
```

**`HEAD` moved mid-run.** The session did **not** abort, and the reconciliation justifying that is
recorded in BLK-0010 — in short: the movement is the exact commit this run was blocked on, `HEAD` and
`origin/main` agree, the tree is clean, and **no evidence produced by this session was invalidated,
because this session had performed no task action against the old state.**

**Re-check `HEAD` again before any further push.** `git fetch` is off the runner allowlist, so a
further move is detectable only when a push is rejected.

## Completed operations

1. Mandatory startup checklist — `CLAUDE.md`, `AGENTS.md`, `implementation/status/current.md`,
   `implementation/operations/CLAUDE-TASKS.md` (TASK-0027 section in full), MSG-0080, MSG-0082,
   BLK-0009 (both committed and working-copy versions), `git status`, `git log`.
2. **Prerequisite verification — failed at session start**, on two independent conditions: TASK-0027
   absent from the committed queue, and a concurrent actor actively writing eleven paths.
3. **Prerequisite re-verification after the mid-run commit — both conditions CLEARED.**
4. **Corpus read attempted — denied.** Two attempts; the first refusal was ambiguous about its cause,
   the second named the boundary explicitly. Not routed around; no third attempt; no other tool tried.
5. **BLK-0010 raised**; this checkpoint written.

## Last VERIFIED operation

**The corpus read denial**, established rather than assumed:

```text
$ ls -l /d/Work/pci-corpus/
ls in '/d/Work/pci-corpus/' was blocked. For security, Claude Code may only list files in the
allowed working directories for this session: 'D:\Work\pci-platform'.
```

The prior state checks, also verified by direct output:

```text
$ git show HEAD:implementation/operations/CLAUDE-TASKS.md | grep -c "TASK-0027"
0     at 09:48:15Z and 09:50:10Z     -> NOT ready in the authoritative queue
11    at 09:52:16Z                   -> READY, after the concurrent session committed
```

## Actual external state as observed

- **The corpus is UNKNOWN to this session.** `D:\Work\pci-corpus\plan.pdf` was **not** read, opened,
  copied, or inspected. Its presence, size, format, language, and content were **not** observed. The
  `626.8 KB / %PDF-1.7` figures recorded elsewhere in the repository by an *interactive* session were
  **not** confirmed here and are not repeated as this session's findings.
- **No `plan.pdf` exists inside the repository** — `git status --porcelain` carried no `.pdf` path, at
  session start and at the end.
- **No permission setting was changed.** `runner-settings.json` was not opened or edited.
- The concurrent actor's writes ran through 09:49:25Z and its commit landed at ~09:51Z.

## Next operation

**None by this session, and none by a retry.** TASK-0027 does not become executable by running again:
the queue condition is cleared, but the permission boundary is unchanged and will refuse the next
unattended runner identically.

**What is required is one decision — MSG-0082 option A (a narrow read permission for
`D:\Work\pci-corpus\`), option B (run TASK-0027 interactively), or option C (an operator-supplied
extraction).** None is Claude's to choose. TASK-0027 needs **no re-authorization**; MSG-0080 still
authorizes it.

## Is resumption safe?

**Yes — and there is nothing to resume.** No survey was performed, no figure produced, no file copied,
no permission changed, no ADR touched, and no other actor's work modified. A future session starts
TASK-0027 from the beginning.

**Do not re-run any operation on the strength of this checkpoint** (`CLAUDE.md` recovery rule f).
Re-verify the corpus read, the committed queue, and `git status` directly — **and do attempt the read
rather than assuming it will fail**, since a denial recorded here is evidence about this runner at this
time, not a standing fact.

## Files written by this session

```text
implementation/blockers/BLK-0010-corpus-read-denied-to-unattended-runner.md   (new)
implementation/operations/checkpoints/TASK-0027.md                            (new, this file)
implementation/blockers/README.md                                             (BLK-0010 index row)
implementation/status/current.md                                              (state reconciliation)
implementation/operations/CLAUDE-TASKS.md                                     (TASK-0027 outcome)
```

All staged **by explicit path**. **`git add -A` was never run** — while the tree held another session's
mid-edit files it would have swept them into history under this session's authorship.
