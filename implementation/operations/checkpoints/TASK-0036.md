# TASK-0036 — Checkpoint

**Task:** TASK-0036 — encode Q4/Q5/Q6 as strict Shape-1 clearance gates in the EPA-0006 probe
specification
**Authority:** MSG-0110 §2–§6; queue section `implementation/operations/CLAUDE-TASKS.md` TASK-0036
**Type:** evidence-instrument update — documentary, no ADR change, no technology selected

---

## Checkpoint 1 — startup verified, nothing changed yet

**Written:** 2026-08-23, after the mandatory startup checklist and before any file was modified.

**Phase:** startup complete; work not begun.

### Starting repository state (recorded for the mid-run movement rule)

```text
git status --short --branch   ->  ## main...origin/main        (no changes; tree clean)
git rev-parse HEAD            ->  f984b9c25d174fb84bfb78bff74ccf56a333093f
git rev-parse origin/main     ->  f984b9c25d174fb84bfb78bff74ccf56a333093f
```

**Starting HEAD = `f984b9c`, and `origin/main` agrees.**

> **Known runner limit, recorded rather than routed around.** `git fetch` is off this runner's Bash
> allowlist and was refused (`This Bash command contains multiple operations. The following part
> requires approval: git fetch origin`). The `origin/main` value above is therefore the **local
> remote-tracking ref**, not a freshly fetched one, and is reported as such. This is the same limit
> MSG-0097 §4 recorded for TASK-0031. The stronger evidence is the push: a mid-run move would turn it
> into a rejected non-fast-forward.

### Session provenance

Supervisor-started. `implementation/operations/supervisor/state/runner.lock` read in this session:

```json
{ "taskId": "TASK-0036", "pid": 25120, "acquired": "2026-08-23T13:17:18Z", "host": "LENOVO-LA0X1754" }
```

The lock names **this** task, so it is not a foreign runner. The heartbeat read alongside it reported
`RUNNER_RUNNING`, `readyTask: TASK-0036`, `head: f984b9c…` — **agreeing with the git state above.**

### Startup checklist — all ten items completed

| # | Item | Result |
|---|---|---|
| 1 | `CLAUDE.md` | read |
| 2 | `AGENTS.md` | read |
| 3 | `implementation/status/current.md` | read (current-position section; the file is 3,419 lines of retained history) |
| 4 | `implementation/operations/CLAUDE-TASKS.md` | read — board row 48 and the TASK-0036 section, lines 3127–3238 |
| 5 | Active work package `docs/program/work-packages/WP-0009-employee-policy-assistant.md` | read in full (358 lines) |
| 6 | Referenced ADRs / specs / architecture | **ADR-0020 as amended** read in full; **EPA-0006 §4.4–§4.8** read in full; MSG-0110, MSG-0111, MSG-0109 §6–§9 read |
| 7 | `docs/operations/pci-server-bootstrap.md` | **not read — and not required.** No host operation is in scope; this task writes repository files on the Windows authoring machine only |
| 8 | OPEN items in `comms/`, `blockers/`, `discoveries/` | reviewed — see below |
| 9 | `git status`, `git log --oneline -5` | run; results above |
| 10 | Verify environment directly | done — lock and heartbeat read from disk in this session, not recalled |

### Open items — none blocks this task

- **No blocker is open.** BLK-0001…BLK-0010 all read **RESOLVED** in
  `implementation/blockers/README.md`; the file's own summary line states *"No blocker is open."*
- **OPEN communications are informational.** MSG-0111 (this task's reconciliation), MSG-0109,
  MSG-0108, MSG-0107a, MSG-0106, MSG-0104, MSG-0103, MSG-0100, MSG-0097, MSG-0093, MSG-0091,
  MSG-0090, MSG-0089, MSG-0087, MSG-0084, MSG-0081, MSG-0060. None requires a decision before this
  task proceeds; MSG-0111 §4 explicitly says its surfaced subtlety *"blocks nothing."*
- **No `TASK-0036.md` checkpoint existed before this one**, which independently corroborates that the
  task has not run before. Recovery rule (f) therefore has nothing to guard against here.

### Prerequisites checked, not assumed

| Prerequisite | Verified how | Result |
|---|---|---|
| MSG-0110 **DECIDED** | file read; header `**Status:** DECIDED` | **MET** |
| MSG-0109 evidence exists | file read; §6.1 design verdicts and §7 nine-row MSG-0104 table present | **MET** |
| TASK-0034 criterion exists | EPA-0006 **§4.6 S1–S11** present at lines 364–541 | **MET** |
| TASK-0035 §4.8 present | EPA-0006 **§4.8** present at lines 607–697 | **MET** |
| Status **READY** | queue board row 48 and section line 3129 both read **READY** | **MET** |

### The one substantive finding already established, before any edit

**The staleness threshold Q5 depends on is NOT fixed numerically anywhere in the accepted set.** This
was checked rather than assumed — `grep -i "stale"` across the whole authoritative `docs/` tree
returns four files, and the only two hits that bear on it are both in ADR-0020:

```text
docs/decisions/ADR-0020-…:35   - **a stale index beyond threshold triggers abstention (A7), never a stale answer**;
docs/decisions/ADR-0020-…:228  - **The staleness threshold that triggers A7** — an operational parameter, tuned with real evidence.
```

Line 228 sits under ADR-0020's ***Deliberately not decided here*** heading. So the accepted set names
the threshold and **deliberately declines to fix its value.**

**Consequence for this task, and it is exactly the case MSG-0111 §4 anticipated:** the Q5
"bounded re-materialisation" gate can be made testable **structurally** — a bound exists, is
configured, is enforced, and its breach triggers abstention A7 — but **not numerically.** Choosing a
number would invent the tolerance MSG-0110 §3 forbids and would amend an accepted ADR by implication.
**The gate will therefore be written as structural, will say so, and the numeric gap will be referred
to the Architecture Lead rather than closed.**

### Next operation

Add EPA-0006 **§4.9** encoding Q4, Q5 and Q6 as explicit clearance gates. Nothing is written yet.

**Resumption is safe.** No file has been modified; the repository is byte-identical to `f984b9c`.

> **A double-application hazard for any session resuming from here.** If EPA-0006 already contains
> the string `### 4.9`, the specification has already been written and **must not be written again** —
> a second copy of a clearance-gate section is worse than none, which is the failure MSG-0097 §
> recorded for TASK-0031's hunk 1. **Check before writing.** At the time of this checkpoint,
> EPA-0006's last subsection is **§4.8** and no `### 4.9` exists.

---

## Checkpoint 2 — deliverable written, committed and pushed

**Written:** 2026-08-23, **after** the push was accepted. Nothing in this checkpoint is anticipated.

**Phase:** complete. Task stops here, as MSG-0110 §6 requires.

### Completed operations, each verified by observation

| # | Operation | How it was verified |
|---|---|---|
| 1 | Startup checklist, prerequisites, starting HEAD | checkpoint 1 above |
| 2 | Checkpoint 1 committed `8c84137` and pushed | `f984b9c..8c84137 main -> main` |
| 3 | **EPA-0006 §4.9 written** — G-Q4, G-Q5, G-Q6, the applied-gates table, Q7 | `git diff --stat` → **272 insertions(+), 0 deletions**, one file |
| 4 | Declared note added after **§4.6 S6**'s table; question-numbering note added at **§4.7**'s head | included in the same additive diff; **no existing sentence deleted or reworded** |
| 5 | **MSG-0112** execution record written | file present and registered |
| 6 | Queue updated — board row 48 and the TASK-0036 section → **COMPLETE**; **MSG-0112** ledger row added | `grep -n "TASK-0036"` shows the board row, the ledger row and the section agreeing |
| 7 | COMMS register updated — MSG-0112 row, newest-first | file |
| 8 | Status updated — new current position; **prior line and prior position retained**, both declared | file |
| 9 | **Boundary verification** | `git diff --name-only docs/` → **empty**; `git diff --name-only docs/decisions/` → **empty** |
| 10 | Double-application check | `grep -c "^### 4.9"` → **1** |
| 11 | **Committed `a3c3729` and pushed** | `8c84137..a3c3729 main -> main`; `git status --porcelain` → **empty** |

### Last verified operation

**Operation 11 — the push was accepted.** That is also this session's mid-run movement check: a move
of `origin/main` under the session would have produced a **rejected non-fast-forward**, and both
pushes were **fast-forward**. `HEAD` was **re-read immediately before the commit** and still read
`8c84137`, the value this session itself had written.

### Actual state as observed, not assumed

- **`docs/` is untouched.** No accepted ADR was modified — ADR-0019 and ADR-0020 included.
- **Nothing is CLEARED.** Nine MSG-0104 verdicts and eight TASK-0035 design verdicts reproduced
  unchanged in MSG-0112 §6. **No figure in the record is new**, and **neither probe was re-run** —
  `implementation/probes/` never appeared in `git status`.
- **No numeric staleness threshold was chosen.** G-Q5.1 is **structural** and says so in its own text;
  the gap is referred as **Q7**.
- **No implementation task is READY.** TASK-0036's *Next eligible task* reads **none**.
- Repository HEAD is **`a3c3729`**, matching `origin/main`, with a clean tree.

### Next operation

**None. The task is complete and the run stops here.** MSG-0110 §6 requires stopping at the
evidence-instrument update and the COMMS record; both exist and are pushed.

**The next action is the Architecture Lead's:** rule **Q7** — whether the structural staleness gate is
the intended standing state or a numeric bound should be fixed, and if so where — or authorize the
next task. **Q7 blocks nothing**; a probe can run G-Q4, G-Q5 and G-Q6 today.

**Resumption is unnecessary, and re-running this task would be a defect.** §4.9 exists. Re-running
would insert a second copy of the same clearance-gate section — the double-application hazard
MSG-0097 recorded for TASK-0031. **Check for `### 4.9` in EPA-0006 before writing anything.**
