# CHECKPOINT — TASK-0004

**Checkpoint:** 2 (final)
**Written:** 2026-08-19
**Task status:** COMPLETE
**Resumption safe:** N/A — task finished; nothing in flight

## Current phase

Complete. Fix written, committed, and verified against a clean initialisation.

## Completed operations

1. Guard moved before `CREATE ROLE` in `deploy/compose/initdb/00-roles.sql` — verified by reading the committed file and by the negative test below.
2. `\set ON_ERROR_STOP on` added — verified: a failing init now exits 3 instead of continuing.
3. Minimum password length (16) enforced — verified by code and by the guard message.
4. Idempotent `ALTER ROLE` path preserving NOSUPERUSER/NOBYPASSRLS — verified by inspection.
5. Post-creation posture assertion — verified by inspection; would raise on SUPERUSER/BYPASSRLS.
6. Grants (CONNECT via `current_database()`, USAGE + CREATE on public) — verified live.
7. `pci_test` database created and owned by `pci_app` — verified live.
8. `PGOPTIONS` wired in the compose postgres service — verified by reading the committed file.

## Last verified operation

**Gate G1, on a throwaway container with its own ephemeral volume** (2026-08-19):

```text
NEGATIVE (no pci.app_password):
  container state: exited exit=3
  guard message present: 3 occurrences
  -> initialisation FAILS instead of reporting healthy with an unusable role

POSITIVE (PGOPTIONS supplied):
  role: password_set=true super=false bypassrls=false login=true
  CREATE on public: true
  USAGE on public: true
  pci_test: exists=true owner=pci_app
  no ERROR/FATAL in the init log
```

**No manual SQL was run in either case.**

## Next operation

None for this task. The remaining proof — a full `docker compose up` from a fresh
`pci-kernel_postgres-data` volume — belongs to TASK-0006 and requires its separate
destructive-operation authorization, which MSG-0012 explicitly did not grant.

## Actual external / system state

- `pci-kernel_postgres-data` volume: **untouched**. Still carries the 2026-08-19 manual
  workaround (role password set out of band, `pci_test` created by hand).
- The live `pci-kernel-postgres-1` container was not restarted or re-initialised.
- All throwaway verification containers were removed.

**This matters for a resuming session:** the live database was provisioned manually before this
fix existed. Its current state does NOT demonstrate the fix, and the fix's own initdb script will
not run against it again — `initdb` scripts execute only on first initialisation.

## Git

HEAD: 4519dfa   origin/main: 4519dfa   working tree: clean

## Notes for a resuming session

An intermediate commit (`a259888`) fixed a defect found during verification: `GRANT CONNECT ON
DATABASE :"POSTGRES_DB"` referenced a psql variable the postgres entrypoint does not define,
which aborted initialisation. That abort was itself evidence the ON_ERROR_STOP fix works.
