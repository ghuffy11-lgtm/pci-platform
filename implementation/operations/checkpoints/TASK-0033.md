# TASK-0033 — Checkpoint

**Task:** TASK-0033 — Bounded retrieval-engine conformance probe (evaluation only)
**Authority:** MSG-0101 (AUTHORIZED) · queue section `CLAUDE-TASKS.md` §TASK-0033
**Owner:** Claude Code — supervisor-started session

---

## Checkpoint 1 — startup verified, environment established, nothing yet written

**Written:** 2026-08-23, after the startup checklist completed and before any deliverable was created.
**Phase:** preparation complete; probe not yet built or run.

### Starting repository state — VERIFIED in this session

```text
git rev-parse HEAD    -> d4a0d2d24504916b002b33f6b6b72b894d39404a
git rev-parse origin/main -> d4a0d2d24504916b002b33f6b6b72b894d39404a
git status --porcelain -> " M implementation/operations/CLAUDE-TASKS.md"
```

**`HEAD = d4a0d2d`, and local `main` agrees with the remote-tracking `origin/main`.**

> **Known runner limit, restated because it bounds the claim above.** `git fetch` is off this runner's
> Bash allowlist. The `origin/main` comparison is against the **local remote-tracking ref**, not a
> freshly fetched one. A mid-run move of the real remote is detectable here only by a **rejected push**.

### The working tree was NOT clean at session start — recorded, not tidied away

Unlike every prior task in this sequence, this session started against a **dirty working tree**. The
diff is exactly one added line:

```text
git diff --stat -> implementation/operations/CLAUDE-TASKS.md | 1 +
                   1 file changed, 1 insertion(+)
```

The inserted line is the **TASK-0033 status-board row marking it READY**. So at session start:

- **MSG-0101 was committed** (it is the tip commit, `d4a0d2d`) and authorizes TASK-0033;
- **the queue row marking TASK-0033 READY existed only in the working tree**, uncommitted and unpushed.

**This is not a mid-run repository movement and the abort rule does not fire.** The abort boundary
(CLAUDE.md, MSG-0028 decision 2) triggers when `HEAD` or `origin/main` moves *after* a session starts.
Neither has moved. What is present is an **incomplete reconciliation** left in the tree by the COMMS
session that wrote MSG-0101 — the reconciliation MSG-0101 §5 explicitly permits, caught mid-flight.

**It is recorded here rather than silently committed**, because a future reader comparing the queue's
history against this task will otherwise find the READY row appearing in the same commit as the work it
authorized, and have no way to tell that the authorization (`d4a0d2d`) genuinely preceded it.

**One consequence is load-bearing and is stated plainly:** the board row references *"the task section"*,
and **no TASK-0033 task section exists in `CLAUDE-TASKS.md`**. `grep -rln "TASK-0033"` over the whole
repository returned exactly two files — `MSG-0101` and the board row itself. **MSG-0101 is therefore the
sole specification for this task**, as MSG-0080 was for TASK-0027 and MSG-0073 for TASK-0025. Completing
the reconciliation — adding the task section — is part of this run.

### Runner lock — this session's own, verified rather than assumed

```text
implementation/operations/supervisor/state/runner.lock
  { "taskId": "TASK-0033", "pid": 15500, "acquired": "2026-08-23T06:37:18Z", "host": "LENOVO-LA0X1754" }

state/heartbeat.json
  decision "RUNNER_RUNNING", reason "TASK-0033 running for 30s", readyTask "TASK-0033",
  runnerActive true, runnerPid 15500, head d4a0d2d24504916b002b33f6b6b72b894d39404a
```

The lock names **this** task and its head matches the head this session started against. **No stale
lock, no concurrent runner.**

### Prerequisites — checked individually, not inherited from the queue row

| Prerequisite | State | How it was verified here |
|---|---|---|
| **MSG-0101 AUTHORIZED** | **MET** | File read in full; header reads `Status: AUTHORIZED`, dated 2026-08-23; §2 names TASK-0033 and §5 permits reconciling it READY and executing it |
| **EPA-0006 exists (TASK-0032)** | **MET** | `implementation/architecture/EPA-0006-assistant-technology-evaluation.md` read; §4.1 Shapes 1/2/3 and §4.4 the three evidence tiers are the ones this probe must apply |
| **ADR-0020 + AMD-01 applied** | **MET** | `docs/decisions/ADR-0020-*.md` read in full; header carries `**Amended:** 2026-08-23 — AMD-01 (MSG-0095), applied in place`; the §4 engine-selection clause is present |
| **TASK-0033 is READY, and the only READY task** | **MET, with the caveat above** | Board row 45 reads `READY`; no other row reads READY. The row is uncommitted — see the section above |
| **No OPEN blocker** | **MET** | No `implementation/blockers/*.md` carries an OPEN status. The only OPEN item repository-wide is DISC-0006, a non-blocking discovery whose substance the status file records as RESOLVED |
| **No prior TASK-0033 work exists** | **MET** | No `checkpoints/TASK-0033.md` existed before this file; `EPA-0007` returns **zero** matches repository-wide; the highest message is **MSG-0101** |

### Execution environment — VERIFIED directly in this session, because this is the first task in the sequence that needs one

Every prior WP-0009 task was documentary. **This one requires an engine to execute queries against**,
so the environment is evidence rather than background.

| Capability | State | Evidence quoted from this session |
|---|---|---|
| **Docker Windows service** | **RUNNING** | `sc query docker` -> `STATE : 4  RUNNING` |
| **Docker Desktop helper service** | **RUNNING** | `sc query com.docker.service` -> `STATE : 4  RUNNING` |
| **`docker` CLI from this runner** | **UNREACHABLE** | `docker --version` -> `bash: docker: command not found` (exit 127). The binary is not on this shell's `PATH`, and `C:\Program Files` is outside the session's allowed read roots, so it could not be located |
| **PostgreSQL client** | **ABSENT** | `psql --version` -> `bash: psql: command not found` (exit 127) |
| **Node.js** | **PRESENT** | `node --version` -> `v24.15.0`; `npm --version` -> `11.12.1` |
| **`node:sqlite` (built-in)** | **PRESENT** | `node -e` probe printed `node:sqlite OK` and `sqlite version 3.51.3` |
| **SQLite FTS5** | **PRESENT** | the same probe created a `create virtual table t using fts5(a)` successfully and printed `FTS5 available` |

**The diagnosis matters more than the failure, per CLAUDE.md rule 5.** The board row's note says
*"Docker engine not reachable"*. **That is not what was found.** The Docker **engine service is running**;
what is unreachable is the **`docker` CLI from this runner's shell**. The distinction is the difference
between "the operator must start Docker" (wrong — it is started) and "this runner cannot invoke it"
(correct). **No workaround was attempted**: the CLI was not invoked by absolute path, no `PATH` was
modified, and no permission was widened. Rule 2 forbids routing around a permission or reachability
boundary, so it is recorded.

**What this makes possible, and what it forecloses.** Docker-hosted candidates — a search engine of
class S, a vector store of class V, a PostgreSQL of class R — **cannot be probed from this session at
all.** What remains reachable is **SQLite via Node's built-in `node:sqlite`**, which is a genuine member
of **class R** (relational, with FTS5 lexical search), exposes `EXPLAIN QUERY PLAN` for tier-3 plan
evidence, and supports user-defined SQL functions, which permits counting how many rows the engine
actually examines. **All three of EPA-0006 §4.4's evidence tiers are obtainable for that one candidate,
with no installation, no network access, and no new permission.**

### Boundary decisions taken before writing anything

1. **No software will be installed and no network will be reached.** `npm install *` is on this runner's
   allowlist, but MSG-0101 §4 stops execution that would require *"provisioning an implementation
   runtime"*, and fetching packages would also send a request to an external service. The probe uses
   only what is already present.
2. **The corpus will be synthetic and generated in-process.** MSG-0101 §4 stops execution that would
   require *"entering a real or confidential corpus"*. `D:\Work\pci-corpus` will not be read by this
   task, even though MSG-0083 grants read access to it.
3. **No engine will be selected, adopted, or recommended.** The candidate is a **test subject**, which
   MSG-0101 §3 permits explicitly and distinguishes from adoption.

### Next operation

Complete the queue reconciliation (task section + MSG-0102), commit, then build and run the probe.

### Resumption safety

**Safe.** Nothing has been created or modified by this session at the time of writing. A resuming
session should re-verify the environment table above rather than trust it, and must re-check whether
`EPA-0007` and the probe directory already exist before writing either — recovery rule (f).

---

## Checkpoint 2 — STOPPED at the mid-run repository-movement boundary; task NOT executed

**Written:** 2026-08-23, immediately after the move was detected and before any further change.
**Phase:** **terminated at a stop boundary.** The probe was never built or run.

### What moved

```text
recorded at session start (checkpoint 1)   HEAD = d4a0d2d24504916b002b33f6b6b72b894d39404a
observed a few minutes later               HEAD = 55a617cef64c7529dfad806d62268029c903d4ce
                                    origin/main = 55a617cef64c7529dfad806d62268029c903d4ce
```

`55a617c` — *"ops: reconcile TASK-0033 - conformance probe, with its execution tiers gated"*, authored
`Sun Aug 23 09:39:24 2026 +0300` (= **06:39:24Z**), 4 files changed, 256 insertions, 2 deletions. It
was written by a **concurrent interactive session**, not by this runner.

**This runner's lock was acquired at 06:37:18Z and its heartbeat read `RUNNER_RUNNING` at 06:37:48Z —
2 minutes 6 seconds before that commit.** The run was already in flight.

### Why the session stopped

Two independent rules fire, and neither is discretionary:

1. **`CLAUDE.md`, *Mid-run repository movement — abort*** (authority MSG-0028 decision 2) — *"a
   fail-closed recovery boundary, not a warning."*
2. **TASK-0033's own stop conditions**, which arrived in the moving commit itself: *"Also stop if
   `origin/main` moves mid-run."*

The continuation clause is **not** satisfied — the repository is at `55a617c` and this session recorded
`d4a0d2d`. **The move was benign and convergent** (it was this task's own authorized reconciliation
under MSG-0101 §5, and it contradicted nothing this session had done), **and that was not treated as
sufficient to continue.** Reasoning in MSG-0103 §2.

### Last verified operation

The startup checklist and environment verification recorded in checkpoint 1. **All of it remains
true**, and none of it was invalidated by the move — the moving commit touched the queue, COMMS, the
comms index and the status file, none of which change the environment findings.

### Actual system state as observed

```text
git rev-parse HEAD        -> 55a617cef64c7529dfad806d62268029c903d4ce
git rev-parse origin/main -> 55a617cef64c7529dfad806d62268029c903d4ce   (settled; equal)
git status -sb            -> ## main...origin/main  (no ahead/behind marker)
```

**The repository has stopped moving.** The tree carries only this session's own documentation.

### What this session produced, stated exactly

- **checkpoint 1 and checkpoint 2** (this file);
- **MSG-0103** — the stop record, and a correction to the probe environment;
- **queue, status and COMMS-index reconciliation** for the above.

**And what it did NOT produce, which matters more:**

- **no probe harness, no fixture, no query, no execution of any engine;**
- **no candidate evaluated and no verdict** — nothing `CLEARED`, `DISQUALIFIED`, or `NOT CLEARED`;
- **no `EPA-0007`** — the identifier remains unused repository-wide;
- **no accepted ADR modified** — `git diff --name-only docs/decisions/` is **empty**;
- **no engine selected, installed, started, or recommended**; no corpus entered; no network reached.

### The one substantive finding, carried into MSG-0103 §3 so it is not lost

**The queue section and MSG-0102 §2 tell the next runner that Tier 2 and Tier 3 evidence cannot be
obtained on this machine. That is not correct for class R**, and acting on it would scope the probe
around a constraint that does not hold.

`sqlite3` **the CLI** is genuinely absent — but **SQLite the engine is embedded in the Node runtime the
same table records as available**, reachable via the built-in `node:sqlite` with no installation, no
`PATH` change, no network and no Docker. VERIFIED here:

```text
node:sqlite OK · sqlite version 3.51.3 · FTS5 available
DatabaseSync.function typeof: function
EXPLAIN QUERY PLAN -> {"detail":"SEARCH c USING INDEX i_scope (scope=? AND eff_from<?)"}
```

So plan evidence and counter instrumentation — **EPA-0006 §4.4's tier 3, the tier it says is the one
that "actually discharges AMD-01's selection criterion"** — are both obtainable. **This is capability
evidence and explicitly not a conformance verdict**; no candidate was probed.

**It is a narrow correction.** Classes S and V stay unreachable (containerised), class K stays
unmeasured (no PostgreSQL), and acceptance criterion 6 — strategy-switching under selectivity — still
cannot be settled for S or V, because SQLite has no approximate vector index. **The probe is not
complete; one class moved from "no evidence obtainable" to "all three tiers obtainable."**

### Next operation

**None in this session.** The next supervisor cycle should start a fresh runner against the now-stable
`HEAD = 55a617c`.

### Resumption safety

**Safe, and there is nothing to resume.** The task is at its starting line, not part-way through.

**Recovery rule (f) applies with unusual force here: do not re-run anything on the strength of this
checkpoint, because nothing was run.** A resuming session must:

- **re-verify the environment from scratch** — an operator may have started Docker Desktop in the
  interval, which the queue's own recovery procedure anticipates and which would widen the probe;
- **re-verify the `node:sqlite` finding above** rather than inherit it — a checkpoint is a pointer,
  never evidence;
- **check that `EPA-0007` and any probe directory still do not exist** before creating either;
- **re-check `HEAD` immediately before every commit and push**, and **read `runner.lock`** before
  assuming no other actor is live.

**TASK-0033 remains READY and this session changed no task status.** The condition that stopped it was
transient and has already cleared.
