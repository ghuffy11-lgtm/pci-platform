# TASK-0047 — Checkpoint

**Task:** TASK-0047 — define the byte-level durability containment requirement (Q19)
**Checkpoint:** 1 (and final)
**Written:** 2026-08-25, **after** the operation was verified — never in anticipation of it
**Session:** interactive COMMS session (not supervisor-started; no `runner.lock` held)

---

## Phase

**COMPLETE.** Documentation/architecture definition only; **nothing was executed and no test count is
claimed.**

## Repository state

| | |
|---|---|
| **Starting HEAD** | **`6bb259a`** — verified equal to `origin/main` before any edit |
| HEAD at completion | see the commit that carries this file |
| Working tree | clean after commit |
| `git diff --name-only docs/` | **empty** |
| Mid-run movement | **none observed**; `origin/main` re-checked before the push |

## Completed operations, each verified

1. **Read the authorization chain before editing:** the queue board row (single READY task),
   `implementation/operations/TASK-0047-byte-durability-architecture-requirement.md`, **MSG-0160**
   (Q19 = YES), **MSG-0158** (TASK-0046 evidence), **§4.13** N1–N5, **§4.16** DA-1…DA-7, **§4.17**.
2. **Checked the recovery condition first:** searched for an existing byte-level durability requirement.
   **§4.13's N-table holds N1–N5 and no byte limb; §4.16 states DA-1…DA-7 as artefact criteria.**
   **N6 did not exist — this is creation, not a second statement of an existing rule.**
3. **Wrote EPA-0006 §4.18 — N6**, with limbs N6.1–N6.3, the two comparison tables (N1 / N6, and
   N6 / DA-1), the evidence boundary, and the *"what this section does NOT do"* block.
4. **Added a declared pointer note under §4.13's N-table.** **No row of that table was changed.**
5. **Recorded MSG-0161**, mapping all seven required outcomes to evidence and referring **Q20**.
6. **Reconciled the queue** (TASK-0047 **COMPLETE**, board row), **the COMMS register and ledger**
   (MSG-0159, MSG-0160, MSG-0161), and **the status file**.
7. **Reconciled two stale status claims found while doing so:** an orphaned fragment left by an earlier
   edit, and a paragraph asserting the **§4.17 promotion was still OUTSTANDING** — **it was discharged
   in `0c39249` and reconciled in MSG-0159**, verified on `main` this session.

## Last verified operation

**§4.18 read back from `origin/main` after pushing**, present between §4.17 and section 5, with
§4.13's table unchanged.

## Next operation

**None. The run stops here.** MSG-0160 authorized **one** bounded task and it is complete. **No task is
READY**, and **the next evidence action — measuring anything against N6 — is Q20 and must be separately
authorized.**

## Actual external state as observed

- **Nothing was installed, executed or deployed.** No probe, fixture or harness was written or run.
- **No engine selected, adopted, deployed, implemented or cleared.**
- **No invariant amended; N1–N5 stand as written; no candidate verdict moved.**
- **Nothing has been measured against N6.** **Unmeasured is not satisfied.**

## Resumption safety

**Safe, and there is nothing to resume.** **Do not re-run this task**: re-running it would insert a
second statement of N6, which is the drift §4.12's Q12 note exists to prevent. **Check whether §4.18
already exists before writing anything** — it does.
