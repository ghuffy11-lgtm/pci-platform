# TASK-0034 — Checkpoint

**Task:** TASK-0034 — Update the retrieval-engine criterion and probe specification for strict Shape-1
**Authority:** MSG-0105 (DECIDED) §3–§5 · queue section `CLAUDE-TASKS.md` §TASK-0034 · reconciled by MSG-0106
**Owner:** Claude Code — supervisor-started session

---

## Checkpoint 1 — startup verified, nothing yet written

**Written:** 2026-08-23, after the mandatory startup checklist completed and before any deliverable was
created.
**Phase:** preparation complete; criterion and probe specification not yet drafted.

### Starting repository state — VERIFIED in this session

```text
git rev-parse HEAD        -> 1451024b6c75a5187a204d3921700752c6c35cf4
git rev-parse origin/main -> 1451024b6c75a5187a204d3921700752c6c35cf4
git status --porcelain    -> (empty)
git status -sb            -> ## main...origin/main
```

**`HEAD = origin/main = 1451024`, and the working tree is clean.** This is the state the abort rule
(CLAUDE.md *Mid-run repository movement*, MSG-0028 decision 2) is measured against; it is re-checked
before every commit and push in this run.

> **Known runner limit, restated because it bounds the claim above.** `git fetch` is off this runner's
> Bash allowlist and **was refused in this session** — the exact refusal was *"This Bash command
> contains multiple operations. The following part requires approval: git fetch origin"*. The
> `origin/main` comparison is therefore against the **local remote-tracking ref**, not a freshly
> fetched one. A mid-run move of the real remote surfaces here only as a **rejected push**. This is
> recorded, not routed around; it matches MSG-0097, MSG-0100 and MSG-0104.

### Runner lock — this session's own, verified rather than assumed

```text
implementation/operations/supervisor/state/runner.lock
  taskId   : TASK-0034
  pid      : 24340
  acquired : 2026-08-23T09:17:18Z
  host     : LENOVO-LA0X1754

implementation/operations/supervisor/state/heartbeat.json
  decision     : RUNNER_RUNNING
  readyTask    : TASK-0034
  head         : 1451024b6c75a5187a204d3921700752c6c35cf4
  runnerActive : true
  runnerPid    : 24340
```

**The lock names this task and the heartbeat's `head` equals this session's verified `HEAD`.** It is
this run's own lock, not a stale one from another session. No concurrent runner is indicated.

### Startup checklist — all ten items, with the one qualification stated

| # | Item | Result |
|---|---|---|
| 1 | `CLAUDE.md` | read in full |
| 2 | `AGENTS.md` | read in full |
| 3 | `implementation/status/current.md` | read (first page; the file is 3,226 lines — the current-position sections and the task table were read) |
| 4 | `implementation/operations/CLAUDE-TASKS.md` | read — status board, ledger, and the full TASK-0034 section |
| 5 | Active work package | WP-0009 — `DEFINED — NOT AUTHORIZED FOR IMPLEMENTATION`; this task is architecture/evidence-instrument work under it, not implementation |
| 6 | Referenced ADRs / specifications / architecture | ADR-0020 §4 as amended by AMD-01 (quoted in EPA-0006 §4); EPA-0006 §3, §4.1–§4.5; MSG-0104 in full; MSG-0105 in full; MSG-0106 in full |
| 7 | `docs/operations/pci-server-bootstrap.md` | **not read — and not required.** This task performs **no host operation**. Nothing is installed, started, deployed, or written outside this repository |
| 8 | OPEN comms / blockers / discoveries | comms register and ledger reviewed; blockers and discoveries directories listed. **No blocker is OPEN** (MSG-0106 §7). The OPEN comms are informational records, none blocking |
| 9 | `git status`, `git log` | quoted above |
| 10 | Environment verified directly | runner lock and heartbeat read from disk in this session, not recalled |

### Prerequisites — checked, not assumed

| Prerequisite | State | Evidence |
|---|---|---|
| **MSG-0105 DECIDED** | **MET** | File read in this session; header reads `Status: DECIDED`, `Authority: Architecture Lead` |
| **MSG-0104 probe evidence exists** | **MET** | File read in full; §7 verdict table present and intact |
| **ADR-0020 + AMD-01 applied** | **MET** | AMD-01 text quoted in EPA-0006 §4; applied in `a1be892` per MSG-0097 |
| **EPA-0006 §4.4 tier model exists** | **MET** | Read at `implementation/architecture/EPA-0006-assistant-technology-evaluation.md` lines 250–278 |
| **TASK-0034 still READY** | **MET** | Status board row 46 reads `**READY**`; it is the only READY task on the board |
| **No prior TASK-0034 checkpoint** | **MET** | `checkpoints/` listed; no `TASK-0034.md` existed before this file. **This run is a first attempt, not a resumption** |

### Recovery note for a later session

The task's own recovery procedure says: **do not re-run the probe.** TASK-0033 is closed, its harness
and captured output are committed at `implementation/probes/TASK-0033/`, and its verdicts stand. This
task changes the **criterion**, not the measurements. Nothing in this run executes the probe, and
nothing in this run re-measures any engine.

### Next operation

Draft the strict Shape-1 criterion and probe specification as a **declared, additive amendment** to
EPA-0006 §4 — the record that carries the §4.1 shape definitions and the §4.4 tier model — then write
the numbered COMMS execution record, then reconcile the queue, the status file and the comms ledger.

**Resumption is safe from here.** Nothing has been written, nothing external has been touched, and no
operation in this run is irreversible.

> **Checkpoint 2 will be appended to this file only after the commit exists and the push is accepted.**
> A checkpoint is written after an operation is verified, never in anticipation of one (`CLAUDE.md`,
> *Checkpointing and Recovery*). If this file ends here, the deliverables were not committed.
