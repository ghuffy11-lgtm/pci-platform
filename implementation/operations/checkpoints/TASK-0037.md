# TASK-0037 — Checkpoint

**Task:** TASK-0037 — version-transition freshness and stale-version fail-closed evidence
**Authority:** MSG-0113 §2–§5; queue section `implementation/operations/CLAUDE-TASKS.md` TASK-0037
**Type:** evidence probe — selects nothing, deploys nothing, amends no ADR

---

## Checkpoint 1 — startup verified, nothing changed yet

**Written:** 2026-08-23, after the mandatory startup checklist and before any file was created or
modified.

**Phase:** startup complete; probe not yet written.

### Starting repository state (recorded for the mid-run movement rule)

```text
git status --short --branch   ->  ## main...origin/main        (no changes; tree clean)
git rev-parse HEAD            ->  57732ac090dfcb718d896665442acba5c9c195b7
git rev-parse origin/main     ->  57732ac090dfcb718d896665442acba5c9c195b7
```

**Starting HEAD = `57732ac`, and `origin/main` agrees.**

> **Known runner limit, recorded rather than routed around, and unchanged from TASK-0036.** `git fetch`
> is off this runner's Bash allowlist. The `origin/main` value above is therefore the **local
> remote-tracking ref**, not a freshly fetched one, and is reported as such. The stronger evidence is
> the push: a mid-run move would turn it into a rejected non-fast-forward.

### Session provenance

Supervisor-started. `implementation/operations/supervisor/state/runner.lock`, read in this session:

```json
{ "taskId": "TASK-0037", "pid": 27556, "acquired": "2026-08-23T18:57:17Z", "host": "LENOVO-LA0X1754" }
```

The lock names **this** task, so it is not a foreign runner. `heartbeat.json` read alongside it
reported `RUNNER_RUNNING`, `readyTask: TASK-0037`, `head: 57732ac…`, `runnerPid: 27556` — **agreeing
with the git state above.**

### Startup checklist — all ten items completed

| # | Item | Result |
|---|---|---|
| 1 | `CLAUDE.md` | read in full |
| 2 | `AGENTS.md` | read in full |
| 3 | `implementation/status/current.md` | read — current-position section (the file is 3,511 lines, newest-first, the remainder being retained history) |
| 4 | `implementation/operations/CLAUDE-TASKS.md` | read — board row 49 and the TASK-0037 section, lines 3276–3409 |
| 5 | Active work package `docs/program/work-packages/WP-0009-employee-policy-assistant.md` | read in full (358 lines) |
| 6 | Referenced ADRs / specs / architecture | **ADR-0018 §1–§8**, **ADR-0020 §1–§5**, **ADR-0017 §5** read directly in the accepted `docs/decisions/` copies; **EPA-0006 §4.6 S1–S11, §4.8, §4.9** read in full; MSG-0113 and MSG-0114 read in full |
| 7 | `docs/operations/pci-server-bootstrap.md` | **not read — and not required.** No host operation is in scope; this task writes repository files and runs an in-process probe on the Windows authoring machine only |
| 8 | OPEN items in `comms/`, `blockers/`, `discoveries/` | reviewed — see below |
| 9 | `git status`, `git log --oneline -8` | run; results above |
| 10 | Verify environment directly | done — see the environment table below; every value was obtained by running a command in this session |

### Open items — one blocker is OPEN and it does not gate this task

- **BLK-0010 — corpus read denied to the unattended runner — is OPEN.** It does **not** gate
  TASK-0037: this task's fixtures are **synthetic and generated in-process**, and the task section
  forbids entering a real corpus. No corpus read is attempted.
- **OPEN communications are informational**: MSG-0092, MSG-0097, MSG-0104, MSG-0109, MSG-0114. None
  requires a decision before this task proceeds; **MSG-0114 says so in its own header** — *"OPEN —
  informational; no decision blocks TASK-0037."*
- **No `TASK-0037.md` checkpoint existed before this one**, which independently corroborates that the
  task has not run before. Recovery rule (f) therefore has nothing to guard against here.

### Prerequisites checked, not assumed

| Prerequisite | Verified how | Result |
|---|---|---|
| MSG-0113 **DECIDED** | file read; header `**Status:** DECIDED` | **MET** |
| EPA-0006 §4.9 gates exist | `### 4.9` present at line 716; **G-Q4**, **G-Q5**, **G-Q6** present at lines 734, 792, 869 | **MET** |
| TASK-0033 harness committed | `implementation/probes/TASK-0033/probe.mjs` (434 lines) + `probe-output.txt` present | **MET** |
| TASK-0035 harness committed | `implementation/probes/TASK-0035/probe.mjs` (670 lines) + `probe-output.txt` (354 lines) present, both read | **MET** |
| Status **READY** | queue board row 49 and section line 3278 both read **READY** | **MET** |

### Environment — checked in this session, as the task section requires

| Capability | Command run | Result |
|---|---|---|
| Node runtime | `node --version` | **`v24.15.0`** — VERIFIED |
| SQLite, embedded | `node -e "…new DatabaseSync(':memory:')… select sqlite_version()"` | **`3.51.3`** — VERIFIED present via `node:sqlite` |
| Docker | `docker info` | **`bash: line 1: docker: command not found`** — the CLI is **not on this runner's PATH**. That is the precise observation; it is *not* a claim about whether Docker Desktop is installed or whether its backend would start. **Nothing was installed and Docker Desktop was not started** — an operator action |

**Consequence, unchanged from TASK-0033 and TASK-0035:** the only reachable engine is a class **R**
member. Classes **S**, **V** and **K** stay unreachable and therefore **NOT CLEARED** with zero
execution evidence (EPA-0006 §4.6 S9).

### Two findings already established from the accepted documents, before any code was written

**1. "Revoked" has an accepted name already, and it is WITHDRAWN.** MSG-0113 §1 requires
update/approve/revoke/supersede to be evidenced. **ADR-0018 §2's lifecycle has no `REVOKED` state**;
its graph is `DRAFT → IN_REVIEW → APPROVED → PUBLISHED → SUPERSEDED` with branches to `REJECTED` and
**`WITHDRAWN`**, and its table records WITHDRAWN as *"No"* answerable, *"Yes, for audit only"*
retrievable, **"Dropped from the projection"**. **The probe will therefore exercise WITHDRAWN rather
than invent a state name**, because inventing one would put a vocabulary in the record that the
accepted ADR does not carry.

**2. "Current approved version" must be read as ADR-0018's PUBLISHED-and-effective version, and the
difference is not cosmetic.** MSG-0113 §2(1)/§3 speak of *"the current approved version"*. **ADR-0018
§2 separates `APPROVED` from `PUBLISHED` and marks `APPROVED (not yet published/effective)` as NOT
answerable.** Reading MSG-0113's phrase as the ADR's `APPROVED` **state** would license answering from
a version ADR-0018 forbids. Under the CLAUDE.md authority order the accepted ADR outranks a
communication, so **the strict reading is taken**: the answerable version is the **PUBLISHED**,
**effective**, in-scope one. **That default is fail-closed** — it can only withhold an answer, never
grant one — **so it needs no ruling to operate**, and it is surfaced to the Architecture Lead as a
question rather than treated as a conflict.

### Next operation

Write `implementation/probes/TASK-0037/probe.mjs` and run it. **Nothing is written yet.**

**Resumption is safe.** No file has been modified; the repository is byte-identical to `57732ac`.

> **A double-execution hazard for any session resuming from here.** If
> `implementation/probes/TASK-0037/` already exists, the probe has already been written and possibly
> run — **read its captured output before running anything.** At the time of this checkpoint the
> directory **does not exist**; `implementation/probes/` contains `TASK-0033` and `TASK-0035` only.
> **The TASK-0033 and TASK-0035 harnesses must not be modified and their existing cases must not be
> re-run** — that evidence stands and is committed.
