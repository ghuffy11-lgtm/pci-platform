# CHECKPOINT — TASK-0005

**Checkpoint:** 1 (final)
**Written:** 2026-08-19
**Task status:** COMPLETE
**Resumption safe:** N/A — task finished; nothing in flight

## Current phase

Complete. `.env.example` shipped per DISC-0008 option 1, guard verified intact.

## Completed operations

1. `.env.example` created at the repository root with placeholder-only values — verified tracked, not gitignored.
2. Generation documented (`openssl rand`), with the instruction to generate on the machine that will use them.
3. Compose header documents the setup step.
4. Placeholder token corrected to be genuinely invalid (see below).

## Last verified operation

**Gate G2** (2026-08-19), against the built `pci-kernel-kernel:latest`:

```text
G2a  no principals      -> "PCI_IDENTITY_MODE=static requires at least one entry in
                            PCI_STATIC_PRINCIPALS"          (refused)
G2b  placeholder token  -> "PCI_STATIC_PRINCIPALS[0].token must be a string of at least
                            16 characters"                  (refused)
G2c  documented setup   -> pci-kernel-kernel-1 Up (healthy), /health/ready HTTP 200
```

The fail-closed guard is intact and was not weakened.

## Next operation

None. Full clean-checkout proof is part of TASK-0006's clean-room run.

## Actual external / system state

- `pci-kernel-kernel-1` running and healthy, using the `.env` created during TASK-0001.
- That `.env` is untouched by this task; it holds a real generated principal, is mode 0600,
  and is gitignored.
- All throwaway verification containers removed.

## Git

HEAD: 4519dfa   origin/main: 4519dfa   working tree: clean

## Notes for a resuming session

The first version of the placeholder token was 35 characters, which **passes** the >=16 minimum.
Copying `.env.example` unedited would have produced a running service authenticating against a
token published in the repository, and the file wrongly claimed all placeholders were rejected.
Corrected in `4519dfa` to a 10-character value that the guard refuses. Found by running the
guard, not by reading it.
