# TASK-0038 — Checkpoint

**Task:** TASK-0038 — kernel-constrained retrieval / non-divergent projection evidence
**Authority:** MSG-0116a **and** MSG-0116b (Q8/Q9/Q10 rulings); queue section
`implementation/operations/CLAUDE-TASKS.md` TASK-0038; MSG-0117 (reconciliation)
**Type:** evidence probe — selects nothing, deploys nothing, amends no ADR

---

## Checkpoint 1 — startup verified, and a repository movement observed and reconciled before any work

**Written:** 2026-08-23, after the mandatory startup checklist and before any file other than this
checkpoint was created or modified.

**Phase:** startup complete; probe not yet written.

### The repository moved during startup. This is recorded first because it is the fail-closed boundary.

**A `HEAD` change was observed inside this session**, between two `git rev-parse` calls a few minutes
apart:

```text
first read  (startup, before any action)   HEAD = fb2d127…  origin/main = fb2d127…
                                           git status --porcelain ->
                                             M implementation/comms/README.md
                                             M implementation/operations/CLAUDE-TASKS.md
second read (still inside startup)         HEAD = origin/main = d0cb38e2a9b4cdc9c1c6e6c8fd4f253d19e040c2
                                           git status --porcelain -> (empty)
```

**The move was diagnosed rather than assumed, and it is fully explained.** `git diff --stat
fb2d127 d0cb38e` returns exactly four files — `MSG-0117-task-0038-reconciliation.md` (new, 97 lines),
`implementation/comms/README.md` (+3), `implementation/operations/CLAUDE-TASKS.md` (+130),
`implementation/status/current.md` (+4/−2) — which is **byte-for-byte the uncommitted reconciliation
this session had already read out of the working tree.** The commit is `d0cb38e ops: reconcile
TASK-0038; record a seventh collision, this time lead-vs-lead`.

**Conclusion, VERIFIED:** the concurrent writer was the **interactive COMMS session**, which finished
and committed its own reconciliation. **This is precisely the BLK-0009 pattern**, whose resolution
reads: *"the concurrent writer was the interactive COMMS session; it stopped writing on seeing the
lock and has now committed its reconciliation, so TASK-0027 is READY in the **committed** queue and
`git status` is clean. … The refusal to run `git add -A` or to execute on an uncommitted READY marking
was correct."*

**Why this session continues rather than aborting.** CLAUDE.md's mid-run rule permits continuing *"only
after reconciliation confirms the repository is again consistent with the state the session recorded at
its start."* Three facts make that reconciliation complete:

1. **No work had been done against `fb2d127`.** The move landed during the startup checklist; no file
   was written, no probe existed, and this checkpoint is the session's first write. There is no
   evidence produced against the old state to invalidate.
2. **The move is additive and its content was independently read** in this session before it was
   committed. Nothing was rewritten, reverted, or force-pushed; `d0cb38e` has `fb2d127` as its parent.
3. **`HEAD`, `origin/main` and the working tree now agree**, and the tree is clean.

**The material consequence is in this session's favour and is the reason the distinction matters:**
before the move, TASK-0038 was READY **only in an uncommitted working tree**. **It is now READY in the
committed queue.** Executing on the uncommitted marking would have repeated the error BLK-0009 records
as correctly refused.

**Starting HEAD for this run is therefore `d0cb38e`**, not `fb2d127`, and it is re-checked before every
push.

> **Known runner limit, recorded rather than routed around, and unchanged from TASK-0036 and
> TASK-0037.** `git fetch origin` is off this runner's Bash allowlist — it was attempted in this
> session and the permission layer declined it. The `origin/main` values above are therefore the
> **local remote-tracking ref**, not a freshly fetched one, and are reported as such. The stronger
> evidence remains the push: a genuine divergence would surface as a rejected non-fast-forward.

### Session provenance

Supervisor-started. `implementation/operations/supervisor/state/runner.lock`, read in this session:

```json
{ "taskId": "TASK-0038", "pid": 23788, "acquired": "2026-08-23T20:27:17Z", "host": "LENOVO-LA0X1754" }
```

**The lock names this task**, and its acquisition time coincides with this session's start, so it is
**INFERRED** to be this session's own lock rather than a foreign runner's. **The PID could not be
confirmed alive**: `tasklist` and `Get-Process` are both off this runner's allowlist and the permission
layer declined all three attempts. **That is recorded as UNKNOWN rather than assumed** — but it changes
nothing here, because the only other writer observed in this session was the interactive COMMS session,
which has since committed and gone quiet.

### Startup checklist — all ten items completed

| # | Item | Result |
|---|---|---|
| 1 | `CLAUDE.md` | read in full |
| 2 | `AGENTS.md` | read in full |
| 3 | `implementation/status/current.md` | current-position section read (the file is 3,633 lines, newest-first, the remainder retained history) |
| 4 | `implementation/operations/CLAUDE-TASKS.md` | read — board row for TASK-0038 and the full TASK-0038 section |
| 5 | Active work package `docs/program/work-packages/WP-0009-employee-policy-assistant.md` | read |
| 6 | Referenced ADRs / specs / architecture | **ADR-0020** read in full in the accepted `docs/decisions/` copy; **EPA-0006 §3, §3.1–§3.3, §4.6 S1–S11, §4.7 Q1–Q3, §4.8, §4.9 G-Q4/G-Q5/G-Q6, §4.10 G-Q7** read in full; **MSG-0116a**, **MSG-0116b**, **MSG-0117** read in full |
| 7 | `docs/operations/pci-server-bootstrap.md` | **not read — and not required.** No host operation is in scope. This task writes repository files and runs an in-process probe on the Windows authoring machine only; the PCI server is not touched |
| 8 | OPEN items in `comms/`, `blockers/`, `discoveries/` | reviewed — see below |
| 9 | `git status`, `git log --oneline` | run; results above |
| 10 | Verify environment directly | done — see the environment table; every value was obtained by running a command in this session |

### Open items — no blocker is OPEN, and that is a change from the last checkpoint

**All ten blockers read RESOLVED**, verified by reading the `**Status:**` line of each of BLK-0001
through BLK-0010 in this session.

> **A correction to the record, recorded rather than silently absorbed.** The TASK-0037 checkpoint
> states *"BLK-0010 — corpus read denied to the unattended runner — is OPEN"*. **The committed
> BLK-0010 now reads `**Status:** **RESOLVED** 2026-08-22`.** Both readings can be honest — a status
> may have been corrected between the two sessions — and **the current committed state is what this
> checkpoint asserts.** It gates nothing here either way: this task's fixtures are synthetic and
> generated in-process, and the task section forbids entering a real corpus.

**OPEN communications are informational** — MSG-0100, MSG-0103, MSG-0104, MSG-0106, MSG-0107,
MSG-0108, MSG-0109, MSG-0111, MSG-0112, MSG-0114, MSG-0115, MSG-0117. **None requires a decision
before this task proceeds**, and MSG-0117 says so in its own header: *"OPEN — informational; no
decision blocks TASK-0038."*

### Prerequisites checked, not assumed

| Prerequisite | Verified how | Result |
|---|---|---|
| MSG-0116a **DECIDED** | `MSG-0116-architecture-lead-rulings-q8-q10.md` read; header `**Status:** DECIDED` | **MET** |
| MSG-0116b **DECIDED** | `MSG-0116-architecture-lead-q8-q10-rulings.md` read; header `**Status:** DECIDED` | **MET** |
| **The two MSG-0116 files agree** | read clause by clause in this session; both rule Q8 *no, under conditions*, Q9 *do not relax the bar*, Q10 *strict effective-version*, and both authorize the same next action. **No stop condition fires**, unlike the MSG-0020 pair | **MET** |
| MSG-0115 evidence present | file present and read | **MET** |
| EPA-0006 gates exist | `### 4.6` … `### 4.10` present; G-Q4/G-Q5/G-Q6 at lines 734/792/877, G-Q7 at 1008 | **MET** |
| TASK-0033 / 0035 / 0037 harnesses committed | `implementation/probes/` contains exactly `TASK-0033`, `TASK-0035`, `TASK-0037`, each with `probe.mjs` + `probe-output.txt` | **MET** |
| Status **READY** in the **committed** queue | true as of `d0cb38e`; **was not true at `fb2d127`** — see the movement section above | **MET** |

### Environment — checked in this session, as the task section requires

| Capability | Command run | Result |
|---|---|---|
| Node runtime | `node --version` | **`v24.15.0`** — VERIFIED |
| SQLite, embedded | `node -e "…new DatabaseSync(':memory:'); select sqlite_version()"` | **`3.51.3`** — VERIFIED present via `node:sqlite` |
| Docker | `which docker` | **`docker NOT on PATH`** — the full PATH was printed and contains no Docker entry. That is the precise observation; it is **not** a claim about whether Docker Desktop is installed or whether its backend would start. **Nothing was installed and Docker Desktop was not started** — an operator action the task section forbids |

**Consequence, unchanged from TASK-0033, TASK-0035 and TASK-0037:** the only reachable engine is a
class **R** member. Classes **S**, **V** and **K** stay unreachable and therefore **NOT CLEARED** with
zero execution evidence (EPA-0006 §4.6 S9).

### No prior TASK-0038 artefact exists — checked, because the recovery procedure requires it

| Check | Command | Result |
|---|---|---|
| Probe directory | `ls implementation/probes/` | `TASK-0033`, `TASK-0035`, `TASK-0037` — **no `TASK-0038`** |
| Checkpoint | `ls implementation/operations/checkpoints/` | no `TASK-0038.md` before this file |
| EPA-0006 section | `grep -c "^### 4.11"` | **`0`** — the section this task will add does not yet exist |

**The double-application hazard MSG-0097 recorded therefore has nothing to guard against yet.** A
session resuming from this checkpoint must re-run these three checks before writing anything.

### What this task must produce, restated from the union of both rulings

**The question:** can a **kernel-constrained / in-query authorization path**, and/or an architecture
that **prevents security-relevant projection divergence**, obtain **E1–E4 and G-Q4 evidence without
examining unauthorized content**? MSG-0115 identified this alternative and **explicitly did not measure
it**; both rulings forbid assuming it works.

**The operative addition from MSG-0116b** — and the reason a runner reading only MSG-0116a would build
the wrong instrument: **the re-check must be instrumented *separately* from retrieval-content
examination**, with evidence that it reads **only authoritative kernel facts**; and **a purported
re-check that reads content-bearing data from an unauthorized candidate is examination and fails
Shape-1.**

**The operative addition from MSG-0116a:** **the measured kernel-read count is not by itself a Shape-1
violation.** Taken together these two fix the instrument design: the re-check's reads are counted on a
**separate** counter from `U`, and that counter is **classified** into metadata reads and
content-bearing reads, with any content-bearing read from an unauthorized candidate reported as a
Shape-1 failure rather than as a number.

**Nothing may be cleared by Q8.** E1–E4 and G-Q4/G-Q5/G-Q6/G-Q7 remain independently necessary.

### Next operation

Write `implementation/probes/TASK-0038/probe.mjs` and run it. **Nothing is written yet** except this
checkpoint.

**Resumption is safe.** Apart from this file, the repository is byte-identical to `d0cb38e`.
