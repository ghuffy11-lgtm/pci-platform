# TASK-0053 checkpoint — re-measure the L4 append arm against a deliberately strong history

**Task:** TASK-0053 — settle the L4/W-B divergence by making residue population a controlled variable.
**Authority:** **MSG-0172 §4** (the divergence ruled re-measurable); **MSG-0174** (defined and queued);
**MSG-0177 §3** (reconciled as the single READY task); the Lead's committed task file
[`TASK-0053-l4-wb-re-measurement.md`](../TASK-0053-l4-wb-re-measurement.md), which is the
specification. **MSG-0158** and **MSG-0163** are the two disagreeing runs; **EPA-0006 §4.18** (N6) and
**§4.16** (DA-1…DA-7) bind as written.
**Type:** Bounded evidence / measurement. **It may clear nothing, and satisfying N6 clears nothing.**

---

## Checkpoint 1 — startup complete, nothing yet built

**Written after the startup checklist was run and verified, before any file was created or modified.**

**Phase:** startup and reading complete; the harness is designed but not written.

**Starting `HEAD` recorded for the mid-run movement rule (CLAUDE.md, MSG-0028 decision 2):**

```
git rev-parse HEAD        -> 48817ea5e0f570aad6f13ad12d7905b42db45b61
git rev-parse origin/main -> 48817ea5e0f570aad6f13ad12d7905b42db45b61
git status                -> On branch main / up to date with 'origin/main' / working tree clean
git log --oneline -1      -> 48817ea Verify TASK-0052; queue TASK-0053, the last authorized work
```

**The `git fetch` denial that bounded TASK-0051 and TASK-0052 applies to this runner too, and the
reconciliation at start of run is the same shape as TASK-0052's.** `git fetch origin` was attempted by
this session and refused (`This command requires approval`), so **this session did not itself read the
remote**. However, **`.git/FETCH_HEAD` was VERIFIED written at 2026-08-26 09:37 local — approximately
six minutes before this checkpoint, and immediately before this session was started — by the Execution
Supervisor cycle that launched it**, and its first line reads:

```
48817ea5e0f570aad6f13ad12d7905b42db45b61		branch 'main' of github-pci:ghuffy11-lgtm/pci-platform
```

So **`origin/main` was live-read at `48817ea` minutes before this run began, and local `HEAD` is the
same commit.** That is an observation of a fetch this session did not perform, stated that way
deliberately. **It does not cover movement DURING the run**; for that the detector is unchanged and is
an **interlock rather than a claim** — a non-fast-forward push rejection, which is where **BLK-0013**
fired. **No workaround for the fetch denial was taken.**

**Mandatory startup checklist — completed and verified in this session:**

1. `CLAUDE.md` read in full.
2. `AGENTS.md` read in full.
3. `implementation/status/current.md` — present (**4 643 lines, 421.6 KB**). **Not read in full: the
   file exceeds the reader's size limit.** That is a fact about the file, not a skipped step. Its
   current state was established from the queue board, MSG-0176 and MSG-0177 instead, and it is updated
   by this task before completion.
4. `implementation/operations/CLAUDE-TASKS.md` read — status board rows for TASK-0043…TASK-0053 and the
   TASK-0045/0046/0048 detail sections.
5. **TASK-0053 confirmed `READY` and confirmed the ONLY READY row**, by the executor's own parser
   rather than by eye:
   ```
   node implementation/probes/TASK-0050/queue-parse-check.mjs
     rows parsed : 51
     PROBLEMS    : none
     READY tasks : TASK-0053
   ```
6. The Lead's committed task file read in full. **It is the specification**, and the queue row says so
   in terms: *"where the file and this row differ, the FILE WINS and the difference is reported."*
   Also read at the source, not from a summary: **EPA-0006 §4.18** (N6, N6.1–N6.3, the N6/N1 table and
   the evidence boundary) and **§4.16** (DA-4 provenance, DA-5 vocabulary, DA-6 fail-closed, DA-7).
7. `docs/operations/pci-server-bootstrap.md` — **NOT read, and recorded as not-applicable rather than
   silently skipped.** The checklist requires it *"before any host operation"*. **This task performs no
   host operation and does not touch the PCI server.** It runs a Node harness on the Windows
   development machine, writing fixtures under the OS temp directory and removing them — the same
   mechanism TASK-0045, TASK-0046, TASK-0048 and TASK-0050 used. **Nothing is installed, no host
   configuration is modified, and the `/data` boundary is not engaged because the PCI server is not
   involved.**
8. Open items reviewed. **BLK-0012 OPEN** (no reachable subject supplies non-adverse E4) —
   **not a prerequisite of this task and does not block it**; it concerns E4 subject reach, which this
   task does not touch. **BLK-0013 CLEARED** (MSG-0169). All other blockers RESOLVED/CLOSED.
   **DISC-0013 and DISC-0014 OPEN**, neither blocking. **MSG-0177 is the newest OPEN communication and
   it is this task's authorization.**
9. `git status` and `git log --oneline -5` run; quoted above.
10. Environment verified directly rather than assumed:
    ```
    node --version                                  -> v24.15.0
    node -e "… SELECT sqlite_version()"             -> 3.51.3   (via node:sqlite)
    ```
    **This is the same subject and build TASK-0046 and TASK-0048 measured** (MSG-0158 and MSG-0163 both
    record *"SQLite 3.51.3 via node:sqlite, node v24.15.0"*), which is what makes this run comparable to
    both. **`py` is NOT used and no grant for it is sought** — MSG-0145's grant is scoped to TASK-0043.

**Prerequisites checked individually, not assumed from the queue:**

| Prerequisite | State |
|---|---|
| MSG-0172 §4 AUTHORIZED (divergence ruled re-measurable) | **VERIFIED** — read at lines 106–121: *"a bounded re-measurement with a deliberately strong history arm IS authorized"* |
| MSG-0174 (defined and queued) | **VERIFIED** — file present |
| MSG-0177 (reconciled READY) | **VERIFIED** — §3 names TASK-0053 the single READY task and quotes the same parser result reproduced above |
| MSG-0158 / MSG-0163 present as the two priors | **VERIFIED** — both files read; the disagreeing numbers confirmed at source, and both prior harnesses and outputs read under `implementation/probes/` |
| EPA-0006 §4.18 binding as written | **VERIFIED** — read at lines 2853–3002 |
| EPA-0006 §4.16 binding as written | **VERIFIED** — DA-4…DA-7 read at lines 2560–2700 |
| TASK-0052 COMPLETE | **VERIFIED** — board row reads COMPLETE 8/8; MSG-0177 §1 records the Lead's artefact-level verification |
| `MSG-0178` free (the row instructs "MSG-0178 or later") | **VERIFIED** — highest message file on disk is `MSG-0177`; the only occurrence of the string `MSG-0178` in the repository is the instruction in the queue row itself |

**One difference between the task file and the queue row, reported as the row requires.**

| | Task file | Queue row / MSG-0177 |
|---|---|---|
| Status line | **"AUTHORIZED — NOT READY."** | **READY** |

**This is not a conflict.** The task file's own next sentence states the condition: *"It becomes READY
only when the Architecture Lead reconciles it into the queue as the single READY task."* **That
condition is satisfied and was verified, not assumed** — commit `48817ea`, authored by the Architecture
Lead, added the READY row, and **MSG-0177 §3 records the reconciliation and the parser validation**.
**The task file is NOT edited** — it is the Lead's artefact and nothing authorizes this session to
rewrite it. This is the same shape TASK-0052 reported for its own file.

---

### What the two priors actually differ in — read from the committed harnesses, not from the summaries

**This was established before designing anything, because it determines whether the design can answer
the question.** Both `implementation/probes/TASK-0046/probe.mjs` and
`implementation/probes/TASK-0048/probe.mjs` were read.

| | TASK-0046 L4 | TASK-0048 L4 |
|---|---|---|
| predecessor removal | **`DROP TABLE chunk`** | **`DELETE FROM chunk`** |
| predecessor rows / width | 200, **padded 200 chars** | 400, padded 60 |
| successor rows / width | 200, unpadded | 400, padded 60 |
| **measured residue pages** | **10 of 16** (`UNAUTH x15` each) | **1 of 14** |
| page cache during the request | **default** | **`PRAGMA cache_size = -8`** |
| journal verdict basis | **parsed page images only** | **`max(parsed, raw byte scan)`** |
| L4 / **W-B** result | **FINDING, 15 markers** (both modes) | **no finding** |
| L4 / **W-A** result | **no finding** (both modes) | **FINDING** (`wal` 1/11 frames; `delete` ×20) |

**Two things this table establishes that neither prior record states.**

1. **Residue density is not the only variable that differed.** Four others did: the transition kind, the
   page-cache setting, the fixture proportions, and the verdict basis. **A design that varies density
   alone would produce a result that cannot distinguish density from those.**
2. **The divergence runs in BOTH directions.** The record so far describes it as *"TASK-0046 saw the
   append leak; TASK-0048 saw nothing"* — but **the same two runs also disagree in the opposite
   direction on W-A**, where TASK-0048 found what TASK-0046 did not. **That is visible only by reading
   the two outputs side by side, and it is recorded here because it widens what a "reconciliation of
   the two priors" (required outcome 6) has to account for.** **Neither prior record is re-opened,
   weakened or contradicted by this observation** — both stand exactly as taken; this says only that
   the contradiction has a second face.

### Design, stated before it is built so the result can be checked against an intention

**Primary factor — residue density**, at **four levels** (target `0`, `~1`, `~10`, `~40` residue pages).
Three is the floor the task file sets; the fourth is level `0`, which doubles as a specificity control.
The levels span **both prior fixtures** (TASK-0048's `1`, TASK-0046's `10`) as required.
**Density is achieved by a calibration sweep over predecessor size that the harness runs and reports,
and it is MEASURED per cell, never assumed.**

**Required co-factors:** journal mode (`delete`, `wal`) × write shape (`W-A` access accounting, `W-B`
append), exactly as MSG-0158 and MSG-0163 used them.

**Two DECLARED secondary factors, beyond the task file's minimum, and here is why they are in scope
rather than scope creep:** transition kind (`DROP` / `DELETE`) and page cache (default / `-8`). **These
are two of the four uncontrolled differences tabulated above.** Required outcome 6 obliges this run to
*reconcile the two priors*, and **with these held at one arbitrary level the reconciliation could not
distinguish density from them** — the conclusion would rest on an assumption the run was built to
avoid. They are declared here, reported per cell, and **the verdict rule below is stated in advance so
that including them cannot become a way to select a preferred answer afterwards.**

**Cells: 4 densities × 2 journal modes × 2 write shapes × 2 transitions × 2 cache settings = 64**, each
reporting its own measured residue count.

**The verdict rule is PRE-REGISTERED in the harness, before any result is seen**, so CONFIRMED /
REFUTED / UNRESOLVED is decided by a rule rather than by reading the numbers and choosing. Required
outcome 5 forbids rounding an unresolved result to either side, and a rule written after the fact is
exactly how that rounding happens.

**Controls GATE the run — required outcome 4, and MSG-0169 §2 is the standing correction it cites.**
The harness must `fail()` and **abort before printing any verdict** when a control does not fire.
*"A printed line is a claim; an abort is an interlock."* Planned gates: a mechanism control that must
produce a finding; an instrument control on a marker never written; a specificity control (density `0`
must produce no history-sourced finding); an independent-variable control (the achieved densities must
actually be distinct and span the required range, or the experiment measured nothing); a provenance
control (zero live unauthorized rows in every L4 cell); and a parser control (journal images
byte-identical to an independently read copy). **A control's finding is never reported as a finding
about the subject** — MSG-0156.

**Next operation:** commit and push this checkpoint, then write the harness at
`implementation/probes/TASK-0053/probe.mjs`, run it, and capture its output.

**Resumption safe: YES.** Nothing has been built, measured or modified. A resuming session may begin
from the harness-writing step after re-verifying `HEAD` against `48817ea` — **and must NOT assume from
this checkpoint that the harness is unwritten; it must look at
`implementation/probes/TASK-0053/`.**

---

## Checkpoint 2 — measured, recorded, published, and verified from `origin/main`

**Written after the operations were verified, never in anticipation of them.** *(At checkpoint 1 this
heading stood empty for exactly that reason.)*

**Phase:** complete.

### Where checkpoint 1's plan was WRONG, stated before what went right

**The plan said four densities and 64 cells. The run has EIGHT densities and 128 cells, and the reason
is a defect in the plan rather than a change of mind.**

**Checkpoint 1 committed to making "residue population" the controlled variable, calibrated on *pages
carrying the unauthorized marker* — which is what both priors reported and what looked like the obvious
definition. It is not one variable.** A page carrying the predecessor's bytes is either **FREE** (on the
free list, reachable only by an **allocating** write) or **CO-RESIDENT** (partially reused by the
successor, so live authorized rows sit beside dead ones, and reachable only by a write that **touches
those rows**). **The first pass, calibrated on the conflated quantity, put a CO-RESIDENT page in the
band that was supposed to stand in for TASK-0048's regime — so the low end of the ladder did not measure
what it was built to measure, and the run contained no cell at all holding a small number of free
pages.**

**It was caught by a change made for an unrelated reason: exhibiting the findings BY PAGE NUMBER rather
than by count**, on §4.19's standard. **The count could not have shown it.**

**`F1`/`F2` were added to cover the missing regime and `F3`/`F4` to narrow a threshold by measurement
instead of interpolating it. The pre-registered rule was then applied to EVERY cell including them, and
it changed the verdict from `CONFIRMED` to `UNRESOLVED`.** **Excluding them would have preserved a
verdict already seen. That is recorded here, in the probe's own output above the numbers, and in
MSG-0178 §9 — three places, because it is the single fact about this run most likely to be quietly
dropped.**

**Two further deviations from checkpoint 1's plan, both declared:** ten controls rather than the six
planned (NC-4b, NC-6b and NC-6c were added as the apparatus defects below made their absence visible),
and a `wal`-mode threshold measured rather than left as a range.

### Completed operations, each verified by observation

1. **Harness written** at `implementation/probes/TASK-0053/probe.mjs` and **run**; output captured at
   `probe-output.txt`. **132 measurements, exit 0, RUN VALID.**
2. **Two defects in the harness's own journal parser found and fixed BEFORE any result was reported —
   and the second was created by the fix for the first.** An **over-read** of sector slack (page numbers
   `0`, `138499412`, `1213157961` on a zero-residue cell), fixed by validating the engine's own record
   checksum; then an **under-read** of **multi-segment** journals, whose later headers carry their own
   nonce, fixed by walking segments. **Neither moved a finding** — findings are taken on the raw byte
   scan — **but both corrupted the image counts, and NC-6 was blind to both.** **NC-6b and NC-6c exist
   because of them.**
3. **Harness and output committed** at `7521ed8` and pushed.
4. **MSG-0178 written**; **`comms/README.md` row added** for it.
5. **Queue row updated to COMPLETE**; **`implementation/status/current.md` updated**, superseded line
   retained rather than overwritten.
6. **MSG-0172 §3 ruling 2's standing pre-push check RUN** before the queue row was pushed.

**Last verified operation:** the read-back from `origin/main`, quoted below.

### Actual state as observed

```text
node implementation/probes/TASK-0053/probe.mjs
  total measurements        : 132
  control failures recorded : 0
  RUN VALIDITY              : VALID
  VERDICT                   : UNRESOLVED
  exit                      : 0

node implementation/probes/TASK-0050/queue-parse-check.mjs
  rows parsed  : 51
  PROBLEMS     : none
  READY tasks  : (none) -> the Supervisor will NOOP, which is correct

git diff --name-only 48817ea..HEAD -- docs/   -> (empty)
git status --short                            -> (empty)
git push origin main                          -> 48817ea..e1d87dc   (fast-forward)
                                              -> e1d87dc..7521ed8   (fast-forward)
```

**Nothing under `docs/` was touched, and no accepted ADR or specification was modified.** **`EPA-0006`
was NOT modified either** — promotion of this evidence into a section is a separate Lead decision on the
MSG-0153 / TASK-0049 mechanism, and **§4.20 is the last allocated number with §4.21 free** (verified by
enumerating the headings).

**The `origin/main` limb of required outcome 7 carries the standing qualification, unchanged and not
rounded up** — `git fetch` is denied to this runner, `.git/FETCH_HEAD` covers the **start** of the run
only, and movement **during** it is caught by a **fast-forward interlock rather than a claim**.
Full statement in MSG-0178 §10. **No workaround was taken.**

**Git commit / HEAD at completion:** the record commit that follows this checkpoint. **Every push in
this run was fast-forward, without exception**, which is the mechanical confirmation that `origin/main`
did not move during it.

### Resumption

**Resumption safe: YES, and the correct resumption is to do nothing.** **TASK-0053 is COMPLETE and the
queue is CORRECTLY EMPTY — MSG-0177 §4 stated that in advance.** A resuming session must **not** treat
the empty queue as a stall and must **not** manufacture work to fill it. **Re-running the probe is
harmless** (it writes only under the OS temp directory and removes it) **but produces no new evidence,
and its output must not be reported as a new measurement.** **Read MSG-0178 first.**

**Nothing was installed, no host configuration was modified, and the PCI server was not touched.**
**Nothing was selected, adopted, preferred, ranked, deployed, implemented or cleared.** **No gate,
invariant, criterion or verdict moved.** **`py` was not used and no grant for it was sought.**
