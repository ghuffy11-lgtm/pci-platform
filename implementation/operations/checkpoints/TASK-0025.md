# TASK-0025 — Checkpoint

**Task:** Promote ADR-0018 … ADR-0022 into the accepted decision register (`docs/decisions/`)
**Authority:** MSG-0073 (AUTHORIZED), answering MSG-0072; acceptance is MSG-0071
**Specification:** MSG-0073 plus the `TASK-0025` section of `implementation/operations/CLAUDE-TASKS.md`.
There is **no separate `TASK-0025-*.md` specification file** — that is expected, not a missing artifact.

---

## Checkpoint 1 — startup verified, no promotion performed yet

**Checkpoint number:** 1
**Written:** 2026-08-21, after the startup checklist and prerequisite verification completed and
**before** any file was created or modified.
**Phase:** startup complete; promotion not started.

### Session start state — VERIFIED this session

| Item | Observed |
|---|---|
| `git status` | `On branch main` / `Your branch is up to date with 'origin/main'.` / `nothing to commit, working tree clean` |
| `git rev-parse HEAD` | `d79801f8db27d6df2f39044c446c2a11510c48ee` |
| `git rev-parse origin/main` | `d79801f8db27d6df2f39044c446c2a11510c48ee` |
| HEAD subject | `d79801f ops: reconcile TASK-0025 into the queue as the single READY task` |

**Starting HEAD for the mid-run-movement rule is `d79801f`.** Local and `origin/main` agree. This is
re-checked before every commit and push, per the task's stop conditions and CLAUDE.md *Mid-run
repository movement — abort*.

> **One nuance recorded so it is not misread as movement.** `supervisor/state/heartbeat.json` reports
> `"head": "9c533b28…"` — the commit *before* `d79801f`. That is the head the **supervisor** observed
> when it began its 20:47:18Z cycle, which coincided with the previous session pushing `d79801f`. It is
> a supervisor observation, not this session's start state. This session verified `d79801f` directly by
> `git rev-parse` before touching anything, and local still equals `origin/main`. **No abort condition
> is met.**

### Runner lock — checked, not assumed stale

`implementation/operations/supervisor/state/runner.lock` exists and reads:

```json
{ "taskId": "TASK-0025", "pid": 16980, "acquired": "2026-08-21T20:47:18Z", "host": "LENOVO-LA0X1754" }
```

It names **this** task and this run: `heartbeat.json` at 20:47:48Z and 20:48:18Z reads
`RUNNER_RUNNING`, `runnerActive: true`, `runnerPid: 16980`, `readyTask: TASK-0025`. This is the
supervisor-started session the lock was taken for. No second runner is indicated.

### Startup checklist — result

| # | Item | Result |
|---|---|---|
| 1 | `CLAUDE.md` | READ |
| 2 | `AGENTS.md` | READ |
| 3 | `implementation/status/current.md` | READ (status, queue position, open communications) |
| 4 | `implementation/operations/CLAUDE-TASKS.md` | READ — status board and the TASK-0025 section |
| 5 | Active work package | WP-0009 tracked; TASK-0025 is a record-promotion task governed by MSG-0073 |
| 6 | Referenced decisions | MSG-0071, MSG-0072, MSG-0073, MSG-0074 read; ADR-0017 promotion precedent read in both copies |
| 7 | `docs/operations/pci-server-bootstrap.md` | **Not read — not required.** No host operation occurs in this task; every change is a repository document on the Windows workstation checkout |
| 8 | OPEN comms / blockers / discoveries | Checked — see below |
| 9 | `git status`, `git log` | Run; results in the table above |
| 10 | Environment state | Verified directly: lock, heartbeat, directory listings of `docs/decisions/` and `implementation/decisions/` |

### Prerequisites — checked individually, not inherited from the queue

| Prerequisite | Verdict | Evidence |
|---|---|---|
| TASK-0024 COMPLETE | **MET** | Status board row reads COMPLETE, 8/8 criteria, MSG-0070 |
| MSG-0071 DECIDED | **MET** | `MSG-0071-architecture-lead-adr-review.md` reads `**Status:** DECIDED`; accepts ADR-0017…ADR-0022 |
| MSG-0073 AUTHORIZED | **MET** | `MSG-0073-task-0025-adr-promotion-authorization.md` reads `**Status:** AUTHORIZED` |
| TASK-0025 still READY | **MET** | Status board row and the task section both read READY; it is the only READY task |
| Exactly one MSG-0073 file | **MET** | Directory listing shows one `MSG-0073-*.md` and one `MSG-0074-*.md` — no collision this time |
| No colliding TASK-0025 specification | **MET** | No `TASK-0025-*.md` exists in `implementation/operations/`; the queue section is the specification, as MSG-0074 records |
| Five drafts present | **MET** | `implementation/decisions/` holds ADR-0018…ADR-0022, each `**ACCEPTED** by MSG-0071 … awaiting promotion` |
| ADR-0017 already promoted | **MET** | `docs/decisions/ADR-0017-grounded-answer-contract.md` exists (lead's commit `d9c4524`). It is **not** re-promoted, altered, or treated as an error |
| Target numbering free | **MET** | `docs/decisions/` holds ADR-0001…ADR-0017 only. ADR-0018…ADR-0022 do **not** exist there — nothing will be overwritten |
| No OPEN blocker | **MET** | Blocker index and records: BLK-0001…BLK-0007 all RESOLVED/CLOSED |
| OPEN communications | **MET — non-blocking** | Two OPEN: MSG-0060 (collision-handling observation) and MSG-0074 (this task's queue reconciliation). Neither blocks promotion |

### Completed operations

1. Startup checklist (items 1–10 above).
2. Prerequisite verification (table above).
3. Read of the promotion convention from **both** ADR-0017 copies and the queue section.
4. Read of the five draft headers, so the exact header lines to be replaced are known before editing.

### Last verified operation

Draft-header inspection of ADR-0018 … ADR-0022. **No file has been created or modified.**

### Next operation

Promote ADR-0018 by byte-copying the draft to `docs/decisions/` and editing **only** the header, then
repeat for ADR-0019 … ADR-0022, then diff each promoted copy against its draft.

### Method — chosen to make the "no body change" guarantee structural

The promoted copy is produced by **copying the draft file**, then editing only:

1. the `**Status:**` block, and
2. an added `**Accepted by:** Architecture Lead — MSG-0071` line.

The body is never retyped, so it cannot drift. The `diff` in the verification step is then a genuine
check rather than a restatement of intent.

### Resumption safety

**Safe.** Nothing has been changed. A resuming session should re-run the startup checklist, re-verify
HEAD, and **look at `docs/decisions/` before writing** — ADR-0017 is legitimately there and a resumed
session that assumes an empty target could overwrite a promoted record.

---

## Checkpoint 2 — promotion performed and verified

**Checkpoint number:** 2
**Written:** 2026-08-21, **after** the five promotions were made and the per-ADR `diff` verification
was run against the files on disk. Not written in anticipation of anything.
**Phase:** promotion complete and verified; documentation complete; commit and push pending at the time
of writing, and confirmed in the commit that carries this file.

### Completed operations — each verified by observation

| # | Operation | Verified how |
|---|---|---|
| 1 | Copied five drafts into `docs/decisions/` byte for byte | `git status --porcelain` showed five `??` paths under `docs/decisions/` and **no modified path there** |
| 2 | Edited the promoted headers — Status block, added `Accepted by` line | `diff` per pair, §"Verification" below |
| 3 | Edited the five draft Status lines to **RATIFIED**, bodies untouched | Same diffs — the only draft-side hunk is the Status block |
| 4 | Updated `implementation/decisions/README.md` | Five rows now read **promoted** with the accepted path; a promotion section records method and conditions |
| 5 | Updated WP-0009 §6.2, §7, §8 | Additive and declared; no prior text deleted |
| 6 | Wrote **MSG-0075**, the execution record | Number verified free first: directory listing, COMMS register, repo-wide `grep` |
| 7 | Updated the COMMS register, the queue board/section/ledger, and `implementation/status/current.md` | Three views agree: MSG-0074 CLOSED, MSG-0075 RECORD, TASK-0025 COMPLETE, no READY task |
| 8 | Closed **MSG-0074** in its own file, the register, and the ledger | Discharged by execution |

### Verification — the diffs

`diff implementation/decisions/ADR-00NN-<slug>.md docs/decisions/ADR-00NN-<slug>.md`

| ADR | Hunks | Body differences |
|---|---|---|
| ADR-0018 | `3,5c3` (Status), `7a6` (`Accepted by`) | **none** |
| ADR-0019 | `3,5c3`, `8a7` | **none** |
| ADR-0020 | `3,5c3`, `8a7` | **none** |
| ADR-0021 | `3,5c3`, `7a6` | **none** |
| ADR-0022 | `3,5c3`, `7a6` | **none** |

Every hunk is a header hunk. ADR-0019 and ADR-0020 offset by one line because each carries an extra
header line — the MSG-0071 condition attached to its acceptance — which was **carried into the promoted
copy deliberately** as required traceability.

The three MSG-0071 conditions were re-checked **in the promoted copies**, not inherited from MSG-0072's
pass over the drafts: no provider/model/framework/runtime selection (ADR-0022 still cites ADR-0003's
Ollama note and still says *"this ADR does not select it or anything else"*); ADR-0019 still reads
*"not complete for production use, deliberately"*; ADR-0017 — untouched by this task — still leaves the
entailment model and thresholds to SPEC-0020. The literal string `READY` occurs in none of the five.

**No test count.** The task is documentary and cannot have one; none is claimed.

### One correction made before commit, recorded rather than smoothed over

Three of these records were first written saying the **MSG-0044 queue gap did not recur** for
TASK-0025. **That was wrong**, and MSG-0074 §"The pattern did not repeat this time" says so plainly:
the gap *did* recur — `grep -c "TASK-0025"` returned 0 on the queue after MSG-0073 landed, the
**eighth** occurrence — and MSG-0074 repaired it *before* the Supervisor's next cycle. What did not
recur was the **sibling-file collision**. The claim was corrected in MSG-0075 §6.1, the COMMS register,
the queue ledger and narrative, the MSG-0074 closure line, and `current.md` before anything was
committed. Recorded here because "repaired in time" reading as "prevented" is exactly how a recurring
defect stops being counted.

### Last verified operation

The per-ADR diffs and the conditions re-check, run against the files on disk.

### Next operation

Re-check HEAD against the session-start value `d79801f`, commit, push, then report.

### Actual system state observed

- Working tree: five new files under `docs/decisions/`, five modified drafts, and the documentation
  updates listed above. **Nothing under `docs/decisions/` was modified** — only added.
- Supervisor: enabled and cycling; `runner.lock` held by this run (pid 16980, TASK-0025);
  `heartbeat.json` reporting `RUNNER_RUNNING`.
- No host, service, container, database, permission, or schedule was touched. **This task ran entirely
  inside the repository on the Windows workstation checkout.** The `/data` boundary governs the PCI
  server and was not engaged.

### Resumption safety

**Safe, and the work is idempotent only if you look first.** A session resuming after this checkpoint
must check `docs/decisions/` before writing anything: ADR-0017 … ADR-0022 are all legitimately present
now, and re-running the promotion blindly would overwrite accepted records. If the five promoted files
exist and their diffs against the drafts show only header hunks, **the promotion is done** — do not
repeat it.

---

## Checkpoint 3 — committed and pushed; TASK-0025 terminal

**Checkpoint number:** 3
**Written:** after the push succeeded and the post-push state was observed.
**Phase:** COMPLETE.

### Git state — VERIFIED

```text
$ git rev-parse HEAD          (before commit)   d79801f8db27d6df2f39044c446c2a11510c48ee
$ git rev-parse origin/main   (before commit)   d79801f8db27d6df2f39044c446c2a11510c48ee
```

**HEAD had not moved from the session-start value.** The BLK-0006 mid-run movement condition did not
fire, so the commit proceeded.

```text
$ git push origin main
To github-pci:ghuffy11-lgtm/pci-platform.git
   d79801f..144af53  main -> main

$ git rev-parse HEAD          144af535a341d84da7e44f977f259e9249bf9e5a
$ git rev-parse origin/main   144af535a341d84da7e44f977f259e9249bf9e5a
$ git status -sb              ## main...origin/main      (clean)
```

**Commit `144af53`** — 18 files, 1529 insertions, 62 deletions. Five files **added** under
`docs/decisions/`; the five drafts changed by **5 lines each**, which is the three-line RATIFIED status
block replacing the two-line prior one — the arithmetic matches "headers only" and would not if a body
had been touched.

### Post-push reconciliation — the four views agree

| View | Says |
|---|---|
| `CLAUDE-TASKS.md` status board | TASK-0025 **COMPLETE**; the only `READY` on the board is the word in the status-values legend — **no task is READY** |
| `implementation/status/current.md` | TASK-0025 COMPLETE, ADR set promoted, no task READY, next action is the Lead's |
| COMMS register + queue ledger | MSG-0074 **CLOSED**, MSG-0075 **RECORD**; **MSG-0060 is the single OPEN message** |
| Repository HEAD | `144af53`, pushed, working tree clean |

**One grep false positive, recorded so it is not re-investigated.** `grep "Status:.*OPEN"` over
`implementation/comms/` still hits `MSG-0074`. That hit is line 9 — the *retained historical quote* of
the previous status line — not line 3, which reads **CLOSED**. Same shape as the DISC-0006 false
positive TASK-0015 recorded.

### Last verified operation

The push, and the post-push `rev-parse`/`status` above.

### Next operation

**None. TASK-0025 is terminal.** The next task is the Architecture Lead's to authorize — A-SURVEY or
A-STACK from WP-0009 §6.2 — and must be reconciled into the queue as the single READY task in the same
commit as its authorization.

### Resumption safety

**Nothing to resume.** A session arriving here should confirm `docs/decisions/` holds ADR-0001 …
ADR-0022 and that the queue has no READY task, and then stop: there is no authorized work pending.

