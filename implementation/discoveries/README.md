# Implementation Discoveries

Findings surfaced during implementation that do **not** require stopping work, but that the
architecture lead should know about.

Anything that changes or blocks architecture belongs in `../decisions/` or `../blockers/` instead.

Every `DISC-*.md` record in this directory is listed below. The **record is the source of truth**;
this table is an index of it. A record present on disk but absent from this table is a defect in the
record, not a missing discovery.

| ID | Title | Status |
|---|---|---|
| [DISC-0001](DISC-0001-governance-tree-duplication.md) | Duplicated governance documents across `knowledge/` and `docs/` | Recorded — no action taken |
| [DISC-0002](DISC-0002-adapter-test-fidelity.md) | In-memory adapter test fidelity gap | Recorded — mitigated, not eliminated |
| [DISC-0003](DISC-0003-dev-identity-adapter.md) | Development identity adapter boundary | Recorded |
| [DISC-0004](DISC-0004-compose-storage-boundary.md) | Compose stack predates the `/data/docker` boundary | **RESOLVED** 2026-08-19 — answered by the pre-staged `/data/docker/daemon.json` on the authorized host; no compose change strictly required |
| [DISC-0005](DISC-0005-test-glob-false-green.md) | `npm test` reports success while running zero tests under POSIX shells | **CORRECTED** 2026-08-19 — the target-platform claim was **wrong** and is disproven by measurement; the defect is real but confined to Git Bash / MSYS on Windows. Do not act on the original recommendation |
| [DISC-0006](DISC-0006-crlf-defeats-anchored-edits.md) | CRLF line endings silently defeat anchored text edits | **RESOLVED** 2026-08-20 by TASK-0003 — attribute pinned, working-tree residue 150 -> 0 |
| [DISC-0007](DISC-0007-initdb-role-provisioning-defect.md) | Database init refuses to create a passwordless role, then creates one anyway | **RESOLVED** 2026-08-19 by TASK-0004 (MSG-0012) — fix verified against a clean initialisation |
| [DISC-0008](DISC-0008-compose-kernel-service-incomplete.md) | Compose kernel service cannot start as committed | **RESOLVED** 2026-08-19 by TASK-0005 (MSG-0012, option 1) — guard verified intact |
| [DISC-0009](DISC-0009-docker-client-state-outside-data.md) | Docker CLI writes client state to `/home/claude`, outside the `/data` boundary | **CLOSED — ACCEPTED, NOT A VIOLATION** 2026-08-19 by MSG-0020(b) / MSG-0022 / MSG-0023 — account-level tool state is not a PCI project artifact under contract v0.2 |
| [DISC-0010](DISC-0010-work-package-register-disagreement.md) | The two work-package registers disagree about what WP-0001 is; `docs/program/work-packages.md` already lists a WP-0002 | **RESOLVED** 2026-08-21 (TASK-0023, MSG-0066) — the moment it predicted arrived: a second work package was allocated. Reconciled under MSG-0062 §7.1 by option 3 (both registers retained, allocation rule added); **WP-0009** allocated, historical WP-0001 preserved. Previously **RECORDED** 2026-08-21 (TASK-0021, MSG-0055 §7.1) |
| [DISC-0011](DISC-0011-epa-0006-4-11-verdict-count.md) | EPA-0006 §4.11's summary says *"Six designs NOT CLEARED, three DISQUALIFIED"* while its own table lists **seven** NOT CLEARED and three DISQUALIFIED | **RECORDED — not corrected** 2026-08-24 (TASK-0041, MSG-0132). **No verdict is wrong**; the defect is confined to one arithmetic summary of a correct table, and **nothing downstream depends on the tally**. Not fixed because TASK-0041 is additive-only and rewording a prior section needs its own authorization. **Row added in the same commit that raises the record** |
| [DISC-0012](DISC-0012-gq4-differential-ran-against-empty-catalogue.md) | The **G-Q4.2 differential** in TASK-0038 and TASK-0039 varied other subjects' rows in the **kernel**, which routing never opens — their store builder skips foreign partition keys, so **the structure catalogue they routed against held no foreign structure at all** | **RECORDED — no verdict moves** 2026-08-24 (TASK-0042, MSG-0140). **TASK-0039's G-Q4 MET is correctly measured and is NOT withdrawn**; what the discovery establishes is **the bound on what it showed** — a gate that cannot fail has not been passed, it has not been run. **No re-run is needed or proposed: TASK-0042 ran the stronger differential**, with **320** foreign catalogue objects present, and it discriminates (**4 vs 4 reads MET**; **32 vs 416 FAILED** for a mechanism selecting the *same* structures). K7/K8 were NOT CLEARED on E2 and E4, never on G-Q4. **Row added in the same commit that raises the record** |
| [DISC-0013](DISC-0013-queue-self-dependency-defeats-supervisor.md) | A **markdown link to a task’s own definition file** placed in the queue’s **dependency cell** makes the task depend on **itself**. The Supervisor extracts dependencies by regex — `TASK-\d{4}` — and **the regex does not know a filename from a dependency**, so a READY task whose own ID appears there is a **contradictory queue**, and a contradictory queue is a **fail-closed no-op by design** | **RECORDED 2026-08-26 (Architecture Lead) — the immediate instance FIXED; the class is not.** **Reproduced by replicating the parser before and after**, not inferred from the symptom: before, `deps = [TASK-0043, TASK-0049, TASK-0050]` → *"TASK-0050 is READY but dependency TASK-0050 is READY"* → **NOOP**; after, `deps = [TASK-0049]` → `PROBLEMS: none` → **starts the runner.** **The Supervisor is not defective and was not changed** — *"every uncertainty is a no-op"* is its stated design principle. **The part that matters more than the fix: TASK-0048 and TASK-0049 carry the SAME defect in their committed rows and both nevertheless executed** — the only reading consistent with both facts is that **the operator started them by hand, which masked the defect for two consecutive tasks.** Their rows are **deliberately left as they are**: both are COMPLETE, only READY tasks are validated, and rewriting a completed task’s committed row to tidy a defect would damage the record for no operational gain. **The failure mode is indistinguishable from "no work to do"** — fourth distinct failure of the queue-row mechanism, and **input to Q17, not an answer to it**. **MSG-0172 §3 acted on it by promoting the parser replicator to a STANDING PRE-PUSH CHECK** |
| [DISC-0014](DISC-0014-subjects-enumerated-to-different-standards.md) | The two E4 test subjects were **enumerated to different standards** and compared as though they were not. `DatabaseSync.setAuthorizer` is present on subject 1 and **neither §4.12 gap 1 nor §4.14 finding 8 reported it** — and §4.14 finding 8 ran on the **same** runtime (SQLite 3.51.3, Node v24.15.0), so **this is not a version change**. §4.12 checked four C-API names and asked for *"trace, profile or log"* members; §4.15 enumerated 42 names on subject 2 and **exercised** its authorizer | **RECORDED — no verdict moves** 2026-08-26 (TASK-0050, MSG-0168 §9). **§4.12's sentence is not false** — an authorizer is not a trace, profile or log — **the defect is in the comparison, not the sentence.** TASK-0050 applied §4.15's wider standard to subject 1, with a control stronger than a silence test (a **denying** authorizer must make a prepare fail, and it did): **NON-ADVERSE, prepare-time, invariant with `N` (3 events at 200 / 1000 / 5000 rows), 0 events on re-execution** — so it **does not answer E4**, which is **precisely §4.15's own classification** of the same instrument on subject 2. **The widened enumeration STRENGTHENS §4.12 and §4.14 rather than overturning them; E4 remains NOT OBTAINABLE on subject 1.** **No section is amended.** The gap it names has no F15 equivalent: the control distinguishes *"reported nothing"* from *"was never running"*, and has nothing to say about *"was never asked about"*. **Row added in the same commit that raises the record** |
| [DISC-0015](DISC-0015-status-cell-word-match-starts-a-runner-and-the-replicator-is-not-faithful.md) | **"AUTHORIZED — NOT READY" in the status cell parses as READY.** The Supervisor matches the cell **word by word** against a fixed list — `\bREADY\b` matches inside *"NOT READY"* — and **there is no negation handling of any kind**. Separately, the **standing pre-push check is NOT a faithful replica** of the Supervisor it is trusted to stand in for | **RECORDED 2026-08-26 (Architecture Lead) — the immediate instance FIXED; the three fidelity gaps deliberately NOT fixed.** **The mirror image of DISC-0013, and the more dangerous of the two**: DISC-0013 made the Supervisor do nothing when it should have acted; **this would have STARTED a runner against a prerequisite known to be unmet.** **Caught by the standing pre-push check itself** (MSG-0172 §3), exactly as intended — **on a row deliberately written NOT to be READY**, which is why the rule is now *run the check before pushing ANY board change*. **Fixed by using `WAITING_FOR_OPERATOR`**, a status word both parsers know and **long enough that longest-match protects it**; the explanatory prose was moved **out of the status cell**, prose in a status cell being the cause. **SECOND FINDING, VERIFIED by reading both sources: the replicator and `supervisor.ps1` disagree in three respects** — their status lists differ in **four entries** (the replicator has `PROPOSED`/`SUPERSEDED`, which the Supervisor would call **unrecognised → contradictory queue → silent NOOP**, and carries `WAITING_FOR_ARCHITECTURE` **truncated**); the replicator **implements no unrecognised-status check at all**, so it *cannot report that failure even in principle*; and it omits the `IN_PROGRESS` validation. **A fourth difference is labelled honestly rather than claimed**: the JS regex is case-sensitive (**VERIFIED by reading**) while PowerShell `-match` is case-insensitive (**INFERRED from documented semantics, not measured on this host** — one line on the Windows machine settles it). **Checked rather than assumed: none of the three is live today** — every board status word was enumerated, none reads `PROPOSED`/`SUPERSEDED`, and the one `ABORTED` row is recognised by the Supervisor. **Latent hazards, not an active stall.** **Not fixed because re-cutting a standing control the Lead relies on for verification is the architecture lead’s call** — and **a check believed faithful, and not, is worse than no check**, since MSG-0172 §3 made a pre-push pass count as evidence. **Fifth distinct failure of the queue-row mechanism; input to Q17** |

## Reconciled 2026-08-20 — TASK-0015

This table listed **three** rows while **nine** `DISC-*.md` records existed. DISC-0004 through
DISC-0009 were absent, so a reader of the index alone would have missed every discovery raised during
host verification — including two that were defects in the deployment artifacts (DISC-0007,
DISC-0008) and one that touches a hard boundary rule (DISC-0009).

Authorized by MSG-0039 (a)/(b). Every status above is transcribed from the record's own header line;
**no discovery record was altered, deleted, or renumbered.** The three pre-existing rows already
agreed with their records and were preserved — DISC-0002's wording was aligned to the record's own
phrasing, which changes no status.

Nothing was found that required architectural judgment: all nine records carry an unambiguous status,
and no index row existed without a corresponding record.

Evidence: [`../comms/MSG-0040-task-0015-execution-record.md`](../comms/MSG-0040-task-0015-execution-record.md),
[`../operations/checkpoints/TASK-0015.md`](../operations/checkpoints/TASK-0015.md).

> This is the **third index in a row** to be found drifting from its own records — after the blocker
> index (BLK-0001 / BLK-0004 shown OPEN when resolved, TASK-0013) and the same index again (BLK-0005
> listed nowhere, TASK-0014). The pattern is consistent: a record is created or closed in its own
> file, and the table that indexes it is not updated in the same commit.
