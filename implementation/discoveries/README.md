# Implementation Discoveries

Findings surfaced during implementation that do **not** require stopping work, but that the
architecture lead should know about.

Anything that changes or blocks architecture belongs in `../decisions/` or `../blockers/` instead.

| ID | Title | Status |
|---|---|---|
| DISC-0001 | Duplicated governance documents across `knowledge/` and `docs/` | Recorded |
| DISC-0002 | In-memory adapter test fidelity gap | Mitigated, not eliminated |
| DISC-0003 | Development identity adapter boundary | Recorded |
