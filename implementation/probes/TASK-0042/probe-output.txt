============================================================================================================
TASK-0042 — ARCHITECTURE-BOUND RETRIEVAL EVIDENCE
             routing · placements · transitions · I5/I7/I8 · the E4 re-check
============================================================================================================
Authority : MSG-0137 (AUTHORIZED) with MSG-0134 (Q1=A strict), MSG-0135 (Q2=B),
            MSG-0136 (Q7=A, zero stale-answer tolerance) and MSG-0133 (Q13) BINDING.
            Queue TASK-0042 · reconciliation MSG-0139.
Subject   : SQLite 3.51.3 via node:sqlite — EPA-0006 class R TEST SUBJECT, not a selection
Runtime   : Node v24.15.0

SELECTS NOTHING · ADOPTS NOTHING · INSTALLS NOTHING · DEPLOYS NOTHING · AMENDS NO ADR
No gate is relaxed. No numeric staleness threshold is proposed. No benchmark, latency,
capacity, recall or throughput figure is produced. No wall clock is read.

READ THIS BEFORE ANY TABLE BELOW:
  * E4 is re-checked in section 2 and is expected to be UNOBTAINABLE again (§4.13 GAP-B).
    NOTHING IN THIS RUN CAN THEREFORE BE CLEARED, whatever any count shows.
  * U1 = 0 IS NEVER CLAIMED. Where an index-entry count appears it is a LOWER BOUND, and a
    zero in the U1lb column means "this bound proves nothing at this size".
  * NO PRIOR CASE IS RE-RUN. TASK-0033/0035/0037/0038/0039 evidence stands as measured.
    The validity gates ARE re-run because §4.6 S8 requires them in every run.
  * Q13 bounds this to the CURRENT/"now" frame. No historical or future frame is answered.

============================================================================================================
SECTION 1 — VALIDITY GATE 1: the adversarial precondition (EPA-0006 §4.6 S8)
============================================================================================================
The UNCONSTRAINED top-k must contain NO authorized chunk. If one appears the fixture is not
adversarial, every measurement below is meaningless, and the run is VOID.

  skew=uniform        M=    50  unconstrained top-6 -> authorized-among-them=0  -> ADVERSARIAL, as required
  skew=uniform        M=   500  unconstrained top-6 -> authorized-among-them=0  -> ADVERSARIAL, as required
  skew=uniform        M=  5000  unconstrained top-6 -> authorized-among-them=0  -> ADVERSARIAL, as required
  skew=bounded-heavy  M=    50  unconstrained top-6 -> authorized-among-them=0  -> ADVERSARIAL, as required
  skew=bounded-heavy  M=   500  unconstrained top-6 -> authorized-among-them=0  -> ADVERSARIAL, as required
  skew=bounded-heavy  M=  5000  unconstrained top-6 -> authorized-among-them=0  -> ADVERSARIAL, as required

  THE ADVERSARIAL PRECONDITION HELD AT EVERY SIZE AND BOTH DISTRIBUTIONS.

============================================================================================================
SECTION 2 — ITEM 6: THE E4 RE-CHECK. Enumerated again, not carried forward.
============================================================================================================
§4.12 gap 1 established E4 UNOBTAINABLE on this test subject by enumeration against a control.
§4.13 GAP-B carries that as the constraint that blocks clearance independently of topology.
MSG-0137 item 6 requires a RE-CHECK on the reachable subject, and forbids inferring anything
from an absent log. A SECOND NEGATIVE IS THE EXPECTED RESULT, NOT A FAILURE.

This is a re-check, not a re-run of a prior CASE: it asks whether the observability position
has changed on the runtime as it stands NOW. The runtime is recorded above so a later reader
can tell whether a different answer would mean a changed engine or a changed probe.

2.1 — The reachable API surface, enumerated in THIS run.

  DatabaseSync.prototype : aggregate, applyChangeset, close, constructor, createSession, createTagStore, enableDefensive, enableLoadExtension, exec, function, loadExtension, location, open, prepare, setAuthorizer
  StatementSync.prototype: all, columns, constructor, get, iterate, run, setAllowBareNamedParameters, setAllowUnknownNamedParameters, setReadBigInts, setReturnArrays

  Searched for a trace/profile/log hook: trace, profile, log, sqllog, stmtStatus, stmt_status, scanstatus, onTrace, setTrace, setProfile, errorLog, setLogger, traceV2
  FOUND: NONE — no member of either prototype is a log, trace or profile hook.

2.2 — Compile options, read from the engine in THIS run.

  DEBUG                      ABSENT    would enable PRAGMA vdbe_trace / vdbe_listing / parser_trace
  ENABLE_SQLLOG              ABSENT    would write an SQL log via SQLITE_CONFIG_SQLLOG
  ENABLE_STMT_SCANSTATUS     ABSENT    would expose PER-LOOP VISIT COUNTS — the ONE API that measures U1 directly
  ENABLE_STAT4               ABSENT    would make the planner sensitive to BOUND PARAMETER VALUES
  ENABLE_DBSTAT_VTAB         PRESENT   exposes the dbstat virtual table — per-b-tree page and cell counts

  Full option list: 49 options; the five above were checked by exact match.

2.3 — The tracing pragmas, attempted, WITH THE NONEXISTENT-PRAGMA CONTROL.

  SQLite SILENTLY IGNORES an unrecognised pragma. "PRAGMA vdbe_trace=on returned no error" is
  therefore evidence of NOTHING. The control is a pragma that certainly does not exist. If the
  tracing pragmas behave identically to it, they are equally inert. WITHOUT THIS CONTROL,
  "the instrument reported nothing" and "the instrument was never running" are the same
  observation (§4.6 S7.3).

  PRAGMA vdbe_trace                             set-> []     read-back-> []    
  PRAGMA vdbe_listing                           set-> []     read-back-> []    
  PRAGMA parser_trace                           set-> []     read-back-> []    
  PRAGMA sql_trace                              set-> []     read-back-> []    
  PRAGMA stmt_scanstatus                        set-> []     read-back-> []    
  PRAGMA this_pragma_certainly_does_not_exist   set-> []     read-back-> []       <- THE CONTROL

  RESULT: every tracing pragma behaves EXACTLY as the pragma that does not exist.
  None was enabled.

2.4 — Is there a log FILE to inspect?

  db.location() for the ':memory:' database -> null
  No database file, therefore no journal, no WAL and no engine-written file of any kind to
  inspect. Nothing was installed and no file was created.

  >> E4 RE-CHECK VERDICT: NOT OBTAINABLE — the position is UNCHANGED from §4.12 gap 1.

     The enumerated reasons, each observed in this run: node:sqlite binds no trace, profile or
     log API; the engine is built without DEBUG, ENABLE_SQLLOG and ENABLE_STMT_SCANSTATUS;
     every tracing pragma is inert and demonstrated inert against a control; and there is no
     database file and so no engine-written file to read.

     Per §4.6 S6 an absent evidence class yields NOT CLEARED. Per MSG-0137 it may NOT be
     inferred. THEREFORE NOTHING IN THIS RUN CAN BE CLEARED ON E4 ALONE, independently of
     every measurement below. The rest of the probe still matters, because it decides whether
     E4 is the ONLY thing missing — and sections 3 to 6 show it is not.

============================================================================================================
SECTION 3 — ITEM 1: THE ROUTING PHASE AND THE REACHABLE PHYSICAL STRUCTURES
============================================================================================================
Q2 = B (MSG-0135): query-time predicates alone are INSUFFICIENT where the engine examines
unauthorized candidates first, so ROUTING AND PHYSICAL STRUCTURE ARE PART OF WHAT MUST BE
MEASURED. Q1 = A (MSG-0134): reading an unauthorized index entry, key or METADATA is
examination. G-Q4.4: routing-phase units are counted in U.

THE GAP IN PRIOR EVIDENCE THIS SECTION CLOSES, STATED BEFORE THE NUMBERS.
  TASK-0038 and TASK-0039 ran the G-Q4.2 differential with other subjects added to the KERNEL,
  but their store builder skips any partition key that is not the subject's — "other subjects'
  partitions are not materialised here". SO THE CATALOGUE THEY ROUTED AGAINST CONTAINED NO
  OTHER SUBJECT'S STRUCTURE AT ALL. A differential that varies rows in a table nobody routes
  over does not test G-Q4.2. This probe materialises them PHYSICALLY. That is not a re-run of
  the prior differential; it is the first run of one that can fail.

3.1 — The catalogue actually contains other subjects' structures now. Counted, not asserted.

  others    catalogue total  own partition  foreign partition  other (kernel etc.)
  0                      32             20                  0                   12
  16                    128             20                 80                   28
  64                    416             20                320                   76

  -> the catalogue holds 320 of ANOTHER SUBJECT'S structures at others=64, and 0 at others=0,
     while the subject's own structure count is unchanged at 20.
     The differential below therefore has something to be wrong about.

  The "other" column is kernel tables and kernel indexes. They are NOT counted as foreign:
  whether reading a catalogue entry that names a KERNEL structure is examination is the §4.9
  G-Q4 unnumbered open interaction, which MSG-0134 does not settle. This probe counts them
  separately and DECIDES NOTHING about them.

3.2 — Two routing MECHANISMS, behaviourally identical. §4.9 G-Q4's design note, measured.

  R-COMPUTED  : names derived from the subject's entitlements alone, resolved by exact key.
  R-CATALOGUE : the ROUTING NEGATIVE CONTROL — scans the structure catalogue for names that
                look applicable. It selects THE SAME STRUCTURES. Only the mechanism differs.

  mech         others   routed  cat.rows  U(route)  other-cat  outcome
  R-COMPUTED   0             4         0         0          0  no other subject's catalogue entry read
  R-COMPUTED   64            4         0         0          0  no other subject's catalogue entry read
  R-CATALOGUE  0             4        32         0         12  no other subject's catalogue entry read
  R-CATALOGUE  64            4       416       320         76  READ 320 CATALOGUE ENTRIES NAMING ANOTHER SUBJECT'S STRUCTURES

  BEHAVIOURALLY IDENTICAL: the two mechanisms selected THE SAME structures (4 vs 4).
  So no functional test could tell them apart — which is exactly why G-Q4.3 demands plan or trace evidence.

  G-Q4.2 — THE DIFFERENTIAL. Collections differing ONLY in other subjects' partitions:
    R-COMPUTED  routed set IDENTICAL, routing reads 4 vs 4 -> G-Q4.2 MET
    R-CATALOGUE routed set IDENTICAL, routing reads 32 vs 416 -> G-Q4.2 FAILED

    G-Q4.2 requires that BOTH the routed set AND the routing-phase read count be unchanged.
    R-CATALOGUE returns the same SET and a DIFFERENT COUNT. A test checking only the set would
    have passed it. THAT IS WHY THE GATE NAMES BOTH.

3.3 — G-Q4.3: catalogue enumeration, evidenced from the PLAN, not from the mechanism's name.

    R-CATALOGUE routing statement plan (EXPLAIN QUERY PLAN, engine-reported):
      SCAN sqlite_schema
    -> A SCAN OVER THE STRUCTURE CATALOGUE. G-Q4.3 FAILED — disqualifying on the same reasoning §4.6 S6/E1 applies to a data scan.

    R-COMPUTED issues NO catalogue statement at all, so there is no routing plan to show.
    THAT ABSENCE IS THE EVIDENCE, and it is a weaker kind than a plan: it shows this probe
    issued no catalogue query, NOT that the engine performed no internal schema resolution.

3.4 — WHAT THE ROUTING INSTRUMENT CANNOT SEE. Recorded as a boundary, not inferred around.

  SQLite resolves a table NAME to a schema object through an in-memory hash built when the
  schema is first loaded. That resolution is NOT reachable by any instrument this runner has:
  the authorizer reports SQLITE_READ for statements that read sqlite_schema EXPLICITLY, and
  reports nothing for implicit name resolution. The check below establishes that by taking the
  placement rather than by asserting it.

    authorizer SQLITE_READ targets: 8 distinct
    among them sqlite_schema / sqlite_master / sqlite_stat*: NONE

    READ THIS CORRECTLY. "NONE" means THE STATEMENT DOES NOT READ THE CATALOGUE AS DATA.
    It does NOT mean the engine performed no schema lookup — the authorizer cannot see
    implicit name resolution, and no reachable instrument on this subject can.

    >> ROUTING-PHASE OBSERVABILITY: PARTIAL. An EXPLICIT catalogue read is measurable and is
       measured above. IMPLICIT SCHEMA RESOLUTION IS NEVER MEASURED on this test subject,
       with the exact limitation: node:sqlite exposes no hook below statement compilation,
       and SQLITE_ENABLE_STMT_SCANSTATUS — the one API that would report per-loop visits —
       is ABSENT from this build (section 2.2). Under §4.6 S9 an unmeasurable stage is NOT
       CLEARED, never a pass by default.

       This is also the §4.9 G-Q4 OPEN INTERACTION in its measurable form: whether an
       exact-key catalogue lookup of an ALREADY-COMPUTED structure name is itself
       examination. MSG-0134 rules on entries describing CHUNKS; MSG-0109 §9 Q4 records the
       STRUCTURE-identifier variant as "related but distinct". THIS PROBE DECIDES NOTHING
       ABOUT IT — it records that the quantity the question asks about IS NOT OBSERVABLE
       HERE, which is a fact the ruling will need either way.

============================================================================================================
SECTION 4 — ITEM 2: PLACEMENT ENUMERATION, CALIBRATION, AND THE MAXIMUM U (§4.6 S7.1–S7.4)
============================================================================================================
S7-R1: every reachable index-cursor placement must be EXERCISED, in addition to the others.
S7-R2: the reported U is the MAXIMUM across exercised applicable placements, a LOWER BOUND.
S7-R3: a row-access-only U = 0 is INSUFFICIENT for E2 where a reachable index-cursor
       placement exists and was not exercised. Nothing discharges R3 except exercising it.

Reachability is established BY TAKING THE PLACEMENT (S7.3), never by reading documentation.

4.0 — ONE MORE PLACEMENT THE COMPILE OPTIONS SAY EXISTS. Taken, not dismissed.

  Section 2.2 reports SQLITE_ENABLE_DBSTAT_VTAB as PRESENT — the one relevant option that is.
  §4.6 S7.3 requires the reachable-but-unexercised set to be EMPTY, so it is taken here rather
  than argued away. What it actually reports decides whether it is a U1 instrument.

    dbstat over index i_t_b -> 1 page row(s), 200 cells total (200 rows inserted)

    IT IS REACHABLE, AND IT IS NOT A U1 INSTRUMENT. dbstat reports the STORED LAYOUT of a
    b-tree — pages and cells that EXIST — and reports it identically whether a query ran or
    not. It cannot say how many entries a TRAVERSAL VISITED, which is the quantity U1 is
    defined on (§4.6 S4 U1: "reads an index entry or key DURING TRAVERSAL").

    So the reachable-but-unexercised set stays EMPTY for the right reason: the placement was
    TAKEN and found to measure a different quantity. That is the §4.6 S7.3 standard —
    enumeration, never assertion — and it is the opposite of arguing from documentation.

4.1 — CALIBRATION against a cohort known by construction. An instrument that cannot reproduce
      a constructed count is not trustworthy and nothing may rest on it.

  Constructed cohort: expired=300 (eff_to closed), notYetBounded=400 (eff_from future), authorized=2; 2 chunks per version.

  PLANNER CHOICE    plan: SEARCH cv USING INDEX i_cal_vo (open_ended=? AND eff_from<?) | SEARCH cc USING COVERING INDEX i_cal_cv (version_id=?)
      P-VIDX index-cursor calls =   302  expected by construction =   302  EXACT — trustworthy
      P-ROW  row-access calls   =   302
      P-CIDX chunk-side calls   =     4  rows returned = 4

  PINNED  i_cal_vb  plan: SEARCH cv USING INDEX i_cal_vb (open_ended=? AND eff_to>?) | SEARCH cc USING COVERING INDEX i_cal_cv (version_id=?)
      P-VIDX index-cursor calls =   402  expected by construction =   402  EXACT — trustworthy
      P-ROW  row-access calls   =     2
      P-CIDX chunk-side calls   =     4  rows returned = 4

  CALIBRATION PASSED on both plans for P-VIDX.

  WHAT CALIBRATION SAYS ABOUT P-CIDX — the placement no prior probe took.
    pinned : P-CIDX=4  rows returned=4  survivors row-accessed=2
    planner: P-CIDX=4  rows returned=4  survivors row-accessed=302

    P-CIDX fires ONCE PER SURVIVING PAIR. On this join shape the chunk cursor is only
    entered for versions that already passed the version-side residual, so this placement
    DOES NOT SEE the entries the version-side residual rejected. IT IS REPORTED AS
    EXERCISED AND AS FINDING NOTHING ADDITIONAL — which is a result about the placement,
    not a licence to treat any zero as U1 = 0.

    Either way S7-R1 is discharged for this placement BY EXERCISING IT. S7.3 is explicit that
    naming or describing a placement is not exercising it.

4.2 — THE GRID. Every placement exercised at every size, on each pattern, with the
      uninstrumented plan captured as the transfer control.

  Columns:  Urow  = unauthorized units at the ROW-ACCESS placement (TASK-0038's placement)
            Nvidx = engine-measured VERSION index entries visited (all entries; the
                    instrument cannot classify them)
            U1lb  = DERIVED conservative lower bound on unauthorized entries visited.
                    A ZERO MEANS "THIS BOUND PROVES NOTHING AT THIS SIZE". NOT U1 = 0.
            Ncidx = chunk-side index-cursor calls (see 4.1 for what it observes)
            Urank = unauthorized units reaching the ranking function (§4.6 S4 U3)
            Uroute= unauthorized catalogue entries read while routing (G-Q4.4)
            Umax  = MAXIMUM across the exercised placements — the candidate's reported U (S7-R2)
            ans   = authorized chunks answered (a non-withholding anchor — U = 0 is
                    purchasable by withholding authorized content, the K4 trap)

------------------------------------------------------------------------------------------------------------
CANDIDATE K7 — I1+I2+I3 partitioned store, planner chooses the version index (TASK-0038/0039's K7)
------------------------------------------------------------------------------------------------------------
       M   Urow   Nvidx    U1lb   Ncidx   Urank  Uroute    Umax  ans  version traversal
      50      7      10       2       6       0       0       7    4  8x SEEK vo (lower bound)
     500     71      74      66       6       0       0      71    4  8x SEEK vo (lower bound)
    5000    714     717     709       6       0       0     714    4  8x SEEK vo (lower bound)

------------------------------------------------------------------------------------------------------------
CANDIDATE K8 — K7 with the bounded limb PINNED to i_<part>_vb (TASK-0038/0039's K8)
------------------------------------------------------------------------------------------------------------
       M   Urow   Nvidx    U1lb   Ncidx   Urank  Uroute    Umax  ans  version traversal
      50      0      10       2       6       0       0       2    4  4x SEEK vb (upper bound) + 4x SEEK vo (lower bound)
     500      0      74      66       6       0       0      66    4  4x SEEK vb (upper bound) + 4x SEEK vo (lower bound)
    5000      0     717     709       6       0       0     709    4  4x SEEK vb (upper bound) + 4x SEEK vo (lower bound)

------------------------------------------------------------------------------------------------------------
CANDIDATE I5 — I5 per-principal materialisation — §4.8 records this NOT MEASURED
------------------------------------------------------------------------------------------------------------
       M   Urow   Nvidx    U1lb   Ncidx   Urank  Uroute    Umax  ans  version traversal
      50      7       9       1       4       0       0       7    4  2x SEEK vo (lower bound)
     500     71      73      65       4       0       0      71    4  2x SEEK vo (lower bound)
    5000    714     716     708       4       0       0     714    4  2x SEEK vo (lower bound)

------------------------------------------------------------------------------------------------------------
CANDIDATE I8 — I8 entitlement-class materialisation — §4.13 records this NEVER MEASURED
------------------------------------------------------------------------------------------------------------
       M   Urow   Nvidx    U1lb   Ncidx   Urank  Uroute    Umax  ans  version traversal
      50      7       9       1       4       0       0       7    4  2x SEEK vo (lower bound)
     500     71      73      65       4       0       0      71    4  2x SEEK vo (lower bound)
    5000    714     716     708       4       0       0     714    4  2x SEEK vo (lower bound)

------------------------------------------------------------------------------------------------------------
CANDIDATE I7 — I7 boundary-refined effectivity — §4.13 records this NEVER MEASURED
------------------------------------------------------------------------------------------------------------
       M   Urow   Nvidx    U1lb   Ncidx   Urank  Uroute    Umax  ans  version traversal
      50      0       2       0       4       0       0       0    4  2x SEEK vo (lower bound)
     500      0       2       0       4       0       0       0    4  2x SEEK vo (lower bound)
    5000      0       2       0       4       0       0       0    4  2x SEEK vo (lower bound)

------------------------------------------------------------------------------------------------------------
CANDIDATE KR — ROUTING NEGATIVE CONTROL — K7 structures, discovered by catalogue scan
------------------------------------------------------------------------------------------------------------
       M   Urow   Nvidx    U1lb   Ncidx   Urank  Uroute    Umax  ans  version traversal
      50      7      10       2       6       0     320     320    4  8x SEEK vo (lower bound)
     500     71      74      66       6       0     320     320    4  8x SEEK vo (lower bound)
    5000    714     717     709       6       0     320     714    4  8x SEEK vo (lower bound)

  Plan-transfer control: 0 of 54 instrumented measurements had a
  seek signature differing from the uninstrumented plan.
  The instruments did not move the planner, so the counts transfer.

============================================================================================================
SECTION 5 — ITEM 5: I5, I7 AND I8. Measured where genuinely observable; otherwise NEVER
            MEASURED with the exact limitation. There is no third option (MSG-0137 item 5).
============================================================================================================
§4.8 records I5 as "not measured". §4.13 records I7 and I8 as "Structural proposal. NEVER
MEASURED". All three are BUILT AND RUN above, on the same fixture, predicate, placements and
collection sizes as K7/K8 — so the numbers are comparable and no new instrument is introduced.

WHAT IS NOW MEASURED FOR EACH, AND WHAT REMAINS NEVER MEASURED:

  pattern   routed  Umax@50  Umax@500  Umax@5000  growth with N
  K7             4        7        71        714  GROWS — decisive evidence of failure (§4.6 S3)
  K8             4        2        66        709  GROWS — decisive evidence of failure (§4.6 S3)
  I5             1        7        71        714  GROWS — decisive evidence of failure (§4.6 S3)
  I8             1        7        71        714  GROWS — decisive evidence of failure (§4.6 S3)
  I7             1        0         0          0  does not grow at these sizes   [U1lb VACUOUS in 3/3 cells]

  "VACUOUS" means the derived bound returned zero while the version cursor DID visit entries.
  A vacuous bound proves NOTHING at that size — it is not a measurement of U1 = 0, and §4.12
  says so in terms. A row with a vacuous bound is not a row with a clean one.

5.1 — I7's interval, and the two invalidating events §4.13 names.

    next effectivity boundary after the query instant: 1700015000
    interval length in fixture units: 15000

    The boundary is COMPUTED FROM KERNEL DATA — eff_from and eff_to and nothing else. That is
    §4.13's load-bearing claim and it is reproduced here rather than restated.

    EVENT 1 — the clock crosses the boundary and the structure is NOT re-refined:
      rows returned by the structure = 4; after the ADR-0020 §3 point-2
      kernel re-check, kept = 4, LEAKED = 0
      authorized chunks in the kernel at that instant = 146; WITHHELD = 142
      -> the KERNEL RE-CHECK caught the leak. The structure alone did not: G-Q5 condition 2 is doing
         the work, which is why §4.9 G-Q5 requires BOTH conditions and neither substitutes.
      -> AND IT WITHHELD 142 AUTHORIZED CHUNKS. That is the OTHER failure direction, and
         §3.3 wrong-exclusive and the K4 trap both say it cannot be traded for a clean U.

      READ THIS AS A RESULT ABOUT I7, NOT ABOUT THE RE-CHECK. The boundary is precisely where
      I7's refinement expires. §4.13 predicted the interval "is invalidated by the event of
      reaching it"; what is measured here is what happens WHEN THAT INVALIDATION IS NOT WIRED,
      and it is a real failure, not a hypothetical one.

    EVENT 2 — a version is INGESTED between t and the next boundary. No boundary is crossed.
      authorized versions in the kernel at this instant: 3
      the ingested version appears in the structure's answer: NO
      -> THE STRUCTURE WITHHELD AN AUTHORIZED VERSION. §4.13 names this: "ingestion is itself an
         invalidating event under N3, and it is the one a boundary-driven design omits most
         naturally." IT IS MEASURED HERE, NOT PREDICTED. And it is a WITHHOLDING failure, which
         §3.3 wrong-exclusive and the K4 trap both say cannot be traded for a clean U.

5.2 — What remains NEVER MEASURED for I5, I7 and I8, with the exact limitation.

  pattern  quantity                                                       status
  I5       structure COUNT and storage replication factor at production scale NEVER MEASURED
  I5       E4 — engine log inspection                                     NEVER MEASURED
  I5       E3 — opaque-stage confinement for a lexical or vector stage    NEVER MEASURED
  I8       entitlement-class COUNT, combinatorial in the worst case       NEVER MEASURED
  I8       the cost of a subject MOVING between classes                   NEVER MEASURED
  I7       re-refinement RATE as a function of corpus boundary density    NEVER MEASURED
  I7       G-Q5.1c abstention-on-breach as a DESIGNED mechanism           NEVER MEASURED
  ALL      implicit schema resolution during routing                      NEVER MEASURED
  ALL      interior b-tree pages, pager reads, and any loop the index-cursor instrument does not sit in NEVER MEASURED

  The exact limitation for each:
    I5 · structure COUNT and storage replication factor at production scale
        corpus scale is UNMEASURED at n=1 (§11 #1). No principal population exists to count against.
    I5 · E4 — engine log inspection
        UNOBTAINABLE on this test subject; section 2 re-check. Not inferable (§4.6 S9).
    I5 · E3 — opaque-stage confinement for a lexical or vector stage
        no lexical or vector stage is built here; FTS5 MATCH internals were NOT MEASURED by MSG-0104 §5.3 and remain so.
    I8 · entitlement-class COUNT, combinatorial in the worst case
        no real entitlement population exists in the fixture; a synthetic count would be an invented figure (§4.6 S11).
    I8 · the cost of a subject MOVING between classes
        requires a migration mechanism this probe does not build and MSG-0137 does not authorize.
    I7 · re-refinement RATE as a function of corpus boundary density
        §4.13: "a function of the corpus's boundary density, which is UNKNOWN" — corpus unmeasured at n=1.
    I7 · G-Q5.1c abstention-on-breach as a DESIGNED mechanism
        this probe measures what the structure returns at and after the boundary; it builds no abstention controller, so the mechanism limb is untested.
    ALL · implicit schema resolution during routing
        no instrument below statement compilation is reachable; ENABLE_STMT_SCANSTATUS ABSENT (section 2.2, section 3.4).
    ALL · interior b-tree pages, pager reads, and any loop the index-cursor instrument does not sit in
        the instrument is a LOWER BOUND by construction (§4.6 S5). No reachable API reports them.

  Under §4.6 S9 every one of these yields NOT CLEARED for the pattern that carries it. There is
  no third option and none is taken.

============================================================================================================
SECTION 6 — ITEM 4: ZERO STALE-ANSWER TOLERANCE ACROSS THE FOUR TRANSITIONS (Q7 = A)
============================================================================================================
MSG-0136: after an update, approval, revocation or supersession THE PRIOR VERSION MUST NOT
ANSWER; where the current approved version cannot be established the system ABSTAINS. NO
ELAPSED-TIME THRESHOLD IS INTRODUCED — so there is no allowance to test against, and
THE TEST IS THE TRANSITION ITSELF.

G-Q7.2's DISCRIMINATOR is the whole test (MSG-0113 §3): query each transition TWICE — once
before the periodic timer has fired and once after. A DESIGN THAT FAILS THE FIRST AND PASSES
THE SECOND WAS MADE CORRECT BY WAITING, NOT BY THE TRANSITION.

The timer PERIOD below is a FIXTURE CONSTANT, present only so a "before" and an "after" exist.
It is NOT a proposed staleness threshold, and no magnitude is judged, proposed or recommended.

  PASS requires ALL of:  the prior version W1 does NOT answer after the transition;
                         where no current answerable version exists, the design ABSTAINS;
                         an EMPTY ANSWER IS NOT AN ABSTENTION (G-Q7.4).

------------------------------------------------------------------------------------------------------------
DESIGN T1 — materialised, PERIODIC re-materialisation only
------------------------------------------------------------------------------------------------------------
  transition     before timer                   after timer                    discriminator
  update         STALE ANSWER — FAIL            answered W2 — pass             MADE CORRECT BY WAITING — periodic, not transition-triggered
  approval       STALE ANSWER — FAIL            answered W2 — pass             MADE CORRECT BY WAITING — periodic, not transition-triggered
  revocation     STALE ANSWER — FAIL            EMPTY ANSWER — FAIL (G-Q7.4)   fails both — not a timing question
  supersession   STALE ANSWER — FAIL            EMPTY ANSWER — FAIL (G-Q7.4)   fails both — not a timing question
  reclassify     STALE ANSWER — FAIL            STALE ANSWER — FAIL            fails both — not a timing question
  unavailable    STALE ANSWER — FAIL            EMPTY ANSWER — FAIL (G-Q7.4)   fails both — not a timing question

  T1: 0 of 6 transitions pass at BOTH instants.

------------------------------------------------------------------------------------------------------------
DESIGN T2 — T1 + transition-triggered invalidation, hooked to LIFECYCLE STATE only
------------------------------------------------------------------------------------------------------------
  transition     before timer                   after timer                    discriminator
  update         answered W2 — pass             answered W2 — pass             transition-triggered
  approval       answered W2 — pass             answered W2 — pass             transition-triggered
  revocation     EMPTY ANSWER — FAIL (G-Q7.4)   EMPTY ANSWER — FAIL (G-Q7.4)   fails both — not a timing question
  supersession   EMPTY ANSWER — FAIL (G-Q7.4)   EMPTY ANSWER — FAIL (G-Q7.4)   fails both — not a timing question
  reclassify     STALE ANSWER — FAIL            STALE ANSWER — FAIL            fails both — not a timing question
  unavailable    STALE ANSWER — FAIL            EMPTY ANSWER — FAIL (G-Q7.4)   fails both — not a timing question

  T2: 2 of 6 transitions pass at BOTH instants.

------------------------------------------------------------------------------------------------------------
DESIGN T3 — T2 + kernel consult for currency + §3 point-2 re-check AGAINST THE KERNEL
------------------------------------------------------------------------------------------------------------
  transition     before timer                   after timer                    discriminator
  update         answered W2 — pass             answered W2 — pass             transition-triggered
  approval       answered W2 — pass             answered W2 — pass             transition-triggered
  revocation     ABSTAINED A4 — pass            ABSTAINED A4 — pass            transition-triggered
  supersession   ABSTAINED A4 — pass            ABSTAINED A4 — pass            transition-triggered
  reclassify     ABSTAINED A1 — pass            ABSTAINED A1 — pass            transition-triggered
  unavailable    ABSTAINED A7 — pass            ABSTAINED A7 — pass            transition-triggered

  T3: 6 of 6 transitions pass at BOTH instants.

------------------------------------------------------------------------------------------------------------
DESIGN T4 — authoritative partitioned store — the truth lives in the partition, no copy
------------------------------------------------------------------------------------------------------------
  transition     before timer                   after timer                    discriminator
  update         answered W2 — pass             answered W2 — pass             transition-triggered
  approval       answered W2 — pass             answered W2 — pass             transition-triggered
  revocation     ABSTAINED A4 — pass            ABSTAINED A4 — pass            transition-triggered
  supersession   ABSTAINED A4 — pass            ABSTAINED A4 — pass            transition-triggered
  reclassify     ABSTAINED A1 — pass            ABSTAINED A1 — pass            transition-triggered
  unavailable    ABSTAINED A7 — pass            ABSTAINED A7 — pass            transition-triggered

  T4: 6 of 6 transitions pass at BOTH instants.

------------------------------------------------------------------------------------------------------------
DESIGN T5 — as T3 but the re-check reads THE COPY (the faked re-check)
------------------------------------------------------------------------------------------------------------
  transition     before timer                   after timer                    discriminator
  update         answered W2 — pass             answered W2 — pass             transition-triggered
  approval       answered W2 — pass             answered W2 — pass             transition-triggered
  revocation     ABSTAINED A4 — pass            ABSTAINED A4 — pass            transition-triggered
  supersession   ABSTAINED A4 — pass            ABSTAINED A4 — pass            transition-triggered
  reclassify     STALE ANSWER — FAIL            STALE ANSWER — FAIL            fails both — not a timing question
  unavailable    ABSTAINED A7 — pass            ABSTAINED A7 — pass            transition-triggered

  T5: 5 of 6 transitions pass at BOTH instants.

------------------------------------------------------------------------------------------------------------
DESIGN NCF — NEGATIVE CONTROL — falls back to the last good snapshot on any difficulty
------------------------------------------------------------------------------------------------------------
  transition     before timer                   after timer                    discriminator
  update         STALE ANSWER — FAIL            answered W2 — pass             MADE CORRECT BY WAITING — periodic, not transition-triggered
  approval       STALE ANSWER — FAIL            answered W2 — pass             MADE CORRECT BY WAITING — periodic, not transition-triggered
  revocation     STALE ANSWER — FAIL            STALE ANSWER — FAIL            fails both — not a timing question
  supersession   STALE ANSWER — FAIL            STALE ANSWER — FAIL            fails both — not a timing question
  reclassify     STALE ANSWER — FAIL            STALE ANSWER — FAIL            fails both — not a timing question
  unavailable    STALE ANSWER — FAIL            STALE ANSWER — FAIL            fails both — not a timing question

  NCF: 0 of 6 transitions pass at BOTH instants.

  THE DISCRIMINATOR, READ ACROSS DESIGNS:
    4 (design, transition) cells were MADE CORRECT BY WAITING.
    Each of those is a design whose mechanism is a TIMER, passing a fixed-time test it should
    have failed. MSG-0113 §3: "Passing a fixed-time test alone does not establish the
    requirement." Under Q7 = A there is no elapsed-time allowance, so a cell that fails at
    T_Q1 FAILS — the later pass is not mitigation.
      T1    update         STALE ANSWER — FAIL  ->  answered W2 — pass
      T1    approval       STALE ANSWER — FAIL  ->  answered W2 — pass
      NCF   update         STALE ANSWER — FAIL  ->  answered W2 — pass
      NCF   approval       STALE ANSWER — FAIL  ->  answered W2 — pass

  FRESHNESS NEGATIVE CONTROL: NCF failed 6 of 6 cells -> the freshness section is VALID (§4.6 S8).

  T3 vs T5 — THE FAKED RE-CHECK, ISOLATED. §4.9 G-Q5.2b calls this "the limb most easily faked
  and the one that matters"; §4.10 result 3 demonstrated it once. It is reproduced here on a
  different fixture, and on the ONE transition that isolates it:
    T3 (re-check reads THE KERNEL): ABSTAINED A1 — pass
    T5 (re-check reads THE COPY)  : STALE ANSWER — FAIL
    -> SEPARATED. Same mechanism, same hooks, same consult; they differ ONLY in what the re-check reads,
       and one abstains while the other answers a version the kernel has reclassified.
       A "re-check" reading the stale copy's own attributes RE-CHECKS THE STALE DATA AGAINST
       ITSELF AND IS A NO-OP, exactly as G-Q5.2b says.

  ZERO STALE-ANSWER TOLERANCE, stated as a count rather than an impression. The counts below
  come from the RECORDED FLAGS, not from the displayed verdict string — a cell that is both
  stale and unauthorized displays only the first, and counting the strings would under-report
  the second. The categories therefore OVERLAP and do not sum to the cell total.

     15 of 36 (design, transition) cells answered the PRIOR version at at least one instant.
     12 of 36 answered a version UNAUTHORIZED for the subject at the query instant.
      6 of 36 returned an EMPTY ANSWER where an abstention was required (G-Q7.4).
      6 of 36 WITHHELD an authorized current version — the other failure direction.

    Under Q7 = A every stale and every unauthorized answer is a FAILURE with no allowance
    available to excuse it, because Q7 = A introduces NO threshold: there is nothing for a
    design to be within. An empty answer fails on G-Q7.4 independently of freshness.

  WHAT PASSING THIS SECTION DOES NOT BUY, stated so no row is misread.
    T3 and T4 pass every transition at both instants. THEY ARE STILL NOT CLEARED. §4.9 and
    §4.10 both say why in terms: these gates are NECESSARY AND NEVER SUFFICIENT, and §4.6 S6
    still governs clearance. §4.10 already demonstrated this rather than asserting it — design
    A6 met BOTH G-Q5 conditions and every G-Q7 requirement and is NOT CLEARED.
    E4 is unobtainable for these designs too (section 2), and section 4 measures their U.

============================================================================================================
SECTION 7 — VALIDITY GATE 2: the retrieval negative control (EPA-0006 §4.6 S8)
============================================================================================================
A deliberately non-conforming candidate must be included in EVERY probe run. If the harness
does not fail it, this run is VOID and its findings prove nothing.

  M=    50  U=   116 answered=1/4  -> FAILS, as required
  M=   500  U=   566 answered=1/4  -> FAILS, as required
  M=  5000  U=  5066 answered=1/4  -> FAILS, as required

  The retrieval control failed in 3 of 3 cases -> the run is VALID (§4.6 S8).

  THE ADVERSARIAL PRECONDITION HELD (section 1) AND THE CONTROLS FAILED AS REQUIRED. Both
  validity gates §4.6 S8 names are satisfied, so the measurements above are interpretable.

  Surface scan (not E4, and not offered as E4): engine-produced text surfaces, scanned for
  unauthorized passage text.
    5 surfaces scanned against 3 unauthorized passage bodies -> 0 occurrence(s).
    Parameters are BOUND, never inlined, so no passage text reaches the plan or the opcode
    operands. THIS SAYS NOTHING ABOUT AN ENGINE LOG, which is what E4 concerns.

============================================================================================================
SECTION 8 — EVIDENCE PER CANDIDATE, AND VERDICTS
============================================================================================================
§4.6 S6: CLEARED requires E1 + E2 + E3 + E4 ALL OBTAINED, U = 0 at every measured collection
size, shown not to grow with N. §4.6 S9: NOT CLEARED is the required answer wherever evidence
is absent. §4.6 S7-R2: the reported U is the MAXIMUM across exercised applicable placements.

  cand   U by size (max across placements) E2                          E4               G-Q4             verdict
  K7     50:7 500:71 5000:714           NOT OBTAINED — grows with N NOT OBTAINABLE   MET              NOT CLEARED
  K8     50:2 500:66 5000:709           NOT OBTAINED — grows with N NOT OBTAINABLE   MET              NOT CLEARED
  I5     50:7 500:71 5000:714           NOT OBTAINED — grows with N NOT OBTAINABLE   MET              NOT CLEARED
  I8     50:7 500:71 5000:714           NOT OBTAINED — grows with N NOT OBTAINABLE   MET              NOT CLEARED
  I7     50:0 500:0 5000:0              zero at every size — see below NOT OBTAINABLE   MET              NOT CLEARED
  KR     50:320 500:320 5000:714        NOT OBTAINED — grows with N NOT OBTAINABLE   FAILED (G-Q4.3)  NOT CLEARED

  EVERY CANDIDATE IS NOT CLEARED, AND E4 ALONE WOULD HAVE BEEN ENOUGH FOR THAT.
  Where E2 also fails, it fails on its own evidence and not on E4's absence.

  WHY EACH IS NOT CLEARED, ITEM BY ITEM — so no row is read as a near-miss:

    K7:
      - E4 NOT OBTAINABLE on this test subject (section 2) — §4.6 S6: absent class -> NOT CLEARED
      - E3 NEVER MEASURED — no opaque stage is instrumented here and §4.9 G-Q6 rejects construction
      - E2 NOT OBTAINED — U reaches 714 and GROWS with N (§4.6 S3: decisive failure)
      - implicit schema resolution during routing NEVER MEASURED (section 3.4)

    K8:
      - E4 NOT OBTAINABLE on this test subject (section 2) — §4.6 S6: absent class -> NOT CLEARED
      - E3 NEVER MEASURED — no opaque stage is instrumented here and §4.9 G-Q6 rejects construction
      - E2 NOT OBTAINED — U reaches 709 and GROWS with N (§4.6 S3: decisive failure)
      - implicit schema resolution during routing NEVER MEASURED (section 3.4)

    I5:
      - E4 NOT OBTAINABLE on this test subject (section 2) — §4.6 S6: absent class -> NOT CLEARED
      - E3 NEVER MEASURED — no opaque stage is instrumented here and §4.9 G-Q6 rejects construction
      - E2 NOT OBTAINED — U reaches 714 and GROWS with N (§4.6 S3: decisive failure)
      - implicit schema resolution during routing NEVER MEASURED (section 3.4)
      - structure/class counts at scale NEVER MEASURED — corpus unmeasured at n=1 (§11 #1)

    I8:
      - E4 NOT OBTAINABLE on this test subject (section 2) — §4.6 S6: absent class -> NOT CLEARED
      - E3 NEVER MEASURED — no opaque stage is instrumented here and §4.9 G-Q6 rejects construction
      - E2 NOT OBTAINED — U reaches 714 and GROWS with N (§4.6 S3: decisive failure)
      - implicit schema resolution during routing NEVER MEASURED (section 3.4)
      - structure/class counts at scale NEVER MEASURED — corpus unmeasured at n=1 (§11 #1)

    I7:
      - E4 NOT OBTAINABLE on this test subject (section 2) — §4.6 S6: absent class -> NOT CLEARED
      - E3 NEVER MEASURED — no opaque stage is instrumented here and §4.9 G-Q6 rejects construction
      - E2 not satisfied on a zero alone — §4.6 S5: a zero proves only that nothing crossed the instrument
      - the U1lb bound is VACUOUS in 3 of 3 cells (zero taken while the version cursor visited entries) — it proves nothing there, and is NOT U1 = 0
      - implicit schema resolution during routing NEVER MEASURED (section 3.4)
      - at the interval boundary the structure WITHHELD authorized content (section 5.1 event 1) and MISSED AN INGESTED VERSION inside the interval (event 2); G-Q5 applies to I7 in full and its abstention-on-breach limb is NEVER MEASURED

    KR:
      - E4 NOT OBTAINABLE on this test subject (section 2) — §4.6 S6: absent class -> NOT CLEARED
      - E3 NEVER MEASURED — no opaque stage is instrumented here and §4.9 G-Q6 rejects construction
      - E2 NOT OBTAINED — U reaches 714 and GROWS with N (§4.6 S3: decisive failure)
      - G-Q4.3 FAILED — the routing phase scans the structure catalogue
      - implicit schema resolution during routing NEVER MEASURED (section 3.4)

  WITHHOLDING CHECK (the K4 trap — U = 0 is purchasable by withholding authorized content):
    K7     answered a non-empty authorized set at every size in section 4: yes
    K8     answered a non-empty authorized set at every size in section 4: yes
    I5     answered a non-empty authorized set at every size in section 4: yes
    I8     answered a non-empty authorized set at every size in section 4: yes
    I7     answered a non-empty authorized set at every size in section 4: yes
    KR     answered a non-empty authorized set at every size in section 4: yes

    That check covers the SECTION 4 grid only, at the steady-state instant. I7's withholding
    failures are at the BOUNDARY and at INGESTION, and are in section 5.1 — a design can pass
    the steady-state anchor and still withhold, which is why both are reported.

  PRIOR VERDICTS, REPRODUCED UNCHANGED AND NOT RE-MEASURED HERE:
    MSG-0104's nine class verdicts            — unchanged
    §4.8's P0…NC design verdicts (TASK-0035)  — unchanged
    §4.10's A0…NC design verdicts (TASK-0037) — unchanged
    §4.11's K0…NC design verdicts (TASK-0038) — unchanged
    §4.12's K7 / K8 verdicts (TASK-0039)      — unchanged: BOTH NOT CLEARED
    K3 and K4 under MSG-0119's strict Q11     — unchanged: BOTH NOT CLEARED
    class D and class H                        — unchanged: DISQUALIFIED
  Nothing in this run relabels, re-runs or re-measures any of them.

============================================================================================================
RUN VALIDITY
============================================================================================================
  adversarial precondition       : HELD at every size and both distributions
  index-cursor calibration       : EXACT on both plans
  retrieval negative control     : failed 3/3 cases
  routing negative control       : FAILED G-Q4.2 as required
  freshness negative control     : failed 6/6 cells
  plan-transfer control          : 0/54 non-transferable
  measurements recorded          : 18 placement grid cells + 36 freshness cells

  No probe-internal defect was detected by the checks above.

  >> THIS RUN IS VALID. Both §4.6 S8 gates are satisfied and the measurements are interpretable.

NOTHING IS CLEARED. NOTHING IS SELECTED. NO ADR IS AMENDED. NO GATE IS RELAXED.
NO NUMERIC STALENESS THRESHOLD IS INTRODUCED. NO IMPLEMENTATION TASK IS AUTHORIZED.
============================================================================================================
